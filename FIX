
import com.kms.katalon.core.configuration.RunConfiguration
import com.kms.katalon.core.util.KeywordUtil

// --- Build paths ---
String reportFolder = RunConfiguration.getReportFolder()
File evidencesDir = new File(reportFolder, "Evidences")

KeywordUtil.logInfo("ReportFolder => " + reportFolder)
KeywordUtil.logInfo("Evidences folder (looking at) => " + evidencesDir.getAbsolutePath())

// --- 1) Folder not found ---
if (!evidencesDir.exists()) {
    KeywordUtil.markFailedAndStop("❌ Evidences folder NOT FOUND. Looking at: " + evidencesDir.getAbsolutePath())
}

// --- 2) Exists but is not a directory ---
if (!evidencesDir.isDirectory()) {
    KeywordUtil.markFailedAndStop("❌ Evidences path exists but is NOT a folder. Path: " + evidencesDir.getAbsolutePath())
}

// --- 3) Folder exists but empty ---
File[] folderFiles = evidencesDir.listFiles()
if (folderFiles == null || folderFiles.length == 0) {
    KeywordUtil.markFailedAndStop("⚠️ Evidences folder is EMPTY (export generated no files). Folder: " + evidencesDir.getAbsolutePath())
}

KeywordUtil.logInfo("Evidences files found => " + (folderFiles*.getName()))

// --- Example: validate your list 'files' before using get(0)/get(1) ---
if (files == null) {
    KeywordUtil.markFailedAndStop("❌ File list is NULL (compareFiles returned null). Evidences folder: " + evidencesDir.getAbsolutePath())
}
if (files.size() < 2) {
    KeywordUtil.markFailedAndStop("⚠️ Not enough files to compare. Found " + files.size() + " => " + files
        + " | Evidences folder: " + evidencesDir.getAbsolutePath())
}

// --- Build file paths (your original logic) ---
String file3 = evidencesDir.getAbsolutePath() + "\\" + files.get(0)
String file4 = evidencesDir.getAbsolutePath() + "\\" + files.get(1)

KeywordUtil.logInfo("Using file3 => " + file3)
KeywordUtil.logInfo("Using file4 => " + file4)

// --- 4) File not found ---
File f3 = new File(file3)
File f4 = new File(file4)

if (!f3.exists()) {
    KeywordUtil.markFailedAndStop("❌ File NOT FOUND: " + f3.getAbsolutePath() + " | Folder: " + evidencesDir.getAbsolutePath())
}
if (!f4.exists()) {
    KeywordUtil.markFailedAndStop("❌ File NOT FOUND: " + f4.getAbsolutePath() + " | Folder: " + evidencesDir.getAbsolutePath())
}

// --- 5) File opened (exists) but is empty ---
if (f3.length() == 0) {
    KeywordUtil.markFailedAndStop("⚠️ File exists but is EMPTY (0 bytes): " + f3.getAbsolutePath())
}
if (f4.length() == 0) {
    KeywordUtil.markFailedAndStop("⚠️ File exists but is EMPTY (0 bytes): " + f4.getAbsolutePath())
}


