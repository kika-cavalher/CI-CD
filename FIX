'ModuleDesResponsabiliteDimensions_Rec': [
	'firstDimension'   : 'Images/BDR/measures.PNG',
	'lastDimension'    : 'Images/BDR/provic.png',
	'lastDimensionOpen': 'Images/BDR/provicOpen.png',
	'firstPageRef'     : 'Images/BDR/FirstPageOfDimensionsRec.PNG',
	'lastPageRef'      : 'Images/BDR/LastPageOfDimensionsRec.PNG',
	'startOfDimensions': 'Images/BDR/startOfDimensions_Rec.png',
	'endOfDimensions'  : 'Images/BDR/endOfDimensions.PNG'
],

'ModuleDesResponsabiliteDimensions_PROD': [
	'firstDimension'   : 'Images/BDR/measures.PNG',
	'lastDimension'    : 'Images/BDR/provic.png',
	'lastDimensionOpen': 'Images/BDR/provicOpen.png',
	'firstPageRef'     : 'Images/BDR/FirstPageOfDimensionsPROD.PNG',
	'lastPageRef'      : 'Images/BDR/LastPageOfDimensionsPROD.PNG',
	'startOfDimensions': 'Images/BDR/startOfDimensions_PROD.PNG',
	'endOfDimensions'  : 'Images/BDR/endOfDimensions.PNG'
],








package commands

import java.awt.Robot
import java.awt.event.KeyEvent

import com.kms.katalon.core.annotation.Keyword
import com.kms.katalon.core.util.KeywordUtil
import com.kms.katalon.core.windows.keyword.WindowsBuiltinKeywords as Windows

class DimensionsNavigator {

	private ImageLoader imageLoader = new ImageLoader()

	@Keyword
	void validateDimensionsImagesExist(String imageGroup) {
		Map images = imageLoader.getImages(imageGroup)

		List<String> requiredKeys = [
			'firstDimension',
			'lastDimension',
			'lastDimensionOpen',
			'firstPageRef',
			'lastPageRef',
			'startOfDimensions',
			'endOfDimensions'
		]

		List<String> missingKeys = []

		requiredKeys.each { String key ->
			if (!images.containsKey(key)) {
				missingKeys.add(key)
			}
		}

		if (!missingKeys.isEmpty()) {
			KeywordUtil.markFailedAndStop(
				'Dimensions image group is missing required keys.\n' +
				'Image group: ' + imageGroup + '\n' +
				'Missing keys: ' + missingKeys.join(', ')
			)
		}

		KeywordUtil.logInfo('All dimensions images exist for group: ' + imageGroup)
	}

	@Keyword
	void clickDimensionImageOrStop(String imageGroup, String imageKey) {
		imageLoader.clickImageOrStop(imageGroup, imageKey)
	}

	@Keyword
	boolean isDimensionImageVisible(String imageGroup, String imageKey) {
		return imageLoader.isVisible(imageGroup, imageKey)
	}

	@Keyword
	void waitForDimensionImageOrStop(String imageGroup, String imageKey, Integer timeoutSeconds) {
		int safeTimeout = timeoutSeconds == null || timeoutSeconds <= 0 ? 15 : timeoutSeconds
		imageLoader.waitForImageOrStop(imageGroup, imageKey, safeTimeout)
	}

	@Keyword
	void clickFirstDimension(String imageGroup) {
		waitForDimensionImageOrStop(imageGroup, 'firstDimension', 15)
		imageLoader.clickImageOrStop(imageGroup, 'firstDimension')
		Windows.delay(1)
	}

	@Keyword
	void validateDimensionsCompleteFlow(String imageGroup, Integer maxDownMoves, Integer maxScrollMoves) {
		int safeDownMoves = maxDownMoves == null || maxDownMoves <= 0 ? 25 : maxDownMoves
		int safeScrollMoves = maxScrollMoves == null || maxScrollMoves <= 0 ? 40 : maxScrollMoves

		validateDimensionsImagesExist(imageGroup)

		KeywordUtil.logInfo('Step 1. Validate and click Measures.')
		waitForDimensionImageOrStop(imageGroup, 'firstDimension', 15)
		imageLoader.clickImageOrStop(imageGroup, 'firstDimension')
		Windows.delay(1)

		KeywordUtil.logInfo('Step 2. Navigate with RIGHT and DOWN until Provic open becomes visible.')
		boolean provicOpened = openDimensionsUntilProvicOpen(imageGroup, safeDownMoves)

		if (!provicOpened) {
			imageLoader.takeEvidence('DimensionsProvicOpenNotFound.png')

			KeywordUtil.markFailedAndStop(
				'Could not open dimensions tree until last open dimension.\n' +
				'Image group: ' + imageGroup + '\n' +
				'Expected image: lastDimensionOpen\n' +
				'Max DOWN moves: ' + safeDownMoves
			)
		}

		KeywordUtil.logInfo('Step 3. Scroll to top and validate first page reference.')
		boolean firstPageVisible = scrollUntilAnyVisible(
			imageGroup,
			['firstPageRef', 'startOfDimensions'],
			true,
			safeScrollMoves
		)

		if (!firstPageVisible) {
			imageLoader.takeEvidence('DimensionsFirstPageRefNotFound.png')

			KeywordUtil.markFailedAndStop(
				'Could not find first page dimensions reference after scrolling to top.\n' +
				'Image group: ' + imageGroup + '\n' +
				'Expected one of: firstPageRef, startOfDimensions\n' +
				'Max scroll moves: ' + safeScrollMoves
			)
		}

		KeywordUtil.logInfo('Step 4. Scroll to bottom and validate last page reference.')
		boolean lastPageVisible = scrollUntilAnyVisible(
			imageGroup,
			['lastPageRef', 'endOfDimensions'],
			false,
			safeScrollMoves
		)

		if (!lastPageVisible) {
			imageLoader.takeEvidence('DimensionsLastPageRefNotFound.png')

			KeywordUtil.markFailedAndStop(
				'Could not find last page dimensions reference after scrolling to bottom.\n' +
				'Image group: ' + imageGroup + '\n' +
				'Expected one of: lastPageRef, endOfDimensions\n' +
				'Max scroll moves: ' + safeScrollMoves
			)
		}

		KeywordUtil.logInfo('Dimensions flow validated successfully.')
	}

	@Keyword
	void openDimensionsUntilProvic(String imageGroup, Integer maxDownMoves) {
		int safeDownMoves = maxDownMoves == null || maxDownMoves <= 0 ? 25 : maxDownMoves

		boolean provicOpened = openDimensionsUntilProvicOpen(imageGroup, safeDownMoves)

		if (!provicOpened) {
			KeywordUtil.markFailedAndStop(
				'Could not open dimensions until Provic open.\n' +
				'Image group: ' + imageGroup
			)
		}
	}

	@Keyword
	void scrollDimensionsToTop(String imageGroup) {
		boolean visible = scrollUntilAnyVisible(
			imageGroup,
			['firstPageRef', 'startOfDimensions'],
			true,
			40
		)

		if (!visible) {
			KeywordUtil.markFailedAndStop(
				'Could not scroll dimensions to top reference.\n' +
				'Image group: ' + imageGroup
			)
		}
	}

	@Keyword
	void validateProvicOpen(String imageGroup, Integer timeoutSeconds) {
		waitForDimensionImageOrStop(imageGroup, 'lastDimensionOpen', timeoutSeconds)
	}

	@Keyword
	void validateMeasuresOpen(String imageGroup, Integer timeoutSeconds) {
		waitForDimensionImageOrStop(imageGroup, 'firstDimension', timeoutSeconds)
	}

	@Keyword
	void validateStartOfDimensions(String imageGroup, Integer timeoutSeconds) {
		waitForDimensionImageOrStop(imageGroup, 'startOfDimensions', timeoutSeconds)
	}

	@Keyword
	void validateEndOfDimensions(String imageGroup, Integer timeoutSeconds) {
		waitForDimensionImageOrStop(imageGroup, 'endOfDimensions', timeoutSeconds)
	}

	@Keyword
	void validateFirstPageReference(String imageGroup, Integer timeoutSeconds) {
		waitForDimensionImageOrStop(imageGroup, 'firstPageRef', timeoutSeconds)
	}

	@Keyword
	void validateLastPageReference(String imageGroup, Integer timeoutSeconds) {
		waitForDimensionImageOrStop(imageGroup, 'lastPageRef', timeoutSeconds)
	}

	private boolean openDimensionsUntilProvicOpen(String imageGroup, int maxDownMoves) {
		if (imageLoader.isVisible(imageGroup, 'lastDimensionOpen')) {
			return true
		}

		pressRight()
		Windows.delay(1)

		for (int i = 0; i < maxDownMoves; i++) {
			if (imageLoader.isVisible(imageGroup, 'lastDimensionOpen')) {
				return true
			}

			pressRight()
			Windows.delay(1)

			if (imageLoader.isVisible(imageGroup, 'lastDimensionOpen')) {
				return true
			}

			pressDown()
			Windows.delay(1)
		}

		return imageLoader.isVisible(imageGroup, 'lastDimensionOpen')
	}

	private boolean scrollUntilAnyVisible(String imageGroup, List<String> imageKeys, boolean scrollUp, int maxScrollMoves) {
		for (int i = 0; i < maxScrollMoves; i++) {
			for (String imageKey : imageKeys) {
				if (imageLoader.isVisible(imageGroup, imageKey)) {
					KeywordUtil.logInfo(
						'Visible dimensions reference found.\n' +
						'Image group: ' + imageGroup + '\n' +
						'Image key: ' + imageKey + '\n' +
						'Scroll direction: ' + (scrollUp ? 'UP' : 'DOWN') + '\n' +
						'Scroll step: ' + i
					)
					return true
				}
			}

			scrollOneStep(scrollUp)
			Windows.delay(1)
		}

		return false
	}

	private void pressRight() {
		Robot robot = new Robot()
		robot.setAutoDelay(150)

		robot.keyPress(KeyEvent.VK_RIGHT)
		robot.keyRelease(KeyEvent.VK_RIGHT)
	}

	private void pressDown() {
		Robot robot = new Robot()
		robot.setAutoDelay(150)

		robot.keyPress(KeyEvent.VK_DOWN)
		robot.keyRelease(KeyEvent.VK_DOWN)
	}

	private void scrollOneStep(boolean scrollUp) {
		Robot robot = new Robot()
		robot.setAutoDelay(150)

		if (scrollUp) {
			robot.mouseWheel(-3)
		} else {
			robot.mouseWheel(3)
		}
	}
}










import com.kms.katalon.core.util.KeywordUtil

String menuName = CustomKeywords.'pageObject.ModuleDesResponsabiliteKeywords.getMenuName'()

CustomKeywords.'jiraIntegration.JiraReportSteps.note'('Search and open menu: ' + menuName)
CustomKeywords.'pageObject.ModuleDesResponsabiliteKeywords.openMenu'(60)

CustomKeywords.'jiraIntegration.JiraReportSteps.note'('Validate opened menu screen: ' + menuName)
CustomKeywords.'pageObject.ModuleDesResponsabiliteKeywords.validateOpenedMenu'(60)

String dimensionsImageGroup = CustomKeywords.'pageObject.ModuleDesResponsabiliteKeywords.getDimensionsImageGroup'()

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Resolve dimensions image group according to current environment'
)
KeywordUtil.logInfo('Current dimensions image group: ' + dimensionsImageGroup)

CustomKeywords.'pageObject.ModuleDesResponsabiliteKeywords.validateDimensionsImagesExist'(dimensionsImageGroup)

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Validate dimensions flow: Measures visible, navigate until Provic open, scroll to top and validate first page, scroll to bottom and validate last page'
)

CustomKeywords.'pageObject.ModuleDesResponsabiliteKeywords.validateDimensionsCompleteFlow'(
	dimensionsImageGroup,
	25,
	40
)

CustomKeywords.'jiraIntegration.JiraReportSteps.note'('Take evidence after dimensions validation')
CustomKeywords.'pageObject.ModuleDesResponsabiliteKeywords.takeEvidence'('ValidateListDimensions.png')
