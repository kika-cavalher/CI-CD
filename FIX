Me ajude a criar um file BEM SIMPLES para o jenkins no katalon em que eu posso somente colocar o file no jenkins e ele já usa para configurar no lugar desse escrito direto no jenkins:


schtasks /run /tn "startWinDriver"
schtasks /run /tn "\eMatch Automation\ArchiveMode"
:loop
for /f "tokens=2 delims=: " %%f in ('schtasks /query /tn "\eMatch Automation\ArchiveMode" /fo list ^| find "Status:"' ) do (
    if "%%f"=="Running" (
        ping -n 6 localhost >nul 2>nul
        goto loop
    )
)
schtasks /end /tn "startWinDriver"


Também quero que ao inves de chamar um task schedule especifico que chame um arquivo bat generico run-ematch-command.bat que pega o valor de qual é o test suite collection automaticamente 
Ao inves desses arquivos bat para cada file 
C:\Users\adgi5appjenkins\Documents\Katalon_Studio_Engine_Windows_64-8.1.0\katalonc.exe -projectPath="D:\jenkins\work\workspace\GI5\eMatch\Archive Mode\label\GI5_SWDCFRNXGI55817"  katalonc -noSplash -runMode=console  -retry=0 -testSuiteCollectionPath="Test Suites/Archive Mode/Archive Mode Collection" -browserType="Windows"

Tambem quero que os testes sempre abram o app e executam para eu ver no remoto! Isso o test case j faz mas por algum motivo ele não ta abrindo. 
Pode gerar um file para o Archive Mode (em que todo o setup fica em cima e depois posso só copiar para os outros tests suites e alterar as variaveis em cima?
Deixe um log no jenkins com informação visual mas sem um arquivo muito grande

