1) Keywords/commands/DimensionsNavigator.groovy
package commands

import java.awt.Robot
import java.awt.event.KeyEvent

import com.kms.katalon.core.annotation.Keyword
import com.kms.katalon.core.util.KeywordUtil
import com.kms.katalon.core.windows.keyword.WindowsBuiltinKeywords as Windows

class DimensionsNavigator {

	private ImageLoader imageLoader = new ImageLoader()

	@Keyword
	void validateDimensionsImagesExist(String imageGroup) {
		Map<String, String> images = imageLoader.getImages(imageGroup)

		List<String> requiredKeys = [
			'firstDimension',
			'lastDimension',
			'lastDimensionOpen',
			'firstPageRef'
		]

		List<String> missingKeys = []

		requiredKeys.each { String key ->
			if (!images.containsKey(key)) {
				missingKeys.add(key)
			}
		}

		if (!missingKeys.isEmpty()) {
			KeywordUtil.markFailedAndStop(
				'Dimensions image group is missing required keys.\n' +
				'Image group: ' + imageGroup + '\n' +
				'Missing keys: ' + missingKeys.join(', ')
			)
		}

		KeywordUtil.logInfo('All dimensions images exist for group: ' + imageGroup)
	}

	@Keyword
	void validateMeasuresOpen(String imageGroup, Integer timeoutSeconds) {
		waitForDimensionImageOrStop(imageGroup, 'firstDimension', timeoutSeconds)
	}

	@Keyword
	void clickFirstDimension(String imageGroup) {
		waitForDimensionImageOrStop(imageGroup, 'firstDimension', 15)
		imageLoader.clickImageOrStop(imageGroup, 'firstDimension')
		Windows.delay(1)
	}

	@Keyword
	void openDimensionsUntilProvic(String imageGroup, Integer maxCycles) {
		int safeCycles = maxCycles == null || maxCycles <= 0 ? 65 : maxCycles

		boolean opened = openDimensionsTreeUntilBottomState(imageGroup, safeCycles)

		if (!opened) {
			imageLoader.takeEvidence('DimensionsBottomStateNotFound.png')

			KeywordUtil.markFailedAndStop(
				'Could not open dimensions tree until Provic area became visible.\n' +
				'Image group: ' + imageGroup + '\n' +
				'Expected images: lastDimensionOpen or lastDimension\n' +
				'Max cycles: ' + safeCycles
			)
		}
	}

	@Keyword
	void validateProvicOpen(String imageGroup, Integer timeoutSeconds) {
		waitForDimensionImageOrStop(imageGroup, 'lastDimensionOpen', timeoutSeconds)
	}

	@Keyword
	void validateLastDimension(String imageGroup, Integer timeoutSeconds) {
		waitForDimensionImageOrStop(imageGroup, 'lastDimension', timeoutSeconds)
	}

	@Keyword
	void scrollDimensionsToTop(String imageGroup) {
		boolean found = scrollUntilImageVisible(imageGroup, 'firstPageRef', true, 40)

		if (!found) {
			imageLoader.takeEvidence('DimensionsFirstPageRefNotFound.png')

			KeywordUtil.markFailedAndStop(
				'Could not scroll to top dimensions reference.\n' +
				'Image group: ' + imageGroup + '\n' +
				'Expected image: firstPageRef'
			)
		}
	}

	@Keyword
	void validateFirstPageReference(String imageGroup, Integer timeoutSeconds) {
		waitForDimensionImageOrStop(imageGroup, 'firstPageRef', timeoutSeconds)
	}

	@Keyword
	void validateDimensionsCompleteFlow(String imageGroup, Integer maxCycles, Integer maxScrollMoves) {
		int safeCycles = maxCycles == null || maxCycles <= 0 ? 65 : maxCycles
		int safeScrollMoves = maxScrollMoves == null || maxScrollMoves <= 0 ? 40 : maxScrollMoves

		validateDimensionsImagesExist(imageGroup)
		validateMeasuresOpen(imageGroup, 15)
		clickFirstDimension(imageGroup)
		openDimensionsUntilProvic(imageGroup, safeCycles)
		validateProvicOpen(imageGroup, 15)
		validateLastDimension(imageGroup, 15)

		boolean found = scrollUntilImageVisible(imageGroup, 'firstPageRef', true, safeScrollMoves)

		if (!found) {
			imageLoader.takeEvidence('DimensionsFirstPageRefNotFound.png')

			KeywordUtil.markFailedAndStop(
				'Could not find first page reference after scrolling to top.\n' +
				'Image group: ' + imageGroup + '\n' +
				'Expected image: firstPageRef\n' +
				'Max scroll moves: ' + safeScrollMoves
			)
		}

		validateFirstPageReference(imageGroup, 15)
	}

	private void waitForDimensionImageOrStop(String imageGroup, String imageKey, Integer timeoutSeconds) {
		int safeTimeout = timeoutSeconds == null || timeoutSeconds <= 0 ? 15 : timeoutSeconds
		imageLoader.waitForImageOrStop(imageGroup, imageKey, safeTimeout)
	}

	private boolean openDimensionsTreeUntilBottomState(String imageGroup, int maxCycles) {
		if (isBottomStateVisible(imageGroup)) {
			KeywordUtil.logInfo('Bottom dimensions state is already visible before navigation.')
			return true
		}

		for (int cycle = 1; cycle <= maxCycles; cycle++) {
			boolean firstCycle = cycle == 1
			int rightMoves = firstCycle ? 3 : 1

			KeywordUtil.logInfo(
				'Dimensions navigation cycle ' + cycle + '/' + maxCycles + '. Sequence: ' +
				(firstCycle ? 'RIGHT, RIGHT, RIGHT, DOWN' : 'RIGHT, DOWN')
			)

			for (int i = 0; i < rightMoves; i++) {
				pressRight()
				Windows.delay(1)

				if (isBottomStateVisible(imageGroup)) {
					KeywordUtil.logInfo(
						'Bottom dimensions state became visible after RIGHT move ' + (i + 1) + '.'
					)
					return true
				}
			}

			pressDown()
			Windows.delay(1)

			if (isBottomStateVisible(imageGroup)) {
				KeywordUtil.logInfo('Bottom dimensions state became visible after DOWN.')
				return true
			}
		}

		Windows.delay(2)

		return isBottomStateVisible(imageGroup)
	}

	private boolean isBottomStateVisible(String imageGroup) {
		return imageLoader.isVisible(imageGroup, 'lastDimensionOpen') ||
			   imageLoader.isVisible(imageGroup, 'lastDimension')
	}

	private boolean scrollUntilImageVisible(String imageGroup, String imageKey, boolean scrollUp, int maxScrollMoves) {
		for (int i = 0; i < maxScrollMoves; i++) {
			if (imageLoader.isVisible(imageGroup, imageKey)) {
				KeywordUtil.logInfo(
					'Image found during scroll.\n' +
					'Image group: ' + imageGroup + '\n' +
					'Image key: ' + imageKey + '\n' +
					'Scroll direction: ' + (scrollUp ? 'UP' : 'DOWN') + '\n' +
					'Scroll step: ' + i
				)
				return true
			}

			scrollOneStep(scrollUp)
			Windows.delay(1)
		}

		return false
	}

	private void pressRight() {
		Robot robot = new Robot()
		robot.setAutoDelay(150)

		robot.keyPress(KeyEvent.VK_RIGHT)
		robot.keyRelease(KeyEvent.VK_RIGHT)
	}

	private void pressDown() {
		Robot robot = new Robot()
		robot.setAutoDelay(150)

		robot.keyPress(KeyEvent.VK_DOWN)
		robot.keyRelease(KeyEvent.VK_DOWN)
	}

	private void scrollOneStep(boolean scrollUp) {
		Robot robot = new Robot()
		robot.setAutoDelay(150)

		if (scrollUp) {
			robot.mouseWheel(-3)
		} else {
			robot.mouseWheel(3)
		}
	}
}
2) Keywords/pageObject/ModuleDesResponsabiliteKeywords.groovy
Acrescenta só este keyword no file
@Keyword
void validateLastDimension(String dimensionsImageGroup, Integer timeoutSeconds) {
	dimensionsNavigator.validateLastDimension(dimensionsImageGroup, timeoutSeconds)
}
3) Test Cases/2. NOK/BDR/Module des Responsabilite/Validate List Dimensions
import com.kms.katalon.core.util.KeywordUtil

String dimensionsImageGroup = CustomKeywords.'pageObject.ModuleDesResponsabiliteKeywords.getDimensionsImageGroup'()

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Resolve dimensions image group for current screen'
)
KeywordUtil.logInfo('Current dimensions image group: ' + dimensionsImageGroup)

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Validate dimensions images exist for current screen'
)
CustomKeywords.'pageObject.ModuleDesResponsabiliteKeywords.validateDimensionsImagesExist'(
	dimensionsImageGroup
)

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Validate Measures image is visible'
)
CustomKeywords.'pageObject.ModuleDesResponsabiliteKeywords.validateMeasuresOpen'(
	dimensionsImageGroup,
	15
)

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Click Measures image to focus dimensions tree'
)
CustomKeywords.'pageObject.ModuleDesResponsabiliteKeywords.clickFirstDimension'(
	dimensionsImageGroup
)

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Navigate through dimensions until Provic area becomes visible'
)
CustomKeywords.'pageObject.ModuleDesResponsabiliteKeywords.openDimensionsUntilProvic'(
	dimensionsImageGroup,
	65
)

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Validate Provic open image at bottom'
)
CustomKeywords.'pageObject.ModuleDesResponsabiliteKeywords.validateProvicOpen'(
	dimensionsImageGroup,
	15
)

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Validate last dimension image at bottom'
)
CustomKeywords.'pageObject.ModuleDesResponsabiliteKeywords.validateLastDimension'(
	dimensionsImageGroup,
	15
)

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Scroll dimensions to top'
)
CustomKeywords.'pageObject.ModuleDesResponsabiliteKeywords.scrollDimensionsToTop'(
	dimensionsImageGroup
)

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Validate first page image after scrolling to top'
)
CustomKeywords.'pageObject.ModuleDesResponsabiliteKeywords.validateFirstPageReference'(
	dimensionsImageGroup,
	15
)

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Take evidence after dimensions validation'
)
CustomKeywords.'pageObject.ModuleDesResponsabiliteKeywords.takeEvidence'(
	'ValidateListDimensions.png'
)
