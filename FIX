Created by CAVALHER Erica (EXT) on Mar 26, 2026
JiraTestPlanManager is responsible for resolving the correct Jira Test Plan key for the current suite.  Centralizing these rules in a dedicated class keeps the flow easier to maintain.

Its main role is to decide between:

Reusing an existing Test Plan
Using a manually provided Test Plan key
Creating a new Test Plan
Use a fixed manually configured Test Plan
Reuse a previously saved Test Plan for the same suite
Validate whether the saved key is still correct
Search Jira for an existing matching Test Plan
Create a new one only when necessary


🏗️ Position in the architecture
This class is used by JiraFlowCoordinator.

It depends on:

• JiraFlowCoordinator owner
• JiraGateway gateway

It does not call Katalon request objects directly. All Jira calls go through the gateway.



🌐 Endpoints triggered by this class

Indirectly through JiraGateway:

• GET /rest/api/2/issue/{issueKey}
• GET /rest/api/2/search
• POST /rest/api/2/issue



🔧 Common changes maintainers may need
Reuse by Epic as well as summary
Extend search or validation logic.
Use latest created Test Plan even with partial match
Change filtering logic in findExistingTestPlanKey().
Stop storing local keys
Remove calls to saveSuiteTestPlanKey() and readSavedSuiteTestPlanKey().
Add stronger validation
Validate more Jira fields before accepting a saved key.


🔧 How to Reuse in a New Katalon Project
✏️ Review in the new project

Fixed Test Plan usage
Force create behavior
Saved key validation
Exact summary matching strategy


🔍 Main methods to validate

findExistingTestPlanKey()

getOrCreateTestPlanKey()



⚠️ Change this file when
The project wants a different Test Plan strategy

Exact name matching is not enough

Local saved key behavior should change

