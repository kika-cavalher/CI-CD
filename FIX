Perfect. Leave the BATs in the same folder as the others:

D:\Projects\eMatchStarters
✅ BAT you need to create there
D:\Projects\eMatchStarters\run-ematch-command.bat

Create it with this content:

@echo off
exit /b 0

This is only a placeholder. Jenkins will overwrite it before each run.

❌ BATs you do not need in this model

Since Katalon already opens and closes REC / PRD internally, do not use Jenkins BATs for:

openEmatch.bat
closeEmatch.bat

So the only BAT Jenkins needs now is:

D:\Projects\eMatchStarters\run-ematch-command.bat
✅ Task Scheduler paths
1. RunEMatchDesktop

In Action > Program/script, point to:

D:\Projects\eMatchStarters\run-ematch-command.bat

Not Desktop anymore.

2. startWinDriver

Keep it pointing to WinAppDriver executable, for example:

C:\Users\adgi5appjenkins\Desktop\Windows Application Driver\WinAppDriver.exe

That one is fine.

✅ Files that change

Only these 4 files need updates because of the new BAT location:

jenkinsIntegration/JenkinsDesktopTaskLogger.groovy
jenkinsIntegration/JenkinsDesktopPipelineRunner.groovy
jenkinsPipelines/Jenkinsfile_ArchiveMode.groovy
jenkinsPipelines/Jenkinsfile_PasserelleBilan.groovy
📄 jenkinsIntegration/JenkinsDesktopTaskLogger.groovy
/*
 * Responsibility:
 * Run one desktop Katalon asset through one generic scheduled task that executes
 * the generated command BAT in the interactive desktop session.
 */

def safeFileName(String value) {
    return (value ?: '').replaceAll(/[^A-Za-z0-9._ -]/, '').replace(' ', '_')
}

def runAsset(script, Map cfg, Map asset) {
    def assetKey = safeFileName(asset.name)

    return script.withEnv([
        "KATALON_EXE=${cfg.katalonExe}",
        "PROJECT_PATH=${cfg.projectPath}",
        "REPORTS_ROOT=${cfg.reportsRoot}",
        "ASSET_NAME=${asset.name}",
        "ASSET_TYPE=${asset.type}",
        "ASSET_PATH=${asset.path}",
        "ASSET_KEY=${assetKey}",
        "ASSET_TIMEOUT_MINUTES=${(asset.timeoutMinutes ?: cfg.defaultTimeoutMinutes).toString()}",
        "DESKTOP_RUNNER_TASK=${cfg.desktopRunnerTask}",
        "WIN_DRIVER_TASK=${cfg.winDriverTask}",
        "DESKTOP_COMMAND_FILE=${cfg.desktopCommandFile}"
    ]) {
        script.powershell(
            returnStatus: true,
            script: '''
$ErrorActionPreference = 'Stop'
$ProgressPreference = 'SilentlyContinue'

function Clean-Text {
    param([string]$Value)

    if ([string]::IsNullOrWhiteSpace($Value)) {
        return ''
    }

    return (($Value -replace '[\\r\\n]+', ' ') -replace '\\s+', ' ').Trim()
}

function Short-Text {
    param(
        [string]$Value,
        [int]$Max = 180
    )

    $text = Clean-Text $Value

    if ($text.Length -gt $Max) {
        return $text.Substring(0, $Max - 3) + '...'
    }

    return $text
}

function Read-NewContent {
    param(
        [string]$Path,
        [ref]$Offset
    )

    if (-not (Test-Path $Path)) {
        return @()
    }

    $fs = $null
    $sr = $null

    try {
        $fs = [System.IO.File]::Open($Path, [System.IO.FileMode]::Open, [System.IO.FileAccess]::Read, [System.IO.FileShare]::ReadWrite)
        [void]$fs.Seek($Offset.Value, [System.IO.SeekOrigin]::Begin)
        $sr = New-Object System.IO.StreamReader($fs)
        $content = $sr.ReadToEnd()
        $Offset.Value = $fs.Position

        if ([string]::IsNullOrWhiteSpace($content)) {
            return @()
        }

        return @($content -split "`r?`n")
    }
    catch {
        return @()
    }
    finally {
        if ($sr) { $sr.Close() }
        if ($fs) { $fs.Close() }
    }
}

function Start-TaskSafe {
    param([string]$TaskName)

    if ([string]::IsNullOrWhiteSpace($TaskName)) {
        return
    }

    Write-Host ("[INFO] Starting scheduled task: " + $TaskName)
    schtasks /run /tn $TaskName | Out-Host
}

function Stop-TaskSafe {
    param([string]$TaskName)

    if ([string]::IsNullOrWhiteSpace($TaskName)) {
        return
    }

    try {
        Write-Host ("[INFO] Stopping scheduled task: " + $TaskName)
        schtasks /end /tn $TaskName 1>$null 2>$null
    }
    catch {
    }
}

function Get-JUnitSummary {
    param([string]$JunitFile)

    $result = [ordered]@{
        Executed = 0
        Passed   = 0
        Failed   = 0
        Skipped  = 0
        Reason   = ''
    }

    if ([string]::IsNullOrWhiteSpace($JunitFile) -or -not (Test-Path $JunitFile)) {
        return $result
    }

    try {
        [xml]$xml = Get-Content $JunitFile

        $testcases = @()

        if ($xml.testsuite) {
            $testcases = @($xml.testsuite.testcase)
        }
        elseif ($xml.testsuites) {
            foreach ($ts in @($xml.testsuites.testsuite)) {
                $testcases += @($ts.testcase)
            }
        }

        $testcases = @($testcases | Where-Object { $_ })
        $result.Executed = $testcases.Count

        $failedNodes = @($testcases | Where-Object { $_.failure -or $_.error })
        $skippedNodes = @($testcases | Where-Object { $_.skipped })

        $result.Failed = $failedNodes.Count
        $result.Skipped = $skippedNodes.Count
        $result.Passed = $result.Executed - $result.Failed - $result.Skipped

        if ($failedNodes.Count -gt 0) {
            $firstBad = $failedNodes[0]

            if ($firstBad.failure) {
                $result.Reason = Short-Text (($firstBad.failure.message + ' ' + $firstBad.failure.InnerText)) 150
            }
            elseif ($firstBad.error) {
                $result.Reason = Short-Text (($firstBad.error.message + ' ' + $firstBad.error.InnerText)) 150
            }
        }
    }
    catch {
        $result.Reason = Short-Text $_.Exception.Message 150
    }

    return $result
}

function Find-LatestJunit {
    param([string]$Root)

    $candidate = Get-ChildItem -Path $Root -Filter 'JUnit_Report.xml' -File -Recurse -ErrorAction SilentlyContinue |
        Sort-Object LastWriteTime -Descending |
        Select-Object -First 1

    if ($candidate) {
        return $candidate.FullName
    }

    return ''
}

$logsDir = Join-Path $env:REPORTS_ROOT 'logs'
if (-not (Test-Path $logsDir)) {
    New-Item -ItemType Directory -Path $logsDir -Force | Out-Null
}

$stdoutLog = Join-Path $logsDir ($env:ASSET_KEY + '.desktop.log')
$statusFile = Join-Path $logsDir ($env:ASSET_KEY + '.status.txt')
$metaFile   = Join-Path $logsDir ($env:ASSET_KEY + '.meta.properties')

Remove-Item $stdoutLog -Force -ErrorAction SilentlyContinue
Remove-Item $statusFile -Force -ErrorAction SilentlyContinue
Remove-Item $metaFile   -Force -ErrorAction SilentlyContinue

$assetArgument = ''
if ($env:ASSET_TYPE -eq 'COLLECTION') {
    $assetArgument = '-testSuiteCollectionPath="' + $env:ASSET_PATH + '"'
}
else {
    $assetArgument = '-testSuitePath="' + $env:ASSET_PATH + '"'
}

$desktopBat = @"
@echo off
setlocal
del /f /q "$statusFile" 2>nul
"$env:KATALON_EXE" -noSplash -runMode=console -consoleLog -projectPath="$env:PROJECT_PATH" -retry=0 $assetArgument -browserType="Windows" -reportFolder="$env:REPORTS_ROOT" 1>> "$stdoutLog" 2>&1
set EXIT_CODE=%ERRORLEVEL%
> "$statusFile" echo EXIT_CODE=%EXIT_CODE%
exit /b %EXIT_CODE%
"@

Set-Content -Path $env:DESKTOP_COMMAND_FILE -Value $desktopBat -Encoding ASCII

Write-Host '------------------------------------------------------------'
Write-Host ('🚀 ASSET START | ' + $env:ASSET_NAME)
Write-Host ('📁 Path        | ' + $env:ASSET_PATH)
Write-Host ('🧪 Type        | ' + $env:ASSET_TYPE)
Write-Host ('📄 Command Bat | ' + $env:DESKTOP_COMMAND_FILE)
Write-Host '------------------------------------------------------------'

Start-TaskSafe -TaskName $env:WIN_DRIVER_TASK
Start-Sleep -Seconds 2
Start-TaskSafe -TaskName $env:DESKTOP_RUNNER_TASK

$stdoutOffset = 0L
$deadline = (Get-Date).AddMinutes([int]$env:ASSET_TIMEOUT_MINUTES)
$exitCode = 124

while ((Get-Date) -lt $deadline) {
    $stdoutLines = Read-NewContent -Path $stdoutLog -Offset ([ref]$stdoutOffset)

    foreach ($line in $stdoutLines) {
        $text = Clean-Text $line
        if (-not [string]::IsNullOrWhiteSpace($text)) {
            Write-Host ('[KATALON] ' + $text)
        }
    }

    if (Test-Path $statusFile) {
        $exitLine = Get-Content $statusFile -ErrorAction SilentlyContinue | Select-Object -First 1
        if ($exitLine -match 'EXIT_CODE=(\\d+)') {
            $exitCode = [int]$Matches[1]
        }
        else {
            $exitCode = 1
        }

        break
    }

    Start-Sleep -Seconds 2
}

$stdoutLines = Read-NewContent -Path $stdoutLog -Offset ([ref]$stdoutOffset)
foreach ($line in $stdoutLines) {
    $text = Clean-Text $line
    if (-not [string]::IsNullOrWhiteSpace($text)) {
        Write-Host ('[KATALON] ' + $text)
    }
}

if ($exitCode -eq 124) {
    Write-Host ('[TIMEOUT] Asset exceeded timeout of ' + $env:ASSET_TIMEOUT_MINUTES + ' minutes')
}

$junitFile = Find-LatestJunit -Root $env:REPORTS_ROOT
$junitSummary = Get-JUnitSummary -JunitFile $junitFile

$status = 'PASSED'
$icon = '✅'

if ($exitCode -eq 124) {
    $status = 'TIMEOUT'
    $icon = '⏰'
}
elseif ($exitCode -ne 0 -or $junitSummary.Failed -gt 0) {
    $status = 'FAILED'
    $icon = '❌'
}

$reason = $junitSummary.Reason
if ([string]::IsNullOrWhiteSpace($reason) -and $status -eq 'TIMEOUT') {
    $reason = 'Asset execution exceeded timeout'
}
if ([string]::IsNullOrWhiteSpace($reason) -and $status -eq 'FAILED') {
    $reason = 'Asset failed without explicit JUnit reason'
}

$metaLines = @()
$metaLines += ('SUITE_NAME=' + $env:ASSET_NAME)
$metaLines += ('SUITE_TYPE=' + $env:ASSET_TYPE)
$metaLines += ('SUITE_PATH=' + $env:ASSET_PATH)
$metaLines += ('STATUS=' + $status)
$metaLines += ('ICON=' + $icon)
$metaLines += ('EXIT_CODE=' + $exitCode)
$metaLines += ('EXECUTED=' + $junitSummary.Executed)
$metaLines += ('PASSED=' + $junitSummary.Passed)
$metaLines += ('FAILED=' + $junitSummary.Failed)
$metaLines += ('SKIPPED=' + $junitSummary.Skipped)
$metaLines += ('DURATION_SECONDS=0')
$metaLines += ('REASON=' + (Short-Text $reason 150))
$metaLines += ('SCREENSHOT_REL=')
$metaLines += ('LOG_REL=logs/' + $env:ASSET_KEY + '.desktop.log')
$metaLines += ('CASE_SUMMARY=')
$metaLines += ('STEP_SUMMARY=')

Set-Content -Path $metaFile -Value $metaLines -Encoding UTF8

Stop-TaskSafe -TaskName $env:DESKTOP_RUNNER_TASK
Stop-TaskSafe -TaskName $env:WIN_DRIVER_TASK

Write-Host '------------------------------------------------------------'
Write-Host ('🏁 ASSET END   | ' + $env:ASSET_NAME)
Write-Host ('📌 STATUS      | ' + $status)
Write-Host ('📄 LOG         | Reports/logs/' + $env:ASSET_KEY + '.desktop.log')
if (-not [string]::IsNullOrWhiteSpace($reason)) {
    Write-Host ('🧾 REASON      | ' + $reason)
}
Write-Host '------------------------------------------------------------'

exit $exitCode
'''
        )
    }
}

return this
📄 jenkinsIntegration/JenkinsDesktopPipelineRunner.groovy
/*
 * Responsibility:
 * Execute one desktop pipeline flow using the shared desktop task logger.
 */

def loadConsoleView(script) {
    return script.load('jenkinsIntegration/JenkinsConsoleView.groovy')
}

def loadResultStore(script) {
    return script.load('jenkinsIntegration/JenkinsResultStore.groovy')
}

def loadDesktopTaskLogger(script) {
    return script.load('jenkinsIntegration/JenkinsDesktopTaskLogger.groovy')
}

def validateSupportFiles(script, Map cfg) {
    script.bat """
@echo off
echo ------------------------------------------------------------
echo VALIDATING TOOLS AND SUPPORT FILES
echo ------------------------------------------------------------

if not exist "${cfg.katalonExe}" (
    echo [ERROR] Katalon executable not found
    echo ${cfg.katalonExe}
    exit /b 1
)

if not exist "${cfg.desktopCommandFile}" (
    echo [ERROR] Desktop command BAT not found
    echo ${cfg.desktopCommandFile}
    exit /b 1
)

schtasks /query /tn "${cfg.desktopRunnerTask}" >nul 2>nul
if errorlevel 1 (
    echo [ERROR] Desktop runner task not found
    echo ${cfg.desktopRunnerTask}
    exit /b 1
)

schtasks /query /tn "${cfg.winDriverTask}" >nul 2>nul
if errorlevel 1 (
    echo [ERROR] WinDriver task not found
    echo ${cfg.winDriverTask}
    exit /b 1
)

if not exist "${script.pwd()}\\jenkinsIntegration\\JenkinsConsoleView.groovy" (
    echo [ERROR] JenkinsConsoleView.groovy not found
    exit /b 1
)

if not exist "${script.pwd()}\\jenkinsIntegration\\JenkinsResultStore.groovy" (
    echo [ERROR] JenkinsResultStore.groovy not found
    exit /b 1
)

if not exist "${script.pwd()}\\jenkinsIntegration\\JenkinsDesktopTaskLogger.groovy" (
    echo [ERROR] JenkinsDesktopTaskLogger.groovy not found
    exit /b 1
)

echo [OK] Support files and scheduled tasks found
echo ------------------------------------------------------------
"""
}

def prepareArtifacts(script, String reportsRoot, def resultStore) {
    script.bat """
@echo off
if exist "${reportsRoot}" rmdir /s /q "${reportsRoot}"
mkdir "${reportsRoot}"
mkdir "${reportsRoot}\\logs"
"""

    resultStore.saveSuiteResults(script, reportsRoot, [])
}

def buildResultFromMeta(def resultStore, script, String reportsRoot, Map asset, int exitCode) {
    def meta = resultStore.readSuiteMeta(script, reportsRoot, asset.name)

    return [
        name              : asset.name,
        type              : meta['SUITE_TYPE'] ?: asset.type,
        path              : meta['SUITE_PATH'] ?: asset.path,
        status            : meta['STATUS'] ?: (exitCode == 0 ? 'PASSED' : (exitCode == 124 ? 'TIMEOUT' : 'FAILED')),
        icon              : meta['ICON'] ?: (exitCode == 0 ? '✅' : (exitCode == 124 ? '⏰' : '❌')),
        exitCode          : meta['EXIT_CODE'] ?: "${exitCode}",
        executed          : meta['EXECUTED'] ?: '0',
        passed            : meta['PASSED'] ?: '0',
        failed            : meta['FAILED'] ?: '0',
        skipped           : meta['SKIPPED'] ?: '0',
        durationSeconds   : meta['DURATION_SECONDS'] ?: '0',
        reason            : meta['REASON'] ?: '',
        screenshotRel     : '',
        primaryArtifactRel: '',
        logRel            : meta['LOG_REL'] ?: '',
        caseSummary       : resultStore.splitSummary(meta['CASE_SUMMARY'] ?: ''),
        stepSummary       : resultStore.splitSummary(meta['STEP_SUMMARY'] ?: '')
    ]
}

def run(script, Map cfg) {
    def consoleView = loadConsoleView(script)
    def resultStore = loadResultStore(script)
    def taskLogger = loadDesktopTaskLogger(script)

    def reportsRoot = "${script.pwd()}\\Reports"
    def suiteResults = []

    try {
        script.stage('Validate Environment') {
            validateSupportFiles(script, cfg)
        }

        script.stage('Prepare Artifacts') {
            prepareArtifacts(script, reportsRoot, resultStore)
        }

        script.stage(cfg.executionStageName ?: 'Run Test Assets') {
            cfg.assets.each { asset ->
                script.stage("Run - ${asset.name}") {
                    consoleView.printSuiteHeader(script, asset.name, asset.path, (asset.timeoutMinutes ?: cfg.defaultTimeoutMinutes).toString())

                    int exitCode = taskLogger.runAsset(script, [
                        katalonExe            : cfg.katalonExe,
                        projectPath           : "${script.pwd()}\\${cfg.projectFileName}",
                        reportsRoot           : reportsRoot,
                        defaultTimeoutMinutes : cfg.defaultTimeoutMinutes,
                        desktopRunnerTask     : cfg.desktopRunnerTask,
                        winDriverTask         : cfg.winDriverTask,
                        desktopCommandFile    : cfg.desktopCommandFile
                    ], asset)

                    def result = buildResultFromMeta(resultStore, script, reportsRoot, asset, exitCode)

                    suiteResults << result
                    resultStore.saveSuiteResults(script, reportsRoot, suiteResults)

                    consoleView.printSuiteResult(script, result)
                    consoleView.printRunningSummary(script, suiteResults)

                    if (result.status != 'PASSED') {
                        script.currentBuild.result = 'FAILURE'
                        script.error("${result.name} finished with status ${result.status}")
                    }
                }
            }
        }
    }
    finally {
        try {
            suiteResults = resultStore.loadSuiteResults(script, reportsRoot)
        }
        catch (ignored) {
        }

        try {
            consoleView.printFinalSummary(script, suiteResults, script.currentBuild.currentResult)
        }
        catch (ignored) {
        }

        script.catchError(buildResult: 'SUCCESS', stageResult: 'FAILURE') {
            script.junit allowEmptyResults: true, testResults: 'Reports/**/JUnit_Report.xml,Reports/logs/*.junit.xml'
        }

        script.catchError(buildResult: 'SUCCESS', stageResult: 'FAILURE') {
            script.archiveArtifacts artifacts: 'Reports/**/*', allowEmptyArchive: true
        }

        if (script.currentBuild.currentResult == 'SUCCESS') {
            def nextJobName = (cfg.nextJobName ?: '').trim()
            if (nextJobName) {
                script.echo "Triggering next Jenkins job: ${nextJobName}"
                script.build job: nextJobName, wait: false, propagate: false
            }
        }
    }
}

return this
📄 jenkinsPipelines/Jenkinsfile_ArchiveMode.groovy
/*
 * Responsibility:
 * Dedicated Jenkins pipeline for Archive Mode build.
 * This build triggers the next job when successful.
 */

def PIPELINE_CONFIG = [
    agentLabel             : 'GI5_SWDCFRNXGI55817',
    pipelineTimeoutMinutes : 180,
    defaultTimeoutMinutes  : 30,
    executionStageName     : 'Run Test Assets',
    projectFileName        : 'eMatch POC.prj',
    katalonExe             : 'C:\\Users\\adgi5appjenkins\\Documents\\Katalon_Studio_Engine_Windows_64-8.1.0\\katalonc.exe',
    winDriverTask          : 'startWinDriver',
    desktopRunnerTask      : 'RunEMatchDesktop',
    desktopCommandFile     : 'D:\\Projects\\eMatchStarters\\run-ematch-command.bat',
    nextJobName            : 'Passerelle Bilan',
    assets                 : [
        [
            name : 'Archive Mode',
            type : 'SUITE',
            path : 'Test Suites/Archive Mode/Archive Mode',
            timeoutMinutes : 30
        ],
        [
            name : 'Archive Mode Collection',
            type : 'COLLECTION',
            path : 'Test Suites/Archive Mode/Archive Mode Collection',
            timeoutMinutes : 30
        ]
    ]
]

node(PIPELINE_CONFIG.agentLabel) {
    timestamps {
        ansiColor('xterm') {
            timeout(time: PIPELINE_CONFIG.pipelineTimeoutMinutes, unit: 'MINUTES') {
                stage('Checkout') {
                    checkout scm
                }

                def runner = load('jenkinsIntegration/JenkinsDesktopPipelineRunner.groovy')
                runner.run(this, PIPELINE_CONFIG)
            }
        }
    }
}
📄 jenkinsPipelines/Jenkinsfile_PasserelleBilan.groovy
/*
 * Responsibility:
 * Dedicated Jenkins pipeline for Passerelle Bilan build.
 * This build does not trigger any next job by default.
 */

def PIPELINE_CONFIG = [
    agentLabel             : 'GI5_SWDCFRNXGI55817',
    pipelineTimeoutMinutes : 180,
    defaultTimeoutMinutes  : 30,
    executionStageName     : 'Run Test Assets',
    projectFileName        : 'eMatch POC.prj',
    katalonExe             : 'C:\\Users\\adgi5appjenkins\\Documents\\Katalon_Studio_Engine_Windows_64-8.1.0\\katalonc.exe',
    winDriverTask          : 'startWinDriver',
    desktopRunnerTask      : 'RunEMatchDesktop',
    desktopCommandFile     : 'D:\\Projects\\eMatchStarters\\run-ematch-command.bat',
    nextJobName            : '',
    assets                 : [
        [
            name : 'Passerelle Bilan',
            type : 'SUITE',
            path : 'Test Suites/PasserelleBilan/Passerelle Bilan',
            timeoutMinutes : 30
        ],
        [
            name : 'Passerelle Bilan Collection',
            type : 'COLLECTION',
            path : 'Test Suites/PasserelleBilan/Passerelle Bilan Collection',
            timeoutMinutes : 30
        ]
    ]
]

node(PIPELINE_CONFIG.agentLabel) {
    timestamps {
        ansiColor('xterm') {
            timeout(time: PIPELINE_CONFIG.pipelineTimeoutMinutes, unit: 'MINUTES') {
                stage('Checkout') {
                    checkout scm
                }

                def runner = load('jenkinsIntegration/JenkinsDesktopPipelineRunner.groovy')
                runner.run(this, PIPELINE_CONFIG)
            }
        }
    }
}
BAT you must create
D:\Projects\eMatchStarters\run-ematch-command.bat

Create exactly this:

@echo off
exit /b 0

Jenkins will rewrite it automatically on every run.

Task Scheduler setup
RunEMatchDesktop

Create or edit it like this:

Action

Program/script:

D:\Projects\eMatchStarters\run-ematch-command.bat
General
Run only when user is logged on
Run with highest privileges
Triggers

None

Settings
Allow task to be run on demand
If already running:
Do not start a new instance
startWinDriver

Keep:

Program/script:

C:\Users\adgi5appjenkins\Desktop\Windows Application Driver\WinAppDriver.exe
Wh
