import static com.kms.katalon.core.testcase.TestCaseFactory.findTestCase

import com.kms.katalon.core.model.FailureHandling
import com.kms.katalon.core.util.KeywordUtil
import com.kms.katalon.core.webui.keyword.WebUiBuiltInKeywords as WebUI
import com.kms.katalon.core.windows.keyword.WindowsBuiltinKeywords as Windows


// ======================================================
// EXPORT CONFIG
// ======================================================
String targetEnvironment = 'REC'
String exportLabel = 'EUR'
String filterValue = '228'

String loginTestCase = 'Component/Authentication/Login ' + targetEnvironment
String closeApplicationTestCase = 'Component/Authentication/Close Application'

String currencySetupTestCase = 'Feature/Contre-valorisation IHM/Settings/Cotre Parameters - ' + exportLabel
String filterTestCase = 'Feature/Contre-valorisation IHM/PLEconomique/FilterSection'

String menuName = 'PL Économique'
String menuResultName = 'PL Économique'

String scenarioName = targetEnvironment + ' Cotre Parameters ' + exportLabel + ' Filter Export'

// ======================================================
// VALIDATION CONFIG
// ======================================================
String requiredHeaders = 'Devise|Devise de ref.|Montant ccy|Montant base ccy'
String currencyHeaders = 'Devise|Devise de ref.'
String amountHeaders = 'Montant ccy|Montant base ccy'

// ======================================================
// TIMEOUT CONFIG
// ======================================================
Integer menuTimeout = 60
Integer screenLoadDelay = 40

// ======================================================
// TEST FLOW
// ======================================================
CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Authenticate and open application\n' +
	'Environment: ' + targetEnvironment
)

WebUI.callTestCase(
	findTestCase(loginTestCase),
	[:],
	FailureHandling.STOP_ON_FAILURE
)

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Configure Cotre Parameters ' + exportLabel + '\n' +
	'Test case: ' + currencySetupTestCase
)

WebUI.callTestCase(
	findTestCase(currencySetupTestCase),
	[:],
	FailureHandling.STOP_ON_FAILURE
)

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Open PL Economique menu\n' +
	'Search: ' + menuName + '\n' +
	'Result: ' + menuResultName
)

CustomKeywords.'component.MenuNavigator.searchAndOpenExactMenuByText'(
	menuName,
	menuResultName,
	menuTimeout
)

Windows.delay(screenLoadDelay)

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Apply Recherche filter\n' +
	'Filter test case: ' + filterTestCase + '\n' +
	'Filter value: ' + filterValue
)

CustomKeywords.'component.RechercheFilterKeywords.applyFilterTestCase'(
	filterTestCase,
	filterValue
)

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Export PL Economique file for ' + exportLabel + '\n' +
	'Export label: ' + exportLabel + '\n' +
	'Environment: ' + targetEnvironment
)

CustomKeywords.'commands.ExportFile.exportFromTwoExportImagesToNo'(
	exportLabel,
	targetEnvironment
)

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Close application\n' +
	'Environment: ' + targetEnvironment
)

WebUI.callTestCase(
	findTestCase(closeApplicationTestCase),
	[:],
	FailureHandling.STOP_ON_FAILURE
)

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Validate exported file\n' +
	'Environment: ' + targetEnvironment + '\n' +
	'Export label: ' + exportLabel
)

WebUI.callTestCase(
	findTestCase('Test Cases/Component/Export Files/Export File Validation'),
	[
		('targetEnvironment') : targetEnvironment,
		('exportLabel')       : exportLabel,
		('requiredHeaders')   : requiredHeaders,
		('currencyHeaders')   : currencyHeaders,
		('amountHeaders')     : amountHeaders
	],
	FailureHandling.STOP_ON_FAILURE
)

KeywordUtil.markPassed(
	scenarioName + ' completed successfully.'
)














import static com.kms.katalon.core.testobject.ObjectRepository.findWindowsObject

import java.awt.Robot
import java.awt.event.KeyEvent

import com.kms.katalon.core.model.FailureHandling
import com.kms.katalon.core.testobject.WindowsTestObject
import com.kms.katalon.core.testobject.WindowsTestObject.LocatorStrategy
import com.kms.katalon.core.util.KeywordUtil
import com.kms.katalon.core.windows.keyword.WindowsBuiltinKeywords as Windows

// ======================================================
// CHANGE CURRENCY
// ======================================================
String exportLabel = 'EUR'

// ======================================================
// ELEMENT CONFIG
// ======================================================
String settingsFooterName = 'MNU_FILE'
String contreValorisationName = 'Contre-valorisation'
String modulesFooterName = 'Modules'

// ======================================================
// TIMEOUT CONFIG
// ======================================================
Integer timeout = 10
Integer delayAfterClick = 2
Integer delayAfterPopupOpen = 2

// ======================================================
// TEST FLOW
// ======================================================
Robot robot = new Robot()
robot.setAutoDelay(120)

def buildNameObject = { String elementName ->
	WindowsTestObject object = new WindowsTestObject()
	object.setLocatorStrategy(LocatorStrategy.NAME)
	object.setLocator(elementName)
	return object
}

def clickByName = { String elementName, String description ->
	WindowsTestObject object = buildNameObject(elementName)

	Windows.waitForElementPresent(
		object,
		timeout,
		FailureHandling.STOP_ON_FAILURE
	)

	CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
		description + '\n' +
		'Element: ' + elementName
	)

	Windows.click(object)
	Windows.delay(delayAfterClick)
}

def pressKey = { Integer keyCode ->
	robot.keyPress(keyCode)
	robot.keyRelease(keyCode)
	Windows.delay(1)
}

def typeCurrencyByKeyboard = { String currency ->
	currency.each { String character ->
		String upperCharacter = character.toUpperCase()

		if (upperCharacter == ' ') {
			pressKey(KeyEvent.VK_SPACE)
		} else {
			int keyCode = KeyEvent."VK_${upperCharacter}"

			robot.keyPress(keyCode)
			robot.keyRelease(keyCode)
			Windows.delay(1)
		}
	}
}

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Open Settings'
)

clickByName(
	settingsFooterName,
	'Open Settings footer'
)

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Open Cotre Parameters'
)

clickByName(
	contreValorisationName,
	'Open Cotre Parameters setting'
)

Windows.delay(delayAfterPopupOpen)

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Move focus to currency field\n' +
	'Action: TAB x2'
)

pressKey(KeyEvent.VK_TAB)
pressKey(KeyEvent.VK_TAB)

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Select currency\n' +
	'Currency: ' + exportLabel + '\n' +
	'Input method: keyboard typing'
)

typeCurrencyByKeyboard(exportLabel)

Windows.delay(1)

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Confirm selected currency\n' +
	'Action: TAB to Valider and ENTER'
)

pressKey(KeyEvent.VK_TAB)
pressKey(KeyEvent.VK_ENTER)

Windows.delay(5)

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Return to Modules'
)

clickByName(
	modulesFooterName,
	'Return to Modules footer'
)

KeywordUtil.markPassed(
	'Cotre Parameters ' + exportLabel + ' configured successfully.'
)






















