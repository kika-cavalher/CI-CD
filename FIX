
pipeline {
    agent { label 'GI5_SWDCFRNXGI55817' }

    stages {
        
    stage('Start Gaia Backend and Frontend'){
        steps{
            script{
                echo "Start Gaia Backend and Frontend"
                def setupTask = "GaiaBackFrontSetup"
                bat "schtasks /run /tn \"${setupTask}\""
                echo "Wait 180 seconds for the backend and frontend to start..."
                sleep(time: 300, unit: 'SECONDS')
        
            }
            
            
        }
    }
      
    stage('Get bitbucket source code') {
         steps {
            cleanWs()
            git credentialsId: '965eceec-b3a3-4ec5-8820-a857c87dd80f', url: 'https://bitbucket-mut.mycloud.intrabpce.fr/scm/gi5/gaia-portal.git'
         }
    }
    
    
    
    stage('GAIA TSC') {
        steps {
            catchError(buildResult: 'SUCCESS', stageResult: 'FAILURE') {
                executeKatalon executeArgs: '-projectPath="D:/jenkins/work/workspace/GI5/Gaia/Gaia" -browserType="Chrome" -testSuiteCollectionPath="Test Suites/GAIA TSC"', location: 'C:/Users/adgi5appjenkins/Documents/Katalon_Studio_Engine_Windows_64-8.1.0', version: '', x11Display: '', xvfbConfiguration: ''
            }
        }
    }
    
 
    
}
    
}

.bat runner (fixo) — salva em C:\Gaia\run_visible_from_file.bat
@echo off
setlocal

set "KATALON_HOME=C:\Users\adgi5appjenkins\Documents\Katalon_Studio_Engine_Windows_64-8.1.0"
set "ARGS_FILE=C:\Gaia\katalon_args.txt"

if not exist "%ARGS_FILE%" (
  echo [bat] ERROR: args file not found: %ARGS_FILE%
  exit /b 2
)

set /p KATALON_ARGS=<"%ARGS_FILE%"

"%KATALON_HOME%\katalonc.exe" -noSplash -runMode=console %KATALON_ARGS% -browserType="Chrome" -noHeadless
exit /b %ERRORLEVEL%
2) Task Scheduler (GaiaKatalon_Visible) chama só esse .bat

Program: C:\Windows\System32\cmd.exe

Arguments:

/c "C:\Gaia\run_visible_from_file.bat"

✅ Nunca mais muda a task.

3) Jenkins escreve o arquivo e dispara a task

No Jenkins stage:

bat """
  echo -projectPath="D:/jenkins/work/workspace/GI5/Gaia/Gaia" -testSuiteCollectionPath="Test Suites/GAIA TSC" -consoleLog -logLevel=DEBUG > C:\\Gaia\\katalon_args.txt
  schtasks /run /tn "GaiaKatalon_Visible"
"""

✅ Agora você muda suites só no Jenkins, e continua vendo o Chrome.
