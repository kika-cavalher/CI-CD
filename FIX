package commands

import static com.kms.katalon.core.testobject.ObjectRepository.findWindowsObject

import java.awt.Robot
import java.awt.Toolkit
import java.awt.datatransfer.StringSelection
import java.awt.event.KeyEvent
import java.text.SimpleDateFormat

import com.kms.katalon.core.annotation.Keyword
import com.kms.katalon.core.configuration.RunConfiguration
import com.kms.katalon.core.util.KeywordUtil
import com.kms.katalon.core.windows.keyword.WindowsBuiltinKeywords as Windows

import internal.GlobalVariable

class ExportFile {

	private ImageLoader imageLoader = new ImageLoader()

	@Keyword
	void exportFromOutilsToNo(String exportLabel) {
		KeywordUtil.logInfo('Step 1. Open Outils menu.')
		imageLoader.waitForImageOrStop('General', 'outilsMenu', 15)
		imageLoader.clickImageOrStop('General', 'outilsMenu')
		Windows.delay(2)

		KeywordUtil.logInfo('Step 2. Validate Export button and click it.')
		imageLoader.waitForImageOrStop('General', 'exportButton', 15)
		imageLoader.clickImageOrStop('General', 'exportButton')
		Windows.delay(3)

		KeywordUtil.logInfo('Step 3. Save file inside report Evidences folder.')
		saveToReportEvidencePath()

		KeywordUtil.logInfo('Step 4. If Kodal asks to open the exported file, click No.')
		clickNoUntilOpenFileModalCloses(2, 10)

		KeywordUtil.logInfo('Export flow finished without opening exported file.')
	}

	private void saveToReportEvidencePath() {
		Robot robot = new Robot()
		robot.setAutoDelay(150)

		String directory = RunConfiguration.getReportFolder() + '\\Evidences'
		new File(directory).mkdirs()

		String fileName = buildAutomaticExportFileName()

		KeywordUtil.logInfo('Export directory: ' + directory)
		KeywordUtil.logInfo('Export file name: ' + fileName)

		Windows.click(findWindowsObject('Object Repository/Acapulco/filePath'))
		Windows.delay(2)

		pasteAndEnter(directory)

		Windows.delay(2)

		setFileName(fileName)

		Windows.click(findWindowsObject('General/btnSave'))
		Windows.delay(5)
	}

	private String buildAutomaticExportFileName() {
		String environment = resolveEnvironmentName()
		String timestamp = new SimpleDateFormat('yyyyMMdd_HHmmss').format(new Date())
		String featureName = resolveCurrentFeatureName()

		return sanitizeFileName(environment + '_' + timestamp + '_' + featureName) + '.xlsx'
	}

	private String resolveEnvironmentName() {
		try {
			if (GlobalVariable.environment != null && !GlobalVariable.environment.toString().trim().isEmpty()) {
				return GlobalVariable.environment.toString().trim().toUpperCase()
			}
		} catch (Exception ignored) {
		}

		try {
			if (GlobalVariable.env != null && !GlobalVariable.env.toString().trim().isEmpty()) {
				return GlobalVariable.env.toString().trim().toUpperCase()
			}
		} catch (Exception ignored) {
		}

		try {
			if (GlobalVariable.ENV != null && !GlobalVariable.ENV.toString().trim().isEmpty()) {
				return GlobalVariable.ENV.toString().trim().toUpperCase()
			}
		} catch (Exception ignored) {
		}

		return 'REC'
	}

	private String resolveCurrentFeatureName() {
		String reportFolder = RunConfiguration.getReportFolder()

		if (reportFolder == null || reportFolder.trim().isEmpty()) {
			return 'Export'
		}

		List<String> parts = reportFolder.replace('/', '\\').split('\\\\') as List<String>

		for (int i = parts.size() - 1; i >= 0; i--) {
			String part = parts[i] == null ? '' : parts[i].trim()

			if (part.isEmpty()) {
				continue
			}

			if (part ==~ /\d{8}_\d{6}/ || part ==~ /\d{14}/) {
				continue
			}

			if (part.equalsIgnoreCase('Reports') ||
					part.equalsIgnoreCase('Evidences') ||
					part.equalsIgnoreCase('Exports')) {
				continue
			}

			return part
		}

		return 'Export'
	}

	private String sanitizeFileName(String text) {
		if (text == null || text.trim().isEmpty()) {
			return 'Export'
		}

		return text
				.replaceAll('[\\\\/:*?"<>|]', '_')
				.replaceAll('\\s+', '_')
				.trim()
	}

	private void setFileName(String fileName) {
		boolean fileNameFieldFound = false

		try {
			Windows.click(findWindowsObject('General/txtFileName'))
			Windows.delay(1)
			fileNameFieldFound = true
		} catch (Exception ignored) {
			KeywordUtil.logInfo('General/txtFileName was not found. Trying shortcut to focus file name field.')
		}

		if (!fileNameFieldFound) {
			focusFileNameFieldWithShortcut()
			Windows.delay(1)
		}

		clearFocusedField()
		pasteText(fileName)
		Windows.delay(1)
	}

	private void clickNoUntilOpenFileModalCloses(Integer interval, Integer maxTimes) {
		Integer safeInterval = interval == null || interval <= 0 ? 2 : interval
		Integer safeMaxTimes = maxTimes == null || maxTimes <= 0 ? 10 : maxTimes

		for (int attempt = 1; attempt <= safeMaxTimes; attempt++) {
			if (imageLoader.waitForImage('General', 'no', safeInterval)) {
				imageLoader.clickImageOrStop('General', 'no')
				Windows.delay(2)
				KeywordUtil.logInfo('Open file modal closed with No. Attempt: ' + attempt)
				continue
			}

			if (imageLoader.waitForImage('General', 'noEn', safeInterval)) {
				imageLoader.clickImageOrStop('General', 'noEn')
				Windows.delay(2)
				KeywordUtil.logInfo('Open file modal closed with NoEN. Attempt: ' + attempt)
				continue
			}

			KeywordUtil.logInfo('Open file modal is no longer visible.')
			return
		}

		if (imageLoader.waitForImage('General', 'no', 1) || imageLoader.waitForImage('General', 'noEn', 1)) {
			imageLoader.takeEvidence('OpenFileModalStillVisible.png')

			KeywordUtil.markFailedAndStop(
					'Open file modal remained visible after clicking No.\n' +
					'Image group: General\n' +
					'Expected image: no or noEn'
					)
		}
	}

	private void focusFileNameFieldWithShortcut() {
		Robot robot = new Robot()
		robot.setAutoDelay(150)

		robot.keyPress(KeyEvent.VK_ALT)
		robot.keyPress(KeyEvent.VK_N)
		robot.keyRelease(KeyEvent.VK_N)
		robot.keyRelease(KeyEvent.VK_ALT)
	}

	private void clearFocusedField() {
		Robot robot = new Robot()
		robot.setAutoDelay(150)

		robot.keyPress(KeyEvent.VK_CONTROL)
		robot.keyPress(KeyEvent.VK_A)
		robot.keyRelease(KeyEvent.VK_A)
		robot.keyRelease(KeyEvent.VK_CONTROL)

		Windows.delay(1)

		robot.keyPress(KeyEvent.VK_BACK_SPACE)
		robot.keyRelease(KeyEvent.VK_BACK_SPACE)
	}

	private void pasteAndEnter(String text) {
		pasteText(text)

		Robot robot = new Robot()
		robot.setAutoDelay(150)

		robot.keyPress(KeyEvent.VK_ENTER)
		robot.keyRelease(KeyEvent.VK_ENTER)
	}

	private void pasteText(String text) {
		StringSelection selection = new StringSelection(text)
		Toolkit.getDefaultToolkit().getSystemClipboard().setContents(selection, null)

		Windows.delay(1)

		Robot robot = new Robot()
		robot.setAutoDelay(150)

		robot.keyPress(KeyEvent.VK_CONTROL)
		robot.keyPress(KeyEvent.VK_V)
		robot.keyRelease(KeyEvent.VK_V)
		robot.keyRelease(KeyEvent.VK_CONTROL)
	}
}
