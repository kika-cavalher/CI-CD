📁 jenkinsFile/EcranRapDetaille.groovy
properties([
    disableConcurrentBuilds(),
    buildDiscarder(logRotator(numToKeepStr: '15'))
])

def NODE_LABEL = 'GI5_SWDCFRNXGI55817'
def SHORT_WORKSPACE = 'D:\\jenkins\\ws\\ERD_V2'

def RUN_NEXT_JOB = false
def NEXT_JOB = ''

def executionFailed = false
def manuallyAborted = false

def isInterruptedError = { Throwable err ->
    def text = "${err?.getClass()?.getName() ?: ''}\n${err?.getCause()?.getClass()?.getName() ?: ''}\n${err?.getMessage() ?: ''}"
    return text.contains('FlowInterruptedException') || text.contains('InterruptedException')
}

def isTimeoutError = { Throwable err ->
    def text = "${err?.getClass()?.getName() ?: ''}\n${err?.getCause()?.getClass()?.getName() ?: ''}\n${err?.getMessage() ?: ''}".toLowerCase()
    return text.contains('timeout') || text.contains('exceededtimeout')
}

def stopCurrentExecution = {
    bat '''
@echo off
schtasks /end /tn "\\eMatch Automation\\RunEMatchCommand" >nul 2>nul
schtasks /end /tn "startWinDriver" >nul 2>nul
taskkill /f /im WinAppDriver.exe /t >nul 2>nul
taskkill /f /im katalonc.exe /t >nul 2>nul
taskkill /f /im KatalonStudio.exe /t >nul 2>nul
ping -n 4 127.0.0.1 >nul
exit /b 0
'''
}

def runCollection = { String stageName, String cmdFile ->
    stage(stageName) {
        try {
            timeout(time: 5, unit: 'HOURS') {
                bat "call \"${env.WORKSPACE}\\jenkins\\${cmdFile}\""
            }
            echo "${stageName} completed successfully."
        } catch (Throwable err) {
            if (isInterruptedError(err) && !isTimeoutError(err)) {
                manuallyAborted = true
                executionFailed = true
                echo "${stageName} was manually aborted."
                throw err
            }

            executionFailed = true

            if (isTimeoutError(err)) {
                echo "${stageName} exceeded the 5 hour limit and was stopped."
            } else {
                echo "${stageName} failed: ${err}"
            }
        } finally {
            stage("Stop ${stageName}") {
                stopCurrentExecution()
            }
        }
    }
}

node(NODE_LABEL) {
    ws(SHORT_WORKSPACE) {
        stage('Checkout') {
            deleteDir()
            checkout scm
        }

        runCollection('Ecran Rap Detaille V2 Details Collection', 'EcranRapDetailleV2Details.cmd')
        runCollection('Ecran Rap Detaille V2 Synthese Collection', 'EcranRapDetailleV2Synthese.cmd')
    }
}

if (manuallyAborted) {
    currentBuild.result = 'ABORTED'
    echo 'Build was manually aborted. Next pipeline will not be scheduled.'
    return
}

stage('Trigger next pipeline') {
    if (RUN_NEXT_JOB && NEXT_JOB?.trim()) {
        echo "Scheduling next pipeline: ${NEXT_JOB}"
        build job: NEXT_JOB,
              wait: false,
              propagate: false
    } else {
        echo 'Next pipeline disabled.'
    }
}

currentBuild.result = executionFailed ? 'FAILURE' : 'SUCCESS'
return
📁 jenkins/EcranRapDetailleV2Details.cmd
@echo off
setlocal EnableExtensions EnableDelayedExpansion

set "EXECUTION_PROFILE=default"
set "BROWSER_TYPE=Windows"
set "KATALON_HOME=C:\Users\adgi5appjenkins\Documents\Katalon_Studio_Engine_Windows_64-8.1.0"

set "JOB_LABEL=Ecran Rap Detaille V2 Details"
set "TEST_SUITE_COLLECTION=Test Suites/Ecran rapp détaille/Ecran Rap Detaille V2 Details/Ecran Rap Detaille V2 Details Collection"

set "STARTER_DIR=D:\Projects\eMatchStarters"
set "CONTEXT_FILE=%STARTER_DIR%\run-context.properties"
set "RESULT_FILE=%STARTER_DIR%\run-result.properties"
set "LOG_FILE=%STARTER_DIR%\logs\run-ematch-command.log"
set "KATALON_CONSOLE_LOG=%STARTER_DIR%\logs\katalon-console.log"

set "GENERIC_TASK=\eMatch Automation\RunEMatchCommand"
set "WIN_DRIVER_TASK=startWinDriver"

set "WAIT_SECONDS=5"
set "MAX_WAIT_CYCLES=720"
set "RUN_ID=%RANDOM%%RANDOM%%RANDOM%"
set "EXPECTED_RUN_ID=%RUN_ID%"

for /f "usebackq delims=" %%I in (`powershell -NoProfile -Command "(Get-Date).ToString('yyyy-MM-dd HH:mm:ss')"`) do set "RUN_STARTED_AT=%%I"

echo.
echo ==================================================
echo [INFO] Starting %JOB_LABEL%
echo [INFO] WORKSPACE=%WORKSPACE%
echo [INFO] TEST_SUITE_COLLECTION=%TEST_SUITE_COLLECTION%
echo [INFO] EXECUTION_PROFILE=%EXECUTION_PROFILE%
echo [INFO] BROWSER_TYPE=%BROWSER_TYPE%
echo [INFO] STARTER_DIR=%STARTER_DIR%
echo [INFO] CONTEXT_FILE=%CONTEXT_FILE%
echo [INFO] RESULT_FILE=%RESULT_FILE%
echo [INFO] LOG_FILE=%LOG_FILE%
echo [INFO] KATALON_CONSOLE_LOG=%KATALON_CONSOLE_LOG%
echo [INFO] GENERIC_TASK=%GENERIC_TASK%
echo [INFO] WIN_DRIVER_TASK=%WIN_DRIVER_TASK%
echo [INFO] RUN_STARTED_AT=%RUN_STARTED_AT%
echo ==================================================
echo.

if "%WORKSPACE%"=="" (
    echo [ERROR] WORKSPACE is empty.
    exit /b 1
)

if not exist "%STARTER_DIR%" (
    echo [ERROR] STARTER_DIR not found: %STARTER_DIR%
    exit /b 1
)

if not exist "%STARTER_DIR%\logs" md "%STARTER_DIR%\logs"

schtasks /end /tn "%GENERIC_TASK%" >nul 2>nul
schtasks /end /tn "%WIN_DRIVER_TASK%" >nul 2>nul
taskkill /f /im WinAppDriver.exe /t >nul 2>nul
taskkill /f /im katalonc.exe /t >nul 2>nul
taskkill /f /im KatalonStudio.exe /t >nul 2>nul
del /f /q "%LOG_FILE%" >nul 2>nul
del /f /q "%KATALON_CONSOLE_LOG%" >nul 2>nul

> "%CONTEXT_FILE%" (
    echo RUN_ID=%EXPECTED_RUN_ID%
    echo JOB_LABEL=%JOB_LABEL%
    echo WORKSPACE=%WORKSPACE%
    echo TEST_SUITE_COLLECTION=%TEST_SUITE_COLLECTION%
    echo EXECUTION_PROFILE=%EXECUTION_PROFILE%
    echo BROWSER_TYPE=%BROWSER_TYPE%
    echo KATALON_HOME=%KATALON_HOME%
)

> "%RESULT_FILE%" (
    echo RUN_ID=%EXPECTED_RUN_ID%
    echo STATUS=QUEUED
    echo EXIT_CODE=999
    echo UPDATED_AT=%DATE% %TIME%
)

echo [INFO] Context file written:
type "%CONTEXT_FILE%"
echo.
echo [INFO] Initial result file:
type "%RESULT_FILE%"
echo.

echo [INFO] Starting WinAppDriver task...
schtasks /run /tn "%WIN_DRIVER_TASK%" >nul 2>nul
if errorlevel 1 (
    echo [ERROR] Could not start scheduled task "%WIN_DRIVER_TASK%".
    call :printTaskInfo
    exit /b 1
)

echo [INFO] Starting visible Katalon task...
schtasks /run /tn "%GENERIC_TASK%" >nul 2>nul
if errorlevel 1 (
    echo [ERROR] Could not start scheduled task "%GENERIC_TASK%".
    schtasks /end /tn "%WIN_DRIVER_TASK%" >nul 2>nul
    call :printTaskInfo
    exit /b 1
)

ping -n 4 127.0.0.1 >nul

set /a WAIT_COUNT=0

:waitResult
set /a WAIT_COUNT+=1
set "FILE_RUN_ID="
set "STATUS="
set "EXIT_CODE="
set "UPDATED_AT="

for /f "usebackq tokens=1,* delims==" %%A in ("%RESULT_FILE%") do (
    if /I "%%A"=="RUN_ID" set "FILE_RUN_ID=%%B"
    if /I "%%A"=="STATUS" set "STATUS=%%B"
    if /I "%%A"=="EXIT_CODE" set "EXIT_CODE=%%B"
    if /I "%%A"=="UPDATED_AT" set "UPDATED_AT=%%B"
)

if !WAIT_COUNT! EQU 1 echo [INFO] Waiting result file update...
if !WAIT_COUNT! EQU 2 echo [INFO] Still waiting result file update...
if !WAIT_COUNT! EQU 3 echo [INFO] Still waiting...
if !WAIT_COUNT! EQU 4 echo [INFO] Still waiting...
if !WAIT_COUNT! EQU 5 echo [INFO] Still waiting...
if !WAIT_COUNT! EQU 10 call :printTaskInfo
if !WAIT_COUNT! EQU 20 call :printTaskInfo
if !WAIT_COUNT! EQU 40 call :printTaskInfo

if /I "!FILE_RUN_ID!"=="%EXPECTED_RUN_ID%" if /I "!STATUS!"=="FINISHED" goto resultDone

if !WAIT_COUNT! GEQ %MAX_WAIT_CYCLES% (
    echo [ERROR] Timeout waiting run-result.properties update.
    echo [ERROR] FILE_RUN_ID=!FILE_RUN_ID!
    echo [ERROR] STATUS=!STATUS!
    echo [ERROR] EXIT_CODE=!EXIT_CODE!
    echo [ERROR] UPDATED_AT=!UPDATED_AT!
    schtasks /end /tn "%GENERIC_TASK%" >nul 2>nul
    schtasks /end /tn "%WIN_DRIVER_TASK%" >nul 2>nul
    call :printTaskInfo
    call :printResultFile
    call :printLog "%LOG_FILE%" "run-ematch-command.log" 40
    call :printLog "%KATALON_CONSOLE_LOG%" "katalon-console.log" 120
    exit /b 1
)

ping -n %WAIT_SECONDS% 127.0.0.1 >nul
goto waitResult

:resultDone
echo.
echo [INFO] Result file updated.
echo [INFO] RUN_ID=!FILE_RUN_ID!
echo [INFO] STATUS=!STATUS!
echo [INFO] EXIT_CODE=!EXIT_CODE!
echo [INFO] UPDATED_AT=!UPDATED_AT!
echo.

schtasks /end /tn "%WIN_DRIVER_TASK%" >nul 2>nul

call :printTaskInfo
call :printResultFile
call :printLog "%LOG_FILE%" "run-ematch-command.log" 30
call :printLog "%KATALON_CONSOLE_LOG%" "katalon-console.log" 120
call :printReportSummary

call :validateExecution
if errorlevel 1 exit /b 1

echo [INFO] %JOB_LABEL% execution finished successfully.
if not "!EXIT_CODE!"=="0" echo [WARN] Katalon returned EXIT_CODE=!EXIT_CODE!, but Jenkins will keep SUCCESS because execution finished.
exit /b 0

:validateExecution
echo [INFO] Validating current execution...

if /I not "!STATUS!"=="FINISHED" (
    echo [ERROR] STATUS is not FINISHED.
    exit /b 1
)

powershell -NoProfile -Command ^
  "$log='%KATALON_CONSOLE_LOG%';" ^
  "if(Test-Path $log){" ^
  "  $text=Get-Content -Path $log -Raw;" ^
  "  $patterns=@(" ^
  "    'Test Suite Collection .* not found'," ^
  "    'Test Suite .* not found'," ^
  "    'FilePathTooLongException'," ^
  "    'Cannot save entity: file path length limit exceeded'," ^
  "    'com\\.kms\\.katalon\\.execution\\.exception\\.ExecutionException'," ^
  "    'Could not start scheduled task'" ^
  "  );" ^
  "  foreach($pattern in $patterns){ if($text -match $pattern){ exit 1 } }" ^
  "}" ^
  "exit 0"
if errorlevel 1 (
    echo [ERROR] Hard execution error detected for %JOB_LABEL%.
    exit /b 1
)

exit /b 0

:printTaskInfo
echo [INFO] Scheduled task status snapshot:
echo [INFO] ---- %WIN_DRIVER_TASK% ----
schtasks /query /tn "%WIN_DRIVER_TASK%" /fo list 2>nul | findstr /i /b /c:"Status:" /c:"Last Result:" /c:"Last Run Time:"
echo [INFO] ---- %GENERIC_TASK% ----
schtasks /query /tn "%GENERIC_TASK%" /fo list 2>nul | findstr /i /b /c:"Status:" /c:"Last Result:" /c:"Last Run Time:"
echo.
exit /b 0

:printResultFile
if exist "%RESULT_FILE%" (
    echo [INFO] Current run-result.properties:
    type "%RESULT_FILE%"
) else (
    echo [WARN] Result file not found: %RESULT_FILE%
)
echo.
exit /b 0

:printLog
set "TARGET_FILE=%~1"
set "TARGET_TITLE=%~2"
set "TARGET_TAIL=%~3"
if exist "%TARGET_FILE%" (
    echo [INFO] Last lines from %TARGET_TITLE%:
    powershell -NoProfile -Command "Get-Content -Path '%TARGET_FILE%' -Tail %TARGET_TAIL%"
) else (
    echo [WARN] File not found: %TARGET_FILE%
)
echo.
exit /b 0

:printReportSummary
if exist "%WORKSPACE%\Reports" (
    echo [INFO] Reports folder found: %WORKSPACE%\Reports
    echo [INFO] Relevant report artifacts:
    powershell -NoProfile -Command ^
      "$root='%WORKSPACE%\Reports';" ^
      "$files=Get-ChildItem -Path $root -Recurse -File -ErrorAction SilentlyContinue | Where-Object { $_.Name -eq 'JUnit_Report.xml' -or $_.Name -eq 'execution0.log' -or $_.Name -eq 'eMatchReport.html' -or $_.Name -like '*.html' } | Sort-Object LastWriteTime -Descending | Select-Object -First 20;" ^
      "foreach($file in $files){ Write-Host ('[REPORT] ' + $file.LastWriteTime.ToString('yyyy-MM-dd HH:mm:ss') + '  ' + $file.FullName) }"
) else (
    echo [WARN] Reports folder not found: %WORKSPACE%\Reports
)
echo.
exit /b 0
📁 jenkins/EcranRapDetailleV2Synthese.cmd
@echo off
setlocal EnableExtensions EnableDelayedExpansion

set "EXECUTION_PROFILE=default"
set "BROWSER_TYPE=Windows"
set "KATALON_HOME=C:\Users\adgi5appjenkins\Documents\Katalon_Studio_Engine_Windows_64-8.1.0"

set "JOB_LABEL=Ecran Rap Detaille V2 Synthese"
set "TEST_SUITE_COLLECTION=Test Suites/Ecran rapp détaille/Ecran Rap Detaille V2 Synthese/Ecran Rap Detaille V2 Synthese Collection"

set "STARTER_DIR=D:\Projects\eMatchStarters"
set "CONTEXT_FILE=%STARTER_DIR%\run-context.properties"
set "RESULT_FILE=%STARTER_DIR%\run-result.properties"
set "LOG_FILE=%STARTER_DIR%\logs\run-ematch-command.log"
set "KATALON_CONSOLE_LOG=%STARTER_DIR%\logs\katalon-console.log"

set "GENERIC_TASK=\eMatch Automation\RunEMatchCommand"
set "WIN_DRIVER_TASK=startWinDriver"

set "WAIT_SECONDS=5"
set "MAX_WAIT_CYCLES=720"
set "RUN_ID=%RANDOM%%RANDOM%%RANDOM%"
set "EXPECTED_RUN_ID=%RUN_ID%"

for /f "usebackq delims=" %%I in (`powershell -NoProfile -Command "(Get-Date).ToString('yyyy-MM-dd HH:mm:ss')"`) do set "RUN_STARTED_AT=%%I"

echo.
echo ==================================================
echo [INFO] Starting %JOB_LABEL%
echo [INFO] WORKSPACE=%WORKSPACE%
echo [INFO] TEST_SUITE_COLLECTION=%TEST_SUITE_COLLECTION%
echo [INFO] EXECUTION_PROFILE=%EXECUTION_PROFILE%
echo [INFO] BROWSER_TYPE=%BROWSER_TYPE%
echo [INFO] STARTER_DIR=%STARTER_DIR%
echo [INFO] CONTEXT_FILE=%CONTEXT_FILE%
echo [INFO] RESULT_FILE=%RESULT_FILE%
echo [INFO] LOG_FILE=%LOG_FILE%
echo [INFO] KATALON_CONSOLE_LOG=%KATALON_CONSOLE_LOG%
echo [INFO] GENERIC_TASK=%GENERIC_TASK%
echo [INFO] WIN_DRIVER_TASK=%WIN_DRIVER_TASK%
echo [INFO] RUN_STARTED_AT=%RUN_STARTED_AT%
echo ==================================================
echo.

if "%WORKSPACE%"=="" (
    echo [ERROR] WORKSPACE is empty.
    exit /b 1
)

if not exist "%STARTER_DIR%" (
    echo [ERROR] STARTER_DIR not found: %STARTER_DIR%
    exit /b 1
)

if not exist "%STARTER_DIR%\logs" md "%STARTER_DIR%\logs"

schtasks /end /tn "%GENERIC_TASK%" >nul 2>nul
schtasks /end /tn "%WIN_DRIVER_TASK%" >nul 2>nul
taskkill /f /im WinAppDriver.exe /t >nul 2>nul
taskkill /f /im katalonc.exe /t >nul 2>nul
taskkill /f /im KatalonStudio.exe /t >nul 2>nul
del /f /q "%LOG_FILE%" >nul 2>nul
del /f /q "%KATALON_CONSOLE_LOG%" >nul 2>nul

> "%CONTEXT_FILE%" (
    echo RUN_ID=%EXPECTED_RUN_ID%
    echo JOB_LABEL=%JOB_LABEL%
    echo WORKSPACE=%WORKSPACE%
    echo TEST_SUITE_COLLECTION=%TEST_SUITE_COLLECTION%
    echo EXECUTION_PROFILE=%EXECUTION_PROFILE%
    echo BROWSER_TYPE=%BROWSER_TYPE%
    echo KATALON_HOME=%KATALON_HOME%
)

> "%RESULT_FILE%" (
    echo RUN_ID=%EXPECTED_RUN_ID%
    echo STATUS=QUEUED
    echo EXIT_CODE=999
    echo UPDATED_AT=%DATE% %TIME%
)

echo [INFO] Context file written:
type "%CONTEXT_FILE%"
echo.
echo [INFO] Initial result file:
type "%RESULT_FILE%"
echo.

echo [INFO] Starting WinAppDriver task...
schtasks /run /tn "%WIN_DRIVER_TASK%" >nul 2>nul
if errorlevel 1 (
    echo [ERROR] Could not start scheduled task "%WIN_DRIVER_TASK%".
    call :printTaskInfo
    exit /b 1
)

echo [INFO] Starting visible Katalon task...
schtasks /run /tn "%GENERIC_TASK%" >nul 2>nul
if errorlevel 1 (
    echo [ERROR] Could not start scheduled task "%GENERIC_TASK%".
    schtasks /end /tn "%WIN_DRIVER_TASK%" >nul 2>nul
    call :printTaskInfo
    exit /b 1
)

ping -n 4 127.0.0.1 >nul

set /a WAIT_COUNT=0

:waitResult
set /a WAIT_COUNT+=1
set "FILE_RUN_ID="
set "STATUS="
set "EXIT_CODE="
set "UPDATED_AT="

for /f "usebackq tokens=1,* delims==" %%A in ("%RESULT_FILE%") do (
    if /I "%%A"=="RUN_ID" set "FILE_RUN_ID=%%B"
    if /I "%%A"=="STATUS" set "STATUS=%%B"
    if /I "%%A"=="EXIT_CODE" set "EXIT_CODE=%%B"
    if /I "%%A"=="UPDATED_AT" set "UPDATED_AT=%%B"
)

if !WAIT_COUNT! EQU 1 echo [INFO] Waiting result file update...
if !WAIT_COUNT! EQU 2 echo [INFO] Still waiting result file update...
if !WAIT_COUNT! EQU 3 echo [INFO] Still waiting...
if !WAIT_COUNT! EQU 4 echo [INFO] Still waiting...
if !WAIT_COUNT! EQU 5 echo [INFO] Still waiting...
if !WAIT_COUNT! EQU 10 call :printTaskInfo
if !WAIT_COUNT! EQU 20 call :printTaskInfo
if !WAIT_COUNT! EQU 40 call :printTaskInfo

if /I "!FILE_RUN_ID!"=="%EXPECTED_RUN_ID%" if /I "!STATUS!"=="FINISHED" goto resultDone

if !WAIT_COUNT! GEQ %MAX_WAIT_CYCLES% (
    echo [ERROR] Timeout waiting run-result.properties update.
    echo [ERROR] FILE_RUN_ID=!FILE_RUN_ID!
    echo [ERROR] STATUS=!STATUS!
    echo [ERROR] EXIT_CODE=!EXIT_CODE!
    echo [ERROR] UPDATED_AT=!UPDATED_AT!
    schtasks /end /tn "%GENERIC_TASK%" >nul 2>nul
    schtasks /end /tn "%WIN_DRIVER_TASK%" >nul 2>nul
    call :printTaskInfo
    call :printResultFile
    call :printLog "%LOG_FILE%" "run-ematch-command.log" 40
    call :printLog "%KATALON_CONSOLE_LOG%" "katalon-console.log" 120
    exit /b 1
)

ping -n %WAIT_SECONDS% 127.0.0.1 >nul
goto waitResult

:resultDone
echo.
echo [INFO] Result file updated.
echo [INFO] RUN_ID=!FILE_RUN_ID!
echo [INFO] STATUS=!STATUS!
echo [INFO] EXIT_CODE=!EXIT_CODE!
echo [INFO] UPDATED_AT=!UPDATED_AT!
echo.

schtasks /end /tn "%WIN_DRIVER_TASK%" >nul 2>nul

call :printTaskInfo
call :printResultFile
call :printLog "%LOG_FILE%" "run-ematch-command.log" 30
call :printLog "%KATALON_CONSOLE_LOG%" "katalon-console.log" 120
call :printReportSummary

call :validateExecution
if errorlevel 1 exit /b 1

echo [INFO] %JOB_LABEL% execution finished successfully.
if not "!EXIT_CODE!"=="0" echo [WARN] Katalon returned EXIT_CODE=!EXIT_CODE!, but Jenkins will keep SUCCESS because execution finished.
exit /b 0

:validateExecution
echo [INFO] Validating current execution...

if /I not "!STATUS!"=="FINISHED" (
    echo [ERROR] STATUS is not FINISHED.
    exit /b 1
)

powershell -NoProfile -Command ^
  "$log='%KATALON_CONSOLE_LOG%';" ^
  "if(Test-Path $log){" ^
  "  $text=Get-Content -Path $log -Raw;" ^
  "  $patterns=@(" ^
  "    'Test Suite Collection .* not found'," ^
  "    'Test Suite .* not found'," ^
  "    'FilePathTooLongException'," ^
  "    'Cannot save entity: file path length limit exceeded'," ^
  "    'com\\.kms\\.katalon\\.execution\\.exception\\.ExecutionException'," ^
  "    'Could not start scheduled task'" ^
  "  );" ^
  "  foreach($pattern in $patterns){ if($text -match $pattern){ exit 1 } }" ^
  "}" ^
  "exit 0"
if errorlevel 1 (
    echo [ERROR] Hard execution error detected for %JOB_LABEL%.
    exit /b 1
)

exit /b 0

:printTaskInfo
echo [INFO] Scheduled task status snapshot:
echo [INFO] ---- %WIN_DRIVER_TASK% ----
schtasks /query /tn "%WIN_DRIVER_TASK%" /fo list 2>nul | findstr /i /b /c:"Status:" /c:"Last Result:" /c:"Last Run Time:"
echo [INFO] ---- %GENERIC_TASK% ----
schtasks /query /tn "%GENERIC_TASK%" /fo list 2>nul | findstr /i /b /c:"Status:" /c:"Last Result:" /c:"Last Run Time:"
echo.
exit /b 0

:printResultFile
if exist "%RESULT_FILE%" (
    echo [INFO] Current run-result.properties:
    type "%RESULT_FILE%"
) else (
    echo [WARN] Result file not found: %RESULT_FILE%
)
echo.
exit /b 0

:printLog
set "TARGET_FILE=%~1"
set "TARGET_TITLE=%~2"
set "TARGET_TAIL=%~3"
if exist "%TARGET_FILE%" (
    echo [INFO] Last lines from %TARGET_TITLE%:
    powershell -NoProfile -Command "Get-Content -Path '%TARGET_FILE%' -Tail %TARGET_TAIL%"
) else (
    echo [WARN] File not found: %TARGET_FILE%
)
echo.
exit /b 0

:printReportSummary
if exist "%WORKSPACE%\Reports" (
    echo [INFO] Reports folder found: %WORKSPACE%\Reports
    echo [INFO] Relevant report artifacts:
    powershell -NoProfile -Command ^
      "$root='%WORKSPACE%\Reports';" ^
      "$files=Get-ChildItem -Path $root -Recurse -File -ErrorAction SilentlyContinue | Where-Object { $_.Name -eq 'JUnit_Report.xml' -or $_.Name -eq 'execution0.log' -or $_.Name -eq 'eMatchReport.html' -or $_.Name -like '*.html' } | Sort-Object LastWriteTime -Descending | Select-Object -First 20;" ^
      "foreach($file in $files){ Write-Host ('[REPORT] ' + $file.LastWriteTime.ToString('yyyy-MM-dd HH:mm:ss') + '  ' + $file.FullName) }"
) else (
    echo [WARN] Reports folder not found: %WORKSPACE%\Reports
)
echo.
exit /b 0
📌
