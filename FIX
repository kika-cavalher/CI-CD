package commands

import org.apache.poi.ss.usermodel.*
import org.apache.poi.ss.usermodel.WorkbookFactory

import java.math.RoundingMode

import com.kms.katalon.core.annotation.Keyword
import com.kms.katalon.core.util.KeywordUtil

class ExportFileValidationKeywords {

	private static final String LIST_SEPARATOR = '|'

	@Keyword
	String validateEvidenceFolderExists(String folderPath) {

		StringBuilder errors = new StringBuilder()

		File folder = new File(safeText(folderPath))

		KeywordUtil.logInfo("📁 [VALIDATE FOLDER] " + folder.getAbsolutePath())

		if (!folder.exists()) {
			errors.append("[ERROR] Evidence folder does not exist: ").append(folder.getAbsolutePath()).append("\n")
		} else if (!folder.isDirectory()) {
			errors.append("[ERROR] Evidence path is not a folder: ").append(folder.getAbsolutePath()).append("\n")
		}

		return errors.toString()
	}

	@Keyword
	String resolveExcelFilesAsString(String folderPath, String requestedFileNames, String includeSubFoldersText) {

		File folder = new File(safeText(folderPath))
		boolean includeSubFolders = isTrue(includeSubFoldersText)

		List<File> files = []

		List<String> requestedFiles = splitText(requestedFileNames)

		if (requestedFiles.size() > 0) {

			for (String fileName : requestedFiles) {

				File file = new File(fileName)

				if (!file.isAbsolute()) {
					file = new File(folder, fileName)
				}

				files.add(file)
			}
		} else if (folder.exists() && folder.isDirectory()) {

			files = collectExcelFiles(folder, includeSubFolders)
		}

		files = files.findAll { File file ->
			file != null && isExcelFile(file.getName()) && !file.getName().startsWith('~$')
		}

		files.sort { File file ->
			file.getAbsolutePath().toLowerCase()
		}

		String result = files.collect { File file ->
			file.getAbsolutePath()
		}.join(LIST_SEPARATOR)

		KeywordUtil.logInfo("📄 [RESOLVED FILES] " + result)

		return result
	}

	@Keyword
	String validateExcelFilesFound(String filesToValidate) {

		StringBuilder errors = new StringBuilder()

		List<File> files = parseFiles(filesToValidate)

		if (files.size() == 0) {
			errors.append("[ERROR] No Excel files found to validate.\n")
		}

		for (File file : files) {

			if (!file.exists()) {
				errors.append("[ERROR] File not found: ").append(file.getAbsolutePath()).append("\n")
			} else if (!file.isFile()) {
				errors.append("[ERROR] Path is not a file: ").append(file.getAbsolutePath()).append("\n")
			} else if (!isExcelFile(file.getName())) {
				errors.append("[ERROR] File is not Excel: ").append(file.getAbsolutePath()).append("\n")
			}
		}

		return errors.toString()
	}

	@Keyword
	String validateMinimumExpectedFileQuantity(String filesToValidate, String minimumExpectedFilesText) {

		int minimumExpectedFiles = parseIntOrZero(minimumExpectedFilesText)

		if (minimumExpectedFiles <= 0) {
			return "[SKIPPED] Minimum expected file quantity was not configured."
		}

		StringBuilder errors = new StringBuilder()

		List<File> files = parseFiles(filesToValidate)

		KeywordUtil.logInfo("📄 [FILE COUNT] found=" + files.size() + " | minimumExpected=" + minimumExpectedFiles)

		if (files.size() < minimumExpectedFiles) {
			errors.append("[ERROR] Expected at least ")
					.append(minimumExpectedFiles)
					.append(" Excel files, but found ")
					.append(files.size())
					.append(".\n")
		}

		return errors.toString()
	}

	@Keyword
	String validateRequiredHeaders(
			String filesToValidate,
			String defaultValidationType,
			String sheetIndexText,
			String headerCurrency,
			String headerCurrencyRef,
			String headerAmountCcy,
			String headerAmountBaseCcy) {

		StringBuilder errors = new StringBuilder()

		List<File> files = parseFiles(filesToValidate)
		int sheetIndex = parseIntOrZero(sheetIndexText)

		for (File file : files) {

			String validationType = resolveValidationTypeForFile(file, defaultValidationType)
			boolean needCurrencyRef = isCurrencyRefRequired(validationType)

			String fileErrors = validateRequiredHeadersForFile(
					file,
					validationType,
					sheetIndex,
					headerCurrency,
					headerCurrencyRef,
					headerAmountCcy,
					headerAmountBaseCcy,
					needCurrencyRef
					)

			if (fileErrors.trim().length() > 0) {
				errors.append(fileErrors)
			}
		}

		return errors.toString()
	}

	@Keyword
	String validateRowsForValidationType(
			String filesToValidate,
			String defaultValidationType,
			String sheetIndexText,
			String headerCurrency,
			String headerCurrencyRef) {

		StringBuilder errors = new StringBuilder()

		List<File> files = parseFiles(filesToValidate)
		int sheetIndex = parseIntOrZero(sheetIndexText)

		for (File file : files) {

			String validationType = resolveValidationTypeForFile(file, defaultValidationType)

			String fileErrors = validateRowsForValidationTypeInFile(
					file,
					validationType,
					sheetIndex,
					headerCurrency,
					headerCurrencyRef
					)

			if (fileErrors.trim().length() > 0) {
				errors.append(fileErrors)
			}
		}

		return errors.toString()
	}

	@Keyword
	String validateMontantCcyEqualsMontantBaseCcy(
			String filesToValidate,
			String defaultValidationType,
			String sheetIndexText,
			String headerCurrency,
			String headerCurrencyRef,
			String headerAmountCcy,
			String headerAmountBaseCcy) {

		StringBuilder errors = new StringBuilder()

		List<File> files = parseFiles(filesToValidate)
		int sheetIndex = parseIntOrZero(sheetIndexText)

		for (File file : files) {

			String validationType = resolveValidationTypeForFile(file, defaultValidationType)

			String fileErrors = validateAmountsInFile(
					file,
					validationType,
					sheetIndex,
					headerCurrency,
					headerCurrencyRef,
					headerAmountCcy,
					headerAmountBaseCcy
					)

			if (fileErrors.trim().length() > 0) {
				errors.append(fileErrors)
			}
		}

		return errors.toString()
	}

	@Keyword
	String validateNoDiscrepanciesBetweenFiles(
			String filesToValidate,
			String defaultValidationType,
			String sheetIndexText,
			String keyHeadersText,
			String compareHeadersText) {

		String keyHeadersClean = safeText(keyHeadersText)
		String compareHeadersClean = safeText(compareHeadersText)

		if (keyHeadersClean.length() == 0 || compareHeadersClean.length() == 0) {
			return "[SKIPPED] Discrepancy validation was not executed because key headers or compare headers were not configured."
		}

		StringBuilder errors = new StringBuilder()

		List<File> files = parseFiles(filesToValidate)
		int sheetIndex = parseIntOrZero(sheetIndexText)

		if (files.size() < 2) {
			return "[SKIPPED] Discrepancy validation needs at least 2 files."
		}

		List<String> keyHeaders = splitText(keyHeadersText)
		List<String> compareHeaders = splitText(compareHeadersText)

		Map<String, String> expectedValuesByKey = [:]
		Map<String, String> sourceByKey = [:]

		for (File file : files) {

			String validationType = resolveValidationTypeForFile(file, defaultValidationType)

			String fileErrors = collectDiscrepanciesFromFile(
					file,
					validationType,
					sheetIndex,
					keyHeaders,
					compareHeaders,
					expectedValuesByKey,
					sourceByKey
					)

			if (fileErrors.trim().length() > 0) {
				errors.append(fileErrors)
			}
		}

		return errors.toString()
	}

	private String validateRequiredHeadersForFile(
			File file,
			String validationType,
			int sheetIndex,
			String headerCurrency,
			String headerCurrencyRef,
			String headerAmountCcy,
			String headerAmountBaseCcy,
			boolean needCurrencyRef) {

		StringBuilder errors = new StringBuilder()

		FileInputStream input = null
		Workbook workbook = null

		try {

			KeywordUtil.logInfo("🧾 [VALIDATE HEADERS] file=" + file.getName() + " | validationType=" + validationType)

			input = new FileInputStream(file)
			workbook = WorkbookFactory.create(input)

			if (workbook.getNumberOfSheets() <= sheetIndex) {

				errors.append("[ERROR] File '")
						.append(file.getName())
						.append("' does not contain sheet index ")
						.append(sheetIndex)
						.append(".\n")
			} else {

				Sheet sheet = workbook.getSheetAt(sheetIndex)
				DataFormatter formatter = new DataFormatter()

				int currencyCol = findHeaderColumn(sheet, headerCurrency, formatter)
				int currencyRefCol = findHeaderColumn(sheet, headerCurrencyRef, formatter)
				int amountCcyCol = findHeaderColumn(sheet, headerAmountCcy, formatter)
				int amountBaseCol = findHeaderColumn(sheet, headerAmountBaseCcy, formatter)

				KeywordUtil.logInfo("🧾 [HEADERS] file=" + file.getName() +
						" | " + headerCurrency + "=" + currencyCol +
						" | " + headerCurrencyRef + "=" + currencyRefCol +
						" | " + headerAmountCcy + "=" + amountCcyCol +
						" | " + headerAmountBaseCcy + "=" + amountBaseCol)

				if (currencyCol < 0) {
					errors.append("[ERROR] File '").append(file.getName()).append("' missing header: ").append(headerCurrency).append("\n")
				}

				if (needCurrencyRef && currencyRefCol < 0) {
					errors.append("[ERROR] File '").append(file.getName()).append("' missing header: ").append(headerCurrencyRef).append("\n")
				}

				if (amountCcyCol < 0) {
					errors.append("[ERROR] File '").append(file.getName()).append("' missing header: ").append(headerAmountCcy).append("\n")
				}

				if (amountBaseCol < 0) {
					errors.append("[ERROR] File '").append(file.getName()).append("' missing header: ").append(headerAmountBaseCcy).append("\n")
				}
			}
		} catch (Exception e) {

			errors.append("[ERROR] Header validation failed for file '")
					.append(file.getName())
					.append("': ")
					.append(e.getClass().getSimpleName())
					.append(" | ")
					.append(e.getMessage())
					.append("\n")
		} finally {

			try {
				if (workbook != null) {
					workbook.close()
				}
			} catch (Exception ignored) {}

			try {
				if (input != null) {
					input.close()
				}
			} catch (Exception ignored) {}
		}

		return errors.toString()
	}

	private String validateRowsForValidationTypeInFile(
			File file,
			String validationType,
			int sheetIndex,
			String headerCurrency,
			String headerCurrencyRef) {

		StringBuilder errors = new StringBuilder()

		FileInputStream input = null
		Workbook workbook = null

		try {

			KeywordUtil.logInfo("🔎 [VALIDATE ROWS] file=" + file.getName() + " | validationType=" + validationType)

			input = new FileInputStream(file)
			workbook = WorkbookFactory.create(input)

			if (workbook.getNumberOfSheets() <= sheetIndex) {

				errors.append("[ERROR] File '")
						.append(file.getName())
						.append("' does not contain sheet index ")
						.append(sheetIndex)
						.append(".\n")
			} else {

				Sheet sheet = workbook.getSheetAt(sheetIndex)
				DataFormatter formatter = new DataFormatter()
				FormulaEvaluator evaluator = workbook.getCreationHelper().createFormulaEvaluator()

				int currencyCol = findHeaderColumn(sheet, headerCurrency, formatter)
				int currencyRefCol = findHeaderColumn(sheet, headerCurrencyRef, formatter)

				boolean needCurrencyRef = isCurrencyRefRequired(validationType)

				if (currencyCol < 0 || (needCurrencyRef && currencyRefCol < 0)) {

					errors.append("[ERROR] File '")
							.append(file.getName())
							.append("' cannot validate rows because required currency headers are missing.\n")
				} else {

					int matchingRows = 0
					int lastRowNum = sheet.getLastRowNum()

					for (int i = 1; i <= lastRowNum; i++) {

						Row row = sheet.getRow(i)

						if (row != null && !isRowEmpty(row, formatter, evaluator)) {

							String currency = getCellText(row, currencyCol, formatter, evaluator)
							String currencyRef = needCurrencyRef ? getCellText(row, currencyRefCol, formatter, evaluator) : ''

							if (shouldValidateRow(validationType, currency, currencyRef)) {
								matchingRows++
							}
						}
					}

					KeywordUtil.logInfo("🔎 [ROWS FOUND] file=" + file.getName() + " | validationType=" + validationType + " | rows=" + matchingRows)

					if (matchingRows == 0) {
						errors.append("[ERROR] File '")
								.append(file.getName())
								.append("' has no rows corresponding to validation type ")
								.append(validationType)
								.append(".\n")
					}
				}
			}
		} catch (Exception e) {

			errors.append("[ERROR] Row validation failed for file '")
					.append(file.getName())
					.append("': ")
					.append(e.getClass().getSimpleName())
					.append(" | ")
					.append(e.getMessage())
					.append("\n")
		} finally {

			try {
				if (workbook != null) {
					workbook.close()
				}
			} catch (Exception ignored) {}

			try {
				if (input != null) {
					input.close()
				}
			} catch (Exception ignored) {}
		}

		return errors.toString()
	}

	private String validateAmountsInFile(
			File file,
			String validationType,
			int sheetIndex,
			String headerCurrency,
			String headerCurrencyRef,
			String headerAmountCcy,
			String headerAmountBaseCcy) {

		StringBuilder errors = new StringBuilder()

		FileInputStream input = null
		Workbook workbook = null

		try {

			KeywordUtil.logInfo("💰 [VALIDATE AMOUNTS] file=" + file.getName() + " | validationType=" + validationType)

			input = new FileInputStream(file)
			workbook = WorkbookFactory.create(input)

			if (workbook.getNumberOfSheets() <= sheetIndex) {

				errors.append("[ERROR] File '")
						.append(file.getName())
						.append("' does not contain sheet index ")
						.append(sheetIndex)
						.append(".\n")
			} else {

				Sheet sheet = workbook.getSheetAt(sheetIndex)
				DataFormatter formatter = new DataFormatter()
				FormulaEvaluator evaluator = workbook.getCreationHelper().createFormulaEvaluator()

				int currencyCol = findHeaderColumn(sheet, headerCurrency, formatter)
				int currencyRefCol = findHeaderColumn(sheet, headerCurrencyRef, formatter)
				int amountCcyCol = findHeaderColumn(sheet, headerAmountCcy, formatter)
				int amountBaseCol = findHeaderColumn(sheet, headerAmountBaseCcy, formatter)

				boolean needCurrencyRef = isCurrencyRefRequired(validationType)

				if (currencyCol < 0 || amountCcyCol < 0 || amountBaseCol < 0 || (needCurrencyRef && currencyRefCol < 0)) {

					errors.append("[ERROR] File '")
							.append(file.getName())
							.append("' cannot validate amounts because required headers are missing.\n")
				} else {

					int lastRowNum = sheet.getLastRowNum()
					int processed = 0
					int validatedRows = 0
					int skippedNullRow = 0
					int skippedEmptyRow = 0
					int invalidNumeric = 0

					for (int i = 1; i <= lastRowNum; i++) {

						Row row = sheet.getRow(i)

						if (row == null) {

							skippedNullRow++
						} else if (isRowEmpty(row, formatter, evaluator)) {

							skippedEmptyRow++
						} else {

							processed++

							String currency = getCellText(row, currencyCol, formatter, evaluator)
							String currencyRef = needCurrencyRef ? getCellText(row, currencyRefCol, formatter, evaluator) : ''

							if (shouldValidateRow(validationType, currency, currencyRef)) {

								validatedRows++

								BigDecimal amountCcy = getCellNumber(row, amountCcyCol, formatter, evaluator)
								BigDecimal amountBase = getCellNumber(row, amountBaseCol, formatter, evaluator)

								if (amountCcy == null || amountBase == null) {

									invalidNumeric++

									errors.append("[ERROR] File '")
											.append(file.getName())
											.append("' row ")
											.append(i + 1)
											.append(" has invalid numeric values. ")
											.append(headerAmountCcy)
											.append("='")
											.append(getCellText(row, amountCcyCol, formatter, evaluator))
											.append("' | ")
											.append(headerAmountBaseCcy)
											.append("='")
											.append(getCellText(row, amountBaseCol, formatter, evaluator))
											.append("'\n")
								} else {

									BigDecimal ccy = round2(amountCcy)
									BigDecimal base = round2(amountBase)

									if (ccy.compareTo(base) != 0) {

										errors.append("[ERROR] File '")
												.append(file.getName())
												.append("' row ")
												.append(i + 1)
												.append(" has different amounts. ")
												.append(headerCurrency)
												.append("='")
												.append(currency)
												.append("'")

										if (needCurrencyRef) {
											errors.append(" | ")
													.append(headerCurrencyRef)
													.append("='")
													.append(currencyRef)
													.append("'")
										}

										errors.append(" | ")
												.append(headerAmountCcy)
												.append("=")
												.append(ccy)
												.append(" | ")
												.append(headerAmountBaseCcy)
												.append("=")
												.append(base)
												.append("\n")
									}
								}
							}
						}

						if (i % 200 == 0) {
							KeywordUtil.logInfo("🟦 [PROGRESS] file=" + file.getName() +
									" | row=" + i + "/" + lastRowNum +
									" | processed=" + processed +
									" | validatedRows=" + validatedRows +
									" | nullRow=" + skippedNullRow +
									" | emptyRow=" + skippedEmptyRow +
									" | invalidNumeric=" + invalidNumeric)
						}
					}

					KeywordUtil.logInfo("✅ [AMOUNT LOOP END] file=" + file.getName() +
							" | processed=" + processed +
							" | validatedRows=" + validatedRows +
							" | skippedNullRow=" + skippedNullRow +
							" | skippedEmptyRow=" + skippedEmptyRow +
							" | invalidNumeric=" + invalidNumeric +
							" | lastRowNum=" + lastRowNum)
				}
			}
		} catch (Exception e) {

			errors.append("[ERROR] Amount validation failed for file '")
					.append(file.getName())
					.append("': ")
					.append(e.getClass().getSimpleName())
					.append(" | ")
					.append(e.getMessage())
					.append("\n")
		} finally {

			try {
				if (workbook != null) {
					workbook.close()
				}
			} catch (Exception ignored) {}

			try {
				if (input != null) {
					input.close()
				}
			} catch (Exception ignored) {}
		}

		return errors.toString()
	}

	private String collectDiscrepanciesFromFile(
			File file,
			String validationType,
			int sheetIndex,
			List<String> keyHeaders,
			List<String> compareHeaders,
			Map<String, String> expectedValuesByKey,
			Map<String, String> sourceByKey) {

		StringBuilder errors = new StringBuilder()

		FileInputStream input = null
		Workbook workbook = null

		try {

			KeywordUtil.logInfo("🔁 [DISCREPANCY CHECK] file=" + file.getName() + " | validationType=" + validationType)

			input = new FileInputStream(file)
			workbook = WorkbookFactory.create(input)

			if (workbook.getNumberOfSheets() <= sheetIndex) {

				errors.append("[ERROR] File '")
						.append(file.getName())
						.append("' does not contain sheet index ")
						.append(sheetIndex)
						.append(".\n")
			} else {

				Sheet sheet = workbook.getSheetAt(sheetIndex)
				DataFormatter formatter = new DataFormatter()
				FormulaEvaluator evaluator = workbook.getCreationHelper().createFormulaEvaluator()

				Map<String, Integer> keyColumns = resolveHeaderColumns(sheet, keyHeaders, formatter)
				Map<String, Integer> compareColumns = resolveHeaderColumns(sheet, compareHeaders, formatter)

				if (keyColumns.size() != keyHeaders.size() || compareColumns.size() != compareHeaders.size()) {

					errors.append("[ERROR] File '")
							.append(file.getName())
							.append("' cannot run discrepancy validation because one or more configured headers were not found.\n")
				} else {

					int lastRowNum = sheet.getLastRowNum()

					for (int i = 1; i <= lastRowNum; i++) {

						Row row = sheet.getRow(i)

						if (row != null && !isRowEmpty(row, formatter, evaluator)) {

							String key = buildValueByHeaders(row, keyHeaders, keyColumns, formatter, evaluator)
							String value = buildValueByHeaders(row, compareHeaders, compareColumns, formatter, evaluator)

							String fullKey = key

							if (!expectedValuesByKey.containsKey(fullKey)) {

								expectedValuesByKey.put(fullKey, value)
								sourceByKey.put(fullKey, file.getName() + " row " + (i + 1))
							} else {

								String expectedValue = expectedValuesByKey.get(fullKey)

								if (!safeText(expectedValue).equals(safeText(value))) {

									errors.append("[ERROR] Discrepancy found for key [")
											.append(fullKey)
											.append("]. Expected from ")
											.append(sourceByKey.get(fullKey))
											.append(": ")
											.append(expectedValue)
											.append(" | Found in ")
											.append(file.getName())
											.append(" row ")
											.append(i + 1)
											.append(": ")
											.append(value)
											.append("\n")
								}
							}
						}
					}
				}
			}
		} catch (Exception e) {

			errors.append("[ERROR] Discrepancy validation failed for file '")
					.append(file.getName())
					.append("': ")
					.append(e.getClass().getSimpleName())
					.append(" | ")
					.append(e.getMessage())
					.append("\n")
		} finally {

			try {
				if (workbook != null) {
					workbook.close()
				}
			} catch (Exception ignored) {}

			try {
				if (input != null) {
					input.close()
				}
			} catch (Exception ignored) {}
		}

		return errors.toString()
	}

	private List<File> collectExcelFiles(File folder, boolean includeSubFolders) {

		List<File> result = []

		File[] children = folder.listFiles()

		if (children != null) {

			for (File child : children) {

				if (child.isFile() && isExcelFile(child.getName()) && !child.getName().startsWith('~$')) {

					result.add(child)
				} else if (includeSubFolders && child.isDirectory()) {

					List<File> childFiles = collectExcelFiles(child, includeSubFolders)

					for (File childFile : childFiles) {
						result.add(childFile)
					}
				}
			}
		}

		return result
	}

	private List<File> parseFiles(String filesToValidate) {

		List<File> result = []
		List<String> paths = splitText(filesToValidate)

		for (String path : paths) {

			String cleanPath = safeText(path)

			if (cleanPath.length() > 0) {
				result.add(new File(cleanPath))
			}
		}

		return result
	}

	private List<String> splitText(String value) {

		List<String> result = []

		String text = safeText(value)
				.replace('\r\n', String.valueOf(LIST_SEPARATOR))
				.replace('\n', String.valueOf(LIST_SEPARATOR))
				.replace(';', String.valueOf(LIST_SEPARATOR))

		if (text.length() > 0) {

			String[] parts = text.split('\\|')

			for (String part : parts) {

				String clean = safeText(part)

				if (clean.length() > 0) {
					result.add(clean)
				}
			}
		}

		return result.unique()
	}

	private boolean isExcelFile(String fileName) {

		String name = safeText(fileName).toLowerCase()

		return name.endsWith('.xlsx') ||
				name.endsWith('.xls') ||
				name.endsWith('.xlsm')
	}

	private String resolveValidationTypeForFile(File file, String defaultValidationType) {

		String source = safeText(file.getAbsolutePath()).toUpperCase()

		if (source.contains('EUR')) {
			return 'EUR'
		}

		if (source.contains('USD')) {
			return 'USD'
		}

		if (source.contains('MIXTE') || source.contains('MIX')) {
			return 'MIXTE'
		}

		return normalizeValidationType(defaultValidationType)
	}

	private String normalizeValidationType(String validationType) {

		String value = safeText(validationType).toUpperCase()

		if (value.contains('EUR')) {
			return 'EUR'
		}

		if (value.contains('USD')) {
			return 'USD'
		}

		if (value.contains('MIX')) {
			return 'MIXTE'
		}

		return safeText(validationType)
	}

	private boolean isCurrencyRefRequired(String validationType) {
		return !('EUR'.equalsIgnoreCase(validationType) || 'USD'.equalsIgnoreCase(validationType))
	}

	private boolean shouldValidateRow(String validationType, String currency, String currencyRef) {

		String type = normalizeValidationType(validationType)

		if ('EUR'.equalsIgnoreCase(type)) {
			return 'EUR'.equalsIgnoreCase(safeText(currency))
		}

		if ('USD'.equalsIgnoreCase(type)) {
			return 'USD'.equalsIgnoreCase(safeText(currency))
		}

		return safeText(currency).length() > 0 && safeText(currency).equalsIgnoreCase(safeText(currencyRef))
	}

	private int findHeaderColumn(Sheet sheet, String headerName, DataFormatter formatter) {

		Row headerRow = sheet.getRow(0)

		if (headerRow == null) {
			KeywordUtil.logInfo("[ERROR] Header row not found in row 1.")
			return -1
		}

		String expectedHeader = normalizeHeader(headerName)
		short lastCellNum = headerRow.getLastCellNum()

		if (lastCellNum <= 0) {
			KeywordUtil.logInfo("[ERROR] Header row is empty.")
			return -1
		}

		for (int i = 0; i < lastCellNum; i++) {

			Cell cell = headerRow.getCell(i)
			String value = cell == null ? '' : formatter.formatCellValue(cell).trim()

			if (normalizeHeader(value).equals(expectedHeader)) {
				return i
			}
		}

		KeywordUtil.logInfo("[ERROR] Header '" + headerName + "' not found.")
		return -1
	}

	private Map<String, Integer> resolveHeaderColumns(Sheet sheet, List<String> headers, DataFormatter formatter) {

		Map<String, Integer> result = [:]

		for (String header : headers) {

			int columnIndex = findHeaderColumn(sheet, header, formatter)

			if (columnIndex >= 0) {
				result.put(header, columnIndex)
			}
		}

		return result
	}

	private String buildValueByHeaders(
			Row row,
			List<String> headers,
			Map<String, Integer> columns,
			DataFormatter formatter,
			FormulaEvaluator evaluator) {

		List<String> values = []

		for (String header : headers) {

			Integer columnIndex = columns.get(header)
			String cellText = getCellText(row, columnIndex, formatter, evaluator)

			BigDecimal number = parseNumber(cellText)

			if (number != null) {
				cellText = round2(number).toPlainString()
			}

			values.add(header + '=' + normalizeValue(cellText))
		}

		return values.join(' | ')
	}

	private String normalizeHeader(String value) {

		return safeText(value)
				.replace('\u00A0', ' ')
				.replaceAll('\\s+', ' ')
				.trim()
				.toLowerCase()
	}

	private String normalizeValue(String value) {

		return safeText(value)
				.replace('\u00A0', ' ')
				.replaceAll('\\s+', ' ')
				.trim()
	}

	private String getCellText(Row row, int col, DataFormatter formatter, FormulaEvaluator evaluator) {

		if (row == null || col < 0) {
			return ''
		}

		Cell cell = row.getCell(col)

		if (cell == null) {
			return ''
		}

		try {
			return formatter.formatCellValue(cell, evaluator).trim()
		} catch (Exception e) {
			return formatter.formatCellValue(cell).trim()
		}
	}

	private BigDecimal getCellNumber(Row row, int col, DataFormatter formatter, FormulaEvaluator evaluator) {

		if (row == null || col < 0) {
			return null
		}

		Cell cell = row.getCell(col)

		if (cell == null) {
			return null
		}

		try {

			CellType cellType = cell.getCellType()

			if (cellType == CellType.NUMERIC) {
				return BigDecimal.valueOf(cell.getNumericCellValue())
			}

			if (cellType == CellType.FORMULA) {

				CellValue evaluatedValue = evaluator.evaluate(cell)

				if (evaluatedValue != null && evaluatedValue.getCellType() == CellType.NUMERIC) {
					return BigDecimal.valueOf(evaluatedValue.getNumberValue())
				}
			}

			String text = getCellText(row, col, formatter, evaluator)

			return parseNumber(text)
		} catch (Exception e) {
			return null
		}
	}

	private BigDecimal parseNumber(String value) {

		String text = safeText(value)

		if (text.length() == 0) {
			return null
		}

		boolean negativeByParentheses = text.startsWith('(') && text.endsWith(')')

		text = text
				.replace('\u00A0', ' ')
				.trim()
				.replaceAll('[^0-9,\\.\\-\\(\\)]', '')
				.replace('(', '')
				.replace(')', '')

		if (text.length() == 0 || '-'.equals(text)) {
			return null
		}

		int commaCount = countOccurrences(text, ',')
		int dotCount = countOccurrences(text, '.')

		if (commaCount > 0 && dotCount > 0) {

			int lastComma = text.lastIndexOf(',')
			int lastDot = text.lastIndexOf('.')

			if (lastComma > lastDot) {
				text = text.replace('.', '')
				text = text.replace(',', '.')
			} else {
				text = text.replace(',', '')
			}
		} else if (commaCount > 0) {

			text = text.replace(',', '.')
		}

		BigDecimal result = new BigDecimal(text)

		if (negativeByParentheses && result.compareTo(BigDecimal.ZERO) > 0) {
			result = result.multiply(BigDecimal.valueOf(-1))
		}

		return result
	}

	private BigDecimal round2(BigDecimal value) {

		if (value == null) {
			return null
		}

		return value.setScale(2, RoundingMode.HALF_UP)
	}

	private boolean isRowEmpty(Row row, DataFormatter formatter, FormulaEvaluator evaluator) {

		if (row == null) {
			return true
		}

		short lastCellNum = row.getLastCellNum()

		if (lastCellNum <= 0) {
			return true
		}

		for (int i = 0; i < lastCellNum; i++) {

			Cell cell = row.getCell(i)

			if (cell != null) {

				String value = ''

				try {
					value = formatter.formatCellValue(cell, evaluator).trim()
				} catch (Exception e) {
					value = formatter.formatCellValue(cell).trim()
				}

				if (value.length() > 0) {
					return false
				}
			}
		}

		return true
	}

	private int parseIntOrZero(String value) {

		try {
			return Integer.parseInt(safeText(value))
		} catch (Exception e) {
			return 0
		}
	}

	private int countOccurrences(String text, String token) {

		int count = 0
		int index = 0

		while (index >= 0) {

			index = text.indexOf(token, index)

			if (index >= 0) {
				count++
				index = index + token.length()
			}
		}

		return count
	}

	private boolean isTrue(String value) {

		String text = safeText(value).toLowerCase()

		return text == 'true' ||
				text == 'yes' ||
				text == 'y' ||
				text == '1' ||
				text == 'sim'
	}

	private String safeText(Object value) {

		if (value == null) {
			return ''
		}

		return value.toString().trim()
	}
}
