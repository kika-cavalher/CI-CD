package common

import static com.kms.katalon.core.testobject.ObjectRepository.findTestObject

import java.util.Arrays

import org.openqa.selenium.Keys

import com.kms.katalon.core.model.FailureHandling
import com.kms.katalon.core.testobject.TestObject
import com.kms.katalon.core.util.KeywordUtil
import com.kms.katalon.core.webui.keyword.WebUiBuiltInKeywords as WebUI

import cucumber.api.java.en.And
import cucumber.api.java.en.Given
import cucumber.api.java.en.Then
import cucumber.api.java.en.When

class Navigation {

	private static final int DEFAULT_TIMEOUT = 60
	private static final int SHORT_TIMEOUT = 10

	private String currentDatabaseName = ''

	@Given("Hue home is opened")
	def hueHomeIsOpenedStep() {
		confirmHueIsInCorrectHomeBreadcrumb()
	}

	@When('^user searches for database "([^"]*)"$')
	def userSearchesForDatabaseStep(String databaseName) {
		searchDatabase(databaseName)
	}

	@When("user searches for the configured default database")
	def userSearchesForConfiguredDefaultDatabaseStep() {
		searchDatabase(currentDatabaseName)
	}

	@And('^user selects database "([^"]*)"$')
	def userSelectsDatabaseStep(String databaseName) {
		selectDatabase(databaseName)
	}

	@And("user selects the first database result")
	def userSelectsFirstDatabaseResultStep() {
		selectDatabase(currentDatabaseName)
	}

	@Then('^selected database title should be "([^"]*)"$')
	def selectedDatabaseTitleShouldBeStep(String databaseName) {
		validateSelectedDatabaseTitle(databaseName)
	}

	@Then("selected database title should be displayed")
	def selectedDatabaseTitleShouldBeDisplayedStep() {
		validateSelectedDatabaseTitle(currentDatabaseName)
	}

	@Given('^database menu "([^"]*)" is selected$')
	def databaseMenuIsSelectedStep(String databaseName) {
		KeywordUtil.logInfo("Starting complete database menu flow for: " + databaseName)

		confirmHueIsInCorrectHomeBreadcrumb()
		searchDatabase(databaseName)
		selectDatabase(databaseName)
		validateSelectedDatabaseTitle(databaseName)

		KeywordUtil.logInfo("Complete database menu flow finished successfully for: " + databaseName)
	}

	private void confirmHueIsInCorrectHomeBreadcrumb() {
		KeywordUtil.logInfo("Confirming Hue is in the correct breadcrumb position")

		WebUI.waitForPageLoad(DEFAULT_TIMEOUT)

		boolean defaultBreadcrumbVisible = isObjectPresent('Home/breadcrumb_DefaultDatabase', SHORT_TIMEOUT)

		if (defaultBreadcrumbVisible) {
			KeywordUtil.logInfo("Default database breadcrumb was found. Returning to Hive breadcrumb.")

			WebUI.waitForElementClickable(
				findTestObject('Home/button_BreadcrumbBack'),
				DEFAULT_TIMEOUT,
				FailureHandling.STOP_ON_FAILURE
			)

			WebUI.click(findTestObject('Home/button_BreadcrumbBack'))
			WebUI.delay(1)
		} else {
			KeywordUtil.logInfo("Default database breadcrumb was not found. Checking if Hive breadcrumb is already visible.")
		}

		boolean hiveBreadcrumbVisible = isObjectPresent('Home/breadcrumb_Hive', DEFAULT_TIMEOUT)

		if (!hiveBreadcrumbVisible) {
			KeywordUtil.markFailedAndStop("Hue is not in the expected Hive breadcrumb position.")
		}

		KeywordUtil.logInfo("Hue is in the correct Hive breadcrumb position")
	}

	private void searchDatabase(String databaseName) {
		databaseName = normalizeDatabaseName(databaseName)
		currentDatabaseName = databaseName

		KeywordUtil.logInfo("Searching database: " + databaseName)

		WebUI.waitForElementVisible(
			findTestObject('Home/input_DatabaseSearch'),
			DEFAULT_TIMEOUT,
			FailureHandling.STOP_ON_FAILURE
		)

		WebUI.click(findTestObject('Home/input_DatabaseSearch'))

		clearDatabaseSearchInput()

		WebUI.sendKeys(
			findTestObject('Home/input_DatabaseSearch'),
			databaseName
		)

		WebUI.delay(1)

		WebUI.waitForElementPresent(
			findTestObject('Home/option_FirstDatabaseSearchResult', [('databaseName') : databaseName]),
			DEFAULT_TIMEOUT,
			FailureHandling.STOP_ON_FAILURE
		)

		KeywordUtil.logInfo("Database was found in the search result list: " + databaseName)
	}

	private void selectDatabase(String databaseName) {
		databaseName = normalizeDatabaseName(databaseName)
		currentDatabaseName = databaseName

		KeywordUtil.logInfo("Selecting database: " + databaseName)

		TestObject databaseOption = findTestObject(
			'Home/option_FirstDatabaseSearchResult',
			[('databaseName') : databaseName]
		)

		WebUI.waitForElementPresent(
			databaseOption,
			DEFAULT_TIMEOUT,
			FailureHandling.STOP_ON_FAILURE
		)

		try {
			WebUI.waitForElementClickable(
				databaseOption,
				SHORT_TIMEOUT,
				FailureHandling.STOP_ON_FAILURE
			)

			WebUI.click(databaseOption)
		} catch (Exception e) {
			KeywordUtil.logInfo("Normal click failed. Trying JavaScript click.")

			WebUI.executeJavaScript(
				"arguments[0].click();",
				Arrays.asList(WebUI.findWebElement(databaseOption, DEFAULT_TIMEOUT))
			)
		}

		WebUI.delay(2)
		WebUI.waitForPageLoad(DEFAULT_TIMEOUT)

		KeywordUtil.logInfo("Database was selected: " + databaseName)
	}

	private void validateSelectedDatabaseTitle(String databaseName) {
		databaseName = normalizeDatabaseName(databaseName)
		currentDatabaseName = databaseName

		KeywordUtil.logInfo("Validating selected database title")
		KeywordUtil.logInfo("Expected database: " + databaseName)

		TestObject selectedTitleObject = findTestObject(
			'Home/title_SelectedDatabase',
			[('databaseName') : databaseName]
		)

		WebUI.waitForElementVisible(
			selectedTitleObject,
			DEFAULT_TIMEOUT,
			FailureHandling.STOP_ON_FAILURE
		)

		String actualTitle = WebUI.getText(selectedTitleObject)

		if (actualTitle == null || actualTitle.trim().length() == 0) {
			actualTitle = WebUI.getAttribute(selectedTitleObject, 'title')
		}

		actualTitle = actualTitle == null ? '' : actualTitle.trim()

		KeywordUtil.logInfo("Actual selected database title: " + actualTitle)

		if (!actualTitle.equals(databaseName)) {
			KeywordUtil.markFailedAndStop(
				"Wrong selected database. Expected: " + databaseName + " | Actual: " + actualTitle
			)
		}

		KeywordUtil.logInfo("Selected database title was validated successfully")
	}

	private void clearDatabaseSearchInput() {
		KeywordUtil.logInfo("Clearing database search input")

		TestObject searchInput = findTestObject('Home/input_DatabaseSearch')

		WebUI.click(searchInput)
		WebUI.sendKeys(searchInput, Keys.chord(Keys.CONTROL, 'a'))
		WebUI.sendKeys(searchInput, Keys.DELETE)

		WebUI.delay(1)
	}

	private boolean isObjectPresent(String objectPath, int timeoutInSeconds) {
		try {
			return WebUI.verifyElementPresent(
				findTestObject(objectPath),
				timeoutInSeconds,
				FailureHandling.OPTIONAL
			)
		} catch (Exception ignored) {
			return false
		}
	}

	private String normalizeDatabaseName(String databaseName) {
		String normalizedDatabaseName = databaseName == null ? '' : databaseName.trim()

		if (normalizedDatabaseName.length() == 0) {
			KeywordUtil.markFailedAndStop("Database name is empty")
		}

		return normalizedDatabaseName
	}
}











Sim, os .feature não mudaram. Seguem somente os Object Repository, em texto normal.

Object Repository/Home/breadcrumb_DefaultDatabase

Selection Method: XPath

Selected Locator:

//span[contains(@class,‘assist-breadcrumb-text’) and contains(normalize-space(.),‘default’)]

Object Repository/Home/button_BreadcrumbBack

Selection Method: XPath

Selected Locator:

//i[contains(@class,‘assist-breadcrumb-back’)]

Object Repository/Home/breadcrumb_Hive

Selection Method: XPath

Selected Locator:

//span[contains(@class,‘assist-breadcrumb-text’) and normalize-space(.)=‘Hive’]

Object Repository/Home/input_DatabaseSearch

Selection Method: XPath

Selected Locator:

//input[contains(@placeholder,‘Filter databases’)]

Object Repository/Home/option_FirstDatabaseSearchResult

Selection Method: XPath

Selected Locator:

//a[contains(@class,‘assist-table-link’) and .//span[normalize-space(.)=’${databaseName}’]]

Object Repository/Home/title_SelectedDatabase

Selection Method: XPath

Selected Locator:

//div[contains(@class,‘assist-breadcrumb’)]//a//span[contains(., ‘${databaseName}’)]
