package component

import java.awt.Robot
import java.awt.event.KeyEvent
import java.awt.event.InputEvent

import com.katalon.keywords.windows.WindowsImageKeywords
import com.kms.katalon.core.annotation.Keyword
import com.kms.katalon.core.model.FailureHandling
import com.kms.katalon.core.testobject.WindowsTestObject
import com.kms.katalon.core.util.KeywordUtil
import com.kms.katalon.core.windows.keyword.WindowsBuiltinKeywords as Windows

import org.openqa.selenium.remote.RemoteWebElement

import commands.ImageLoader
import commands.WindowsObjectLoader


class FilterKeywords {

	private static final Integer DEFAULT_TIMEOUT = 10
	private static final Integer POLLING_DELAY_SECONDS = 1

	private final ImageLoader imageLoader =
		new ImageLoader()


	// ======================================================
	// OPEN FILTER
	// ======================================================

	@Keyword
	void openFilter(
			String imageGroup,
			String filterFieldImageKey,
			String filterButtonImageKey,
			Integer timeoutSeconds
		) {

		Integer timeout = safeTimeout(timeoutSeconds)

		waitForImagePresent(
			imageGroup,
			filterFieldImageKey,
			timeout,
			'Filter field was not displayed.'
		)

		WindowsImageKeywords.clickImage(
			image(imageGroup, filterFieldImageKey)
		)

		waitForImagePresent(
			imageGroup,
			filterButtonImageKey,
			timeout,
			'Filter button was not displayed after opening the filter field.'
		)

		WindowsImageKeywords.clickImage(
			image(imageGroup, filterButtonImageKey)
		)
	}


	// ======================================================
	// CLEAR CURRENT SELECTION
	// ======================================================

	@Keyword
	void clearCurrentSelection(
			String imageGroup,
			String selectAllImageKey,
			String emptySelectionImageKey,
			String closeFilterImageKey,
			Integer timeoutSeconds
		) {

		Integer timeout = safeTimeout(timeoutSeconds)

		waitForImagePresent(
			imageGroup,
			selectAllImageKey,
			timeout,
			'Select all option was not displayed.'
		)

		WindowsImageKeywords.doubleClickImage(
			image(imageGroup, selectAllImageKey)
		)

		Windows.delay(POLLING_DELAY_SECONDS)

		waitForImagePresent(
			imageGroup,
			emptySelectionImageKey,
			timeout,
			'Empty selection option was not displayed.'
		)

		WindowsImageKeywords.doubleClickImage(
			image(imageGroup, emptySelectionImageKey)
		)

		Windows.delay(POLLING_DELAY_SECONDS)

		waitForImagePresent(
			imageGroup,
			closeFilterImageKey,
			timeout,
			'Close filter button was not displayed.'
		)

		WindowsImageKeywords.clickImage(
			image(imageGroup, closeFilterImageKey)
		)

		Windows.delay(POLLING_DELAY_SECONDS)
	}


	// ======================================================
	// APPLY TEXT FILTER WITH CHECKBOX
	// ======================================================

	@Keyword
	void applyTextCheckboxFilter(
			String imageGroup,
			String filterFieldImageKey,
			String filterButtonImageKey,
			String textInputImageKey,
			String checkboxImageKey,
			String closeFilterImageKey,
			String appliedFilterImageKey,
			String filterValue,
			Integer timeoutSeconds
		) {

		Integer timeout = safeTimeout(timeoutSeconds)
		String value = safeText(filterValue)

		if (value.isEmpty()) {
			KeywordUtil.markFailedAndStop(
				'Filter value cannot be empty.'
			)

			return
		}

		openFilter(
			imageGroup,
			filterFieldImageKey,
			filterButtonImageKey,
			timeout
		)

		waitForImagePresent(
			imageGroup,
			textInputImageKey,
			timeout,
			'Filter text input was not displayed.'
		)

		replaceTextOnImage(
			image(imageGroup, textInputImageKey),
			value
		)

		Windows.delay(2)

		waitForImagePresent(
			imageGroup,
			checkboxImageKey,
			timeout,
			'Filter checkbox was not displayed for value: ' +
			value
		)

		WindowsImageKeywords.clickImage(
			image(imageGroup, checkboxImageKey)
		)

		Windows.delay(POLLING_DELAY_SECONDS)

		waitForImagePresent(
			imageGroup,
			closeFilterImageKey,
			timeout,
			'Close filter button was not displayed.'
		)

		WindowsImageKeywords.clickImage(
			image(imageGroup, closeFilterImageKey)
		)

		if (!safeText(appliedFilterImageKey).isEmpty()) {
			waitForImagePresent(
				imageGroup,
				appliedFilterImageKey,
				timeout,
				'Filter was not applied correctly for value: ' +
					value
			)
		}

		KeywordUtil.logInfo(
			'[PASS] Text filter applied successfully. Value: ' +
			value
		)
	}


	// ======================================================
	// APPLY SINGLE RESULT TEXT FILTER
	// ======================================================

	@Keyword
	void applySingleResultTextFilter(
			String imageGroup,
			String objectGroup,
			String hoverImageKey,
			String hoverObjectKey,
			String filterButtonImageKey,
			String filterButtonObjectKey,
			String textInputImageKey,
			String textInputObjectKey,
			String resultCheckboxImageKey,
			String resultCheckboxObjectKey,
			String closeButtonImageKey,
			String closeButtonObjectKey,
			String appliedFilterImageKey,
			String filterValue,
			Integer timeoutSeconds
		) {

		Integer timeout = safeTimeout(timeoutSeconds)
		String value = safeText(filterValue)

		if (value.isEmpty()) {
			KeywordUtil.markFailedAndStop(
				'Filter value cannot be empty.'
			)

			return
		}

		// Hover and click the gray filter icon on the same header.
		hoverAndOpenFilterOnSameHeader(
			imageGroup,
			objectGroup,
			hoverImageKey,
			hoverObjectKey,
			[
				filterButtonImageKey
			],
			filterButtonObjectKey,
			timeout
		)

		// Search by image exactly like the existing working text filter.
		waitForImagePresent(
			imageGroup,
			textInputImageKey,
			timeout,
			'Filter text input was not displayed.'
		)

		replaceTextOnImage(
			image(
				imageGroup,
				textInputImageKey
			),
			value
		)

		Windows.delay(2)

		// Click the checkbox image returned by the search.
		waitForImagePresent(
			imageGroup,
			resultCheckboxImageKey,
			timeout,
			'Filter checkbox was not displayed for value: ' +
				value
		)

		WindowsImageKeywords.clickImage(
			image(
				imageGroup,
				resultCheckboxImageKey
			)
		)

		Windows.delay(POLLING_DELAY_SECONDS)

		// Close by image.
		waitForImagePresent(
			imageGroup,
			closeButtonImageKey,
			timeout,
			'Close filter button was not displayed.'
		)

		WindowsImageKeywords.clickImage(
			image(
				imageGroup,
				closeButtonImageKey
			)
		)

		Windows.delay(POLLING_DELAY_SECONDS)

		KeywordUtil.logInfo(
			'[PASS] Single result text filter applied successfully. Value: ' +
				value
		)
	}

	// ======================================================
	// SELECT ONLY ONE FILTER VALUE
	// ======================================================

	@Keyword
	void selectOnlyFilterValue(
			String imageGroup,
			String objectGroup,
			String hoverImageKey,
			String hoverObjectKey,
			List<String> filterButtonImageKeys,
			String filterButtonObjectKey,
			String valuesTitleImageKey,
			String valuesTitleObjectKey,
			String selectAllCheckedImageKey,
			String selectAllObjectKey,
			String textInputImageKey,
			String textInputObjectKey,
			String resultCheckboxImageKey,
			String resultCheckboxObjectKey,
			String closeButtonImageKey,
			String closeButtonObjectKey,
			String filterValue,
			Integer timeoutSeconds
		) {

		Integer timeout = safeTimeout(timeoutSeconds)
		String value = safeText(filterValue)

		if (value.isEmpty()) {
			KeywordUtil.markFailedAndStop(
				'Filter value cannot be empty.'
			)

			return
		}

		// Hover and click the gray filter icon on the same header.
		hoverAndOpenFilterOnSameHeader(
			imageGroup,
			objectGroup,
			hoverImageKey,
			hoverObjectKey,
			filterButtonImageKeys,
			filterButtonObjectKey,
			timeout
		)

		// Search requested value without changing (Tout).
		replaceTextImageFirstThenObject(
			imageGroup,
			objectGroup,
			textInputImageKey,
			textInputObjectKey,
			value,
			timeout
		)

		Windows.delay(2)

		// Select the first checkbox returned by the search.
		clickImageFirstThenObject(
			imageGroup,
			objectGroup,
			resultCheckboxImageKey,
			resultCheckboxObjectKey,
			'Filter result checkbox: ' + value,
			timeout
		)

		Windows.delay(POLLING_DELAY_SECONDS)

		// Close filter.
		clickImageFirstThenObject(
			imageGroup,
			objectGroup,
			closeButtonImageKey,
			closeButtonObjectKey,
			'Close filter button',
			timeout
		)

		Windows.delay(POLLING_DELAY_SECONDS)

		KeywordUtil.logInfo(
			'[PASS] Only the requested filter value was selected: ' +
				value
		)
	}

	// ======================================================
	// DRAG FILTER TO TARGET AREA
	// ======================================================

	@Keyword
	void dragFilterToTargetAndValidate(
			String imageGroup,
			String sourceFilterImageKey,
			String targetAreaImageKey,
			String appliedFilterImageKey,
			String availableFiltersValidationImageKey,
			Integer timeoutSeconds
		) {

		Integer timeout = safeTimeout(timeoutSeconds)

		waitForImagePresent(
			imageGroup,
			sourceFilterImageKey,
			timeout,
			'Source filter was not displayed before drag and drop.'
		)

		waitForImagePresent(
			imageGroup,
			targetAreaImageKey,
			timeout,
			'Filter target area was not displayed.'
		)

		WindowsImageKeywords.dragAndDrop(
			image(imageGroup, sourceFilterImageKey),
			image(imageGroup, targetAreaImageKey)
		)

		waitForImagePresent(
			imageGroup,
			appliedFilterImageKey,
			timeout,
			'Filter was not applied to the target area.'
		)

		waitForImagePresent(
			imageGroup,
			availableFiltersValidationImageKey,
			timeout,
			'Available filter list was not updated after drag and drop.'
		)

		KeywordUtil.logInfo(
			'[PASS] Filter drag and drop completed and validated.'
		)
	}


	// ======================================================
	// SELECT ALL EXCEPT BLANKS
	// ======================================================

	@Keyword
	void selectAllExceptBlanks(
			String imageGroup,
			String objectGroup,
			String hoverImageKey,
			String hoverObjectKey,
			String filterButtonImageKey,
			String filterButtonObjectKey,
			String valuesTitleImageKey,
			String valuesTitleObjectKey,
			String allCheckedImageKey,
			String allCheckedObjectKey,
			String closeButtonImageKey,
			String closeButtonObjectKey,
			Integer timeoutSeconds
		) {

		Integer timeout = safeTimeout(timeoutSeconds)

		// Hover and click the gray filter icon on the same header.
		hoverAndOpenFilterOnSameHeader(
			imageGroup,
			objectGroup,
			hoverImageKey,
			hoverObjectKey,
			[
				filterButtonImageKey
			],
			filterButtonObjectKey,
			timeout
		)

		// Validate Values tab
		waitForImageOrObjectPresent(
			imageGroup,
			objectGroup,
			valuesTitleImageKey,
			valuesTitleObjectKey,
			'Values tab was not displayed after opening the filter.',
			timeout
		)

		// Select all values
		pressDownAndSpace()

		Windows.delay(POLLING_DELAY_SECONDS)

		// Deselect blanks
		pressDownAndSpace()

		// Validate that All remains checked
		waitForImageOrObjectPresent(
			imageGroup,
			objectGroup,
			allCheckedImageKey,
			allCheckedObjectKey,
			'All values were not selected after removing blanks.',
			timeout
		)

		// Close filter
		clickImageFirstThenObject(
			imageGroup,
			objectGroup,
			closeButtonImageKey,
			closeButtonObjectKey,
			'Close filter button',
			timeout
		)

		Windows.delay(POLLING_DELAY_SECONDS)

		KeywordUtil.logInfo(
			'[PASS] Filter configured with all values selected except blanks.'
		)
	}


	// ======================================================
	// IMAGE VALIDATION
	// ======================================================

	@Keyword
	Boolean isImageDisplayed(
			String imageGroup,
			String imageKey,
			Integer timeoutSeconds
		) {

		return waitForImage(
			imageGroup,
			imageKey,
			safeTimeout(timeoutSeconds)
		)
	}


	// ======================================================
	// INTERNAL HELPERS
	// ======================================================


	private void hoverImageFirstThenObject(
			String imageGroup,
			String objectGroup,
			String imageKey,
			String objectKey,
			Integer timeoutSeconds
		) {

		Integer timeout = safeTimeout(timeoutSeconds)
		Integer imageTimeout = Math.min(timeout, 3)

		Boolean imageFound = false

		try {
			imageFound = imageLoader.waitForImage(
				imageGroup,
				imageKey,
				imageTimeout
			)
		} catch (Throwable ignored) {
			imageFound = false
		}

		WindowsTestObject targetObject =
			object(objectGroup, objectKey)

		if (!Windows.waitForElementPresent(
			targetObject,
			timeout,
			FailureHandling.OPTIONAL
		)) {
			KeywordUtil.markFailedAndStop(
				'Hover target was not found.\n' +
					'Image group: ' + imageGroup + '\n' +
					'Image key: ' + imageKey + '\n' +
					'Object group: ' + objectGroup + '\n' +
					'Object key: ' + objectKey
			)

			return
		}

		List<RemoteWebElement> targetElements =
			Windows.findElements(
				targetObject
			)

		if (targetElements == null ||
			targetElements.isEmpty()) {

			KeywordUtil.markFailedAndStop(
				'Hover target element was found but its screen coordinates could not be resolved.\n' +
					'Object group: ' + objectGroup + '\n' +
					'Object key: ' + objectKey
			)

			return
		}

		RemoteWebElement targetElement =
			targetElements.get(0)

		String boundingRectangle =
			safeText(
				targetElement.getAttribute(
					'BoundingRectangle'
				)
			)

		Map<String, Integer> coordinates =
			parseBoundingRectangle(
				boundingRectangle
			)

		if (coordinates.isEmpty()) {
			KeywordUtil.markFailedAndStop(
				'Hover target was found but BoundingRectangle could not be parsed.\n' +
					'Object group: ' + objectGroup + '\n' +
					'Object key: ' + objectKey + '\n' +
					'BoundingRectangle: ' + boundingRectangle
			)

			return
		}

		Integer mouseX =
			coordinates.left +
				(
					coordinates.right -
					coordinates.left
				).intdiv(2)

		Integer mouseY =
			coordinates.top +
				(
					coordinates.bottom -
					coordinates.top
				).intdiv(2)

		Robot robot = new Robot()

		robot.mouseMove(
			mouseX,
			mouseY
		)

		Windows.delay(POLLING_DELAY_SECONDS)

		KeywordUtil.logInfo(
			imageFound
				? '[IMAGE] Hover anchor found. Physical mouse moved to the centralized Windows object.'
				: '[OBJECT] Image was not found. Physical mouse moved to the centralized Windows object.'
		)
	}

	private Map<String, Integer> parseBoundingRectangle(
			String boundingRectangle
		) {

		String value = safeText(
			boundingRectangle
		)

		if (value.isEmpty()) {
			return [:]
		}

		// Katalon 9 / WinAppDriver format:
		// Left:409 Top:204 Width:94 Height:26
		def leftTopSizeMatcher = value =~
			/(?i)left\s*[:=]\s*(-?\d+)\D+top\s*[:=]\s*(-?\d+)\D+width\s*[:=]\s*(\d+)\D+height\s*[:=]\s*(\d+)/

		if (leftTopSizeMatcher.find()) {
			Integer left =
				leftTopSizeMatcher.group(1).toInteger()

			Integer top =
				leftTopSizeMatcher.group(2).toInteger()

			Integer width =
				leftTopSizeMatcher.group(3).toInteger()

			Integer height =
				leftTopSizeMatcher.group(4).toInteger()

			return [
				left   : left,
				top    : top,
				right  : left + width,
				bottom : top + height
			]
		}

		// Alternative WinAppDriver format:
		// l=409,t=204,r=503,b=230
		def edgesMatcher = value =~
			/(?i)l\s*[:=]\s*(-?\d+)\D+t\s*[:=]\s*(-?\d+)\D+r\s*[:=]\s*(-?\d+)\D+b\s*[:=]\s*(-?\d+)/

		if (edgesMatcher.find()) {
			return [
				left   : edgesMatcher.group(1).toInteger(),
				top    : edgesMatcher.group(2).toInteger(),
				right  : edgesMatcher.group(3).toInteger(),
				bottom : edgesMatcher.group(4).toInteger()
			]
		}

		return [:]
	}

	private void clickImageFirstThenObject(
			String imageGroup,
			String objectGroup,
			String imageKey,
			String objectKey,
			String description,
			Integer timeoutSeconds
		) {

		Integer timeout = safeTimeout(timeoutSeconds)
		Integer imageTimeout = Math.min(timeout, 3)

		try {
			if (imageLoader.waitForImage(
				imageGroup,
				imageKey,
				imageTimeout
			)) {
				WindowsImageKeywords.clickImage(
					image(imageGroup, imageKey)
				)

				KeywordUtil.logInfo(
					'[IMAGE] ' + description + ' clicked.'
				)

				return
			}
		} catch (Throwable imageError) {
			KeywordUtil.logInfo(
				'[IMAGE] Could not click ' +
					description +
					'. Trying Windows object.\n' +
					'Reason: ' +
					safeText(imageError.message)
			)
		}

		WindowsTestObject targetObject =
			object(objectGroup, objectKey)

		if (!Windows.waitForElementPresent(
			targetObject,
			timeout,
			FailureHandling.OPTIONAL
		)) {
			KeywordUtil.markFailedAndStop(
				description +
					' was not found by image or Windows object.\n' +
					'Image group: ' + imageGroup + '\n' +
					'Image key: ' + imageKey + '\n' +
					'Object group: ' + objectGroup + '\n' +
					'Object key: ' + objectKey
			)

			return
		}

		Windows.click(
			targetObject,
			FailureHandling.STOP_ON_FAILURE
		)

		KeywordUtil.logInfo(
			'[OBJECT] ' + description + ' clicked.'
		)
	}

	private void waitForImageOrObjectPresent(
			String imageGroup,
			String objectGroup,
			String imageKey,
			String objectKey,
			String failureMessage,
			Integer timeoutSeconds
		) {

		Integer timeout = safeTimeout(timeoutSeconds)
		Integer imageTimeout = Math.min(timeout, 5)

		try {
			if (imageLoader.waitForImage(
				imageGroup,
				imageKey,
				imageTimeout
			)) {
				KeywordUtil.logInfo(
					'[IMAGE] Expected filter state displayed. Key: ' +
						imageKey
				)

				return
			}
		} catch (Throwable ignored) {
		}

		WindowsTestObject targetObject =
			object(objectGroup, objectKey)

		if (Windows.waitForElementPresent(
			targetObject,
			timeout,
			FailureHandling.OPTIONAL
		)) {
			KeywordUtil.logInfo(
				'[OBJECT] Expected filter state displayed. Key: ' +
					objectKey
			)

			return
		}

		KeywordUtil.markFailedAndStop(
			failureMessage + '\n' +
				'Image group: ' + imageGroup + '\n' +
				'Image key: ' + imageKey + '\n' +
				'Object group: ' + objectGroup + '\n' +
				'Object key: ' + objectKey
		)
	}

	private void pressDownAndSpace() {
		Robot robot = new Robot()

		robot.keyPress(KeyEvent.VK_DOWN)
		robot.keyRelease(KeyEvent.VK_DOWN)

		Thread.sleep(200L)

		robot.keyPress(KeyEvent.VK_SPACE)
		robot.keyRelease(KeyEvent.VK_SPACE)

		Thread.sleep(300L)
	}

	private WindowsTestObject object(
			String objectGroup,
			String objectKey
		) {

		return WindowsObjectLoader.getObject(
			objectGroup,
			objectKey
		)
	}



	private void clearAllFilterValues(
			String imageGroup,
			String objectGroup,
			String selectAllCheckedImageKey,
			String selectAllObjectKey,
			Integer timeoutSeconds
		) {

		Integer timeout = safeTimeout(timeoutSeconds)
		Integer imageTimeout = Math.min(timeout, 10)

		Boolean allCurrentlyChecked = false

		try {
			allCurrentlyChecked =
				imageLoader.waitForImage(
					imageGroup,
					selectAllCheckedImageKey,
					imageTimeout
				)
		} catch (Throwable ignored) {
			allCurrentlyChecked = false
		}

		if (allCurrentlyChecked) {
			WindowsImageKeywords.clickImage(
				image(
					imageGroup,
					selectAllCheckedImageKey
				)
			)

			Windows.delay(POLLING_DELAY_SECONDS)

			KeywordUtil.logInfo(
				'[IMAGE] All filter values deselected.'
			)

			return
		}

		WindowsTestObject selectAllObject =
			object(
				objectGroup,
				selectAllObjectKey
			)

		if (!Windows.waitForElementPresent(
			selectAllObject,
			timeout,
			FailureHandling.OPTIONAL
		)) {
			KeywordUtil.markFailedAndStop(
				'Select all filter option was not found.\n' +
					'Image group: ' + imageGroup + '\n' +
					'Image key: ' + selectAllCheckedImageKey + '\n' +
					'Object group: ' + objectGroup + '\n' +
					'Object key: ' + selectAllObjectKey
			)

			return
		}

		// Current state is partial or unchecked.
		// First click selects all.
		Windows.click(
			selectAllObject,
			FailureHandling.STOP_ON_FAILURE
		)

		Windows.delay(POLLING_DELAY_SECONDS)

		// Second click deselects all.
		Windows.click(
			selectAllObject,
			FailureHandling.STOP_ON_FAILURE
		)

		Windows.delay(POLLING_DELAY_SECONDS)

		KeywordUtil.logInfo(
			'[OBJECT] All filter values deselected.'
		)
	}


	private void hoverAndOpenFilterOnSameHeader(
			String imageGroup,
			String objectGroup,
			String hoverImageKey,
			String hoverObjectKey,
			List<String> filterButtonImageKeys,
			String filterButtonObjectKey,
			Integer timeoutSeconds
		) {

		Integer timeout = safeTimeout(timeoutSeconds)

		WindowsTestObject headerObject =
			object(
				objectGroup,
				hoverObjectKey
			)

		if (!Windows.waitForElementPresent(
			headerObject,
			timeout,
			FailureHandling.OPTIONAL
		)) {
			KeywordUtil.markFailedAndStop(
				'Filter header was not found.\n' +
					'Image group: ' + imageGroup + '\n' +
					'Image key: ' + hoverImageKey + '\n' +
					'Object group: ' + objectGroup + '\n' +
					'Object key: ' + hoverObjectKey
			)

			return
		}

		List<RemoteWebElement> headerElements =
			Windows.findElements(
				headerObject
			)

		if (headerElements == null ||
			headerElements.isEmpty()) {

			KeywordUtil.markFailedAndStop(
				'Filter header coordinates could not be resolved.\n' +
					'Object group: ' + objectGroup + '\n' +
					'Object key: ' + hoverObjectKey
			)

			return
		}

		RemoteWebElement headerElement =
			headerElements.get(0)

		String boundingRectangle =
			safeText(
				headerElement.getAttribute(
					'BoundingRectangle'
				)
			)

		Map<String, Integer> coordinates =
			parseBoundingRectangle(
				boundingRectangle
			)

		if (coordinates.isEmpty()) {
			KeywordUtil.markFailedAndStop(
				'Filter header BoundingRectangle could not be parsed.\n' +
					'Object group: ' + objectGroup + '\n' +
					'Object key: ' + hoverObjectKey + '\n' +
					'BoundingRectangle: ' + boundingRectangle
			)

			return
		}

		Integer headerWidth =
			coordinates.right -
				coordinates.left

		Integer headerCenterX =
			coordinates.left +
				headerWidth.intdiv(2)

		Integer headerCenterY =
			coordinates.top +
				(
					coordinates.bottom -
					coordinates.top
				).intdiv(2)

		Integer filterClickX =
			coordinates.right -
				Math.max(
					5,
					Math.min(
						9,
						headerWidth.intdiv(6)
					)
				)

		Robot robot = new Robot()

		robot.mouseMove(
			headerCenterX,
			headerCenterY
		)

		Windows.delay(POLLING_DELAY_SECONDS)

		robot.mouseMove(
			filterClickX,
			headerCenterY
		)

		Thread.sleep(300L)

		robot.mousePress(
			InputEvent.BUTTON1_DOWN_MASK
		)

		robot.mouseRelease(
			InputEvent.BUTTON1_DOWN_MASK
		)

		Windows.delay(POLLING_DELAY_SECONDS)

		KeywordUtil.logInfo(
			'[COORDINATE] Filter icon clicked on the same hovered header: ' +
				hoverObjectKey
		)
	}

	private void clickFirstAvailableImageThenObject(
			String imageGroup,
			String objectGroup,
			List<String> imageKeys,
			String objectKey,
			String description,
			Integer timeoutSeconds
		) {

		Integer timeout = safeTimeout(timeoutSeconds)
		Integer imageTimeout = Math.min(timeout, 10)

		List<String> safeImageKeys =
			imageKeys == null
				? []
				: imageKeys.findAll {
					String key ->
						!safeText(key).isEmpty()
				}

		for (String imageKey : safeImageKeys) {
			try {
				if (imageLoader.waitForImage(
					imageGroup,
					imageKey,
					imageTimeout
				)) {
					WindowsImageKeywords.clickImage(
						image(
							imageGroup,
							imageKey
						)
					)

					KeywordUtil.logInfo(
						'[IMAGE] ' +
							description +
							' clicked using key: ' +
							imageKey
					)

					return
				}
			} catch (Throwable imageError) {
				KeywordUtil.logInfo(
					'[IMAGE] Could not click ' +
						description +
						' using key ' +
						imageKey +
						'.\nReason: ' +
						safeText(imageError.message)
				)
			}
		}

		WindowsTestObject targetObject =
			object(
				objectGroup,
				objectKey
			)

		if (!Windows.waitForElementPresent(
			targetObject,
			timeout,
			FailureHandling.OPTIONAL
		)) {
			KeywordUtil.markFailedAndStop(
				description +
					' was not found by any image or Windows object.\n' +
					'Image group: ' + imageGroup + '\n' +
					'Image keys: ' + safeImageKeys + '\n' +
					'Object group: ' + objectGroup + '\n' +
					'Object key: ' + objectKey
			)

			return
		}

		Windows.click(
			targetObject,
			FailureHandling.STOP_ON_FAILURE
		)

		KeywordUtil.logInfo(
			'[OBJECT] ' +
				description +
				' clicked.'
		)
	}


	private void replaceTextObjectFirstThenImage(
			String imageGroup,
			String objectGroup,
			String imageKey,
			String objectKey,
			String value,
			Integer timeoutSeconds
		) {

		Integer timeout = safeTimeout(timeoutSeconds)

		WindowsTestObject inputObject =
			object(
				objectGroup,
				objectKey
			)

		if (Windows.waitForElementPresent(
			inputObject,
			timeout,
			FailureHandling.OPTIONAL
		)) {
			Windows.click(
				inputObject,
				FailureHandling.STOP_ON_FAILURE
			)

			Robot robot = new Robot()

			robot.keyPress(KeyEvent.VK_CONTROL)
			robot.keyPress(KeyEvent.VK_A)
			robot.keyRelease(KeyEvent.VK_A)
			robot.keyRelease(KeyEvent.VK_CONTROL)

			robot.keyPress(KeyEvent.VK_BACK_SPACE)
			robot.keyRelease(KeyEvent.VK_BACK_SPACE)

			Windows.setText(
				inputObject,
				value
			)

			KeywordUtil.logInfo(
				'[OBJECT] Filter text entered successfully: ' +
					value
			)

			return
		}

		Integer imageTimeout =
			Math.min(
				timeout,
				10
			)

		try {
			if (imageLoader.waitForImage(
				imageGroup,
				imageKey,
				imageTimeout
			)) {
				replaceTextOnImage(
					image(
						imageGroup,
						imageKey
					),
					value
				)

				KeywordUtil.logInfo(
					'[IMAGE] Filter text entered successfully: ' +
						value
				)

				return
			}
		} catch (Throwable imageError) {
			KeywordUtil.logInfo(
				'[IMAGE] Could not use filter text input.\n' +
					'Reason: ' +
					safeText(imageError.message)
			)
		}

		KeywordUtil.markFailedAndStop(
			'Filter text input was not found by Windows object or image.\n' +
				'Object group: ' + objectGroup + '\n' +
				'Object key: ' + objectKey + '\n' +
				'Image group: ' + imageGroup + '\n' +
				'Image key: ' + imageKey
		)
	}

	private void clickObjectFirstThenImage(
			String imageGroup,
			String objectGroup,
			String imageKey,
			String objectKey,
			String description,
			Integer timeoutSeconds
		) {

		Integer timeout = safeTimeout(timeoutSeconds)

		WindowsTestObject targetObject =
			object(
				objectGroup,
				objectKey
			)

		if (Windows.waitForElementPresent(
			targetObject,
			timeout,
			FailureHandling.OPTIONAL
		)) {
			Windows.click(
				targetObject,
				FailureHandling.STOP_ON_FAILURE
			)

			KeywordUtil.logInfo(
				'[OBJECT] ' +
					description +
					' clicked.'
			)

			return
		}

		Integer imageTimeout =
			Math.min(
				timeout,
				10
			)

		try {
			if (imageLoader.waitForImage(
				imageGroup,
				imageKey,
				imageTimeout
			)) {
				WindowsImageKeywords.clickImage(
					image(
						imageGroup,
						imageKey
					)
				)

				KeywordUtil.logInfo(
					'[IMAGE] ' +
						description +
						' clicked.'
				)

				return
			}
		} catch (Throwable imageError) {
			KeywordUtil.logInfo(
				'[IMAGE] Could not click ' +
					description +
					'.\nReason: ' +
					safeText(imageError.message)
			)
		}

		KeywordUtil.markFailedAndStop(
			description +
				' was not found by Windows object or image.\n' +
				'Object group: ' + objectGroup + '\n' +
				'Object key: ' + objectKey + '\n' +
				'Image group: ' + imageGroup + '\n' +
				'Image key: ' + imageKey
		)
	}

	private void replaceTextImageFirstThenObject(
			String imageGroup,
			String objectGroup,
			String imageKey,
			String objectKey,
			String value,
			Integer timeoutSeconds
		) {

		Integer timeout = safeTimeout(timeoutSeconds)
		Integer imageTimeout = Math.min(timeout, 10)

		try {
			if (imageLoader.waitForImage(
				imageGroup,
				imageKey,
				imageTimeout
			)) {
				replaceTextOnImage(
					image(imageGroup, imageKey),
					value
				)

				KeywordUtil.logInfo(
					'[IMAGE] Filter text entered successfully.'
				)

				return
			}
		} catch (Throwable imageError) {
			KeywordUtil.logInfo(
				'[IMAGE] Could not use the filter text input. Trying Windows object.\n' +
					'Reason: ' +
					safeText(imageError.message)
			)
		}

		WindowsTestObject inputObject =
			object(objectGroup, objectKey)

		if (!Windows.waitForElementPresent(
			inputObject,
			timeout,
			FailureHandling.OPTIONAL
		)) {
			KeywordUtil.markFailedAndStop(
				'Filter text input was not found by image or Windows object.\n' +
					'Image group: ' + imageGroup + '\n' +
					'Image key: ' + imageKey + '\n' +
					'Object group: ' + objectGroup + '\n' +
					'Object key: ' + objectKey
			)

			return
		}

		Windows.click(
			inputObject,
			FailureHandling.STOP_ON_FAILURE
		)

		Robot robot = new Robot()

		robot.keyPress(KeyEvent.VK_CONTROL)
		robot.keyPress(KeyEvent.VK_A)
		robot.keyRelease(KeyEvent.VK_A)
		robot.keyRelease(KeyEvent.VK_CONTROL)

		robot.keyPress(KeyEvent.VK_BACK_SPACE)
		robot.keyRelease(KeyEvent.VK_BACK_SPACE)

		Windows.setText(
			inputObject,
			value
		)

		KeywordUtil.logInfo(
			'[OBJECT] Filter text entered successfully.'
		)
	}

	private void replaceTextOnImage(
			String imagePath,
			String value
		) {

		WindowsImageKeywords.clickImage(imagePath)

		Robot robot = new Robot()

		robot.keyPress(KeyEvent.VK_CONTROL)
		robot.keyPress(KeyEvent.VK_A)
		robot.keyRelease(KeyEvent.VK_A)
		robot.keyRelease(KeyEvent.VK_CONTROL)

		robot.keyPress(KeyEvent.VK_BACK_SPACE)
		robot.keyRelease(KeyEvent.VK_BACK_SPACE)

		WindowsImageKeywords.typeOnImage(
			imagePath,
			value
		)
	}

	private Boolean waitForImage(
			String imageGroup,
			String imageKey,
			Integer timeoutSeconds
		) {

		return imageLoader.waitForImage(
			imageGroup,
			imageKey,
			timeoutSeconds
		)
	}

	private void waitForImagePresent(
			String imageGroup,
			String imageKey,
			Integer timeoutSeconds,
			String failureMessage
		) {

		if (!waitForImage(
			imageGroup,
			imageKey,
			timeoutSeconds
		)) {
			KeywordUtil.markFailedAndStop(
				failureMessage + '\n' +
				'Image group: ' + imageGroup + '\n' +
				'Image key: ' + imageKey
			)
		}
	}

	private String image(
			String imageGroup,
			String imageKey
		) {

		return imageLoader.getImage(
			imageGroup,
			imageKey
		)
	}

	private Integer safeTimeout(Integer timeoutSeconds) {
		return timeoutSeconds != null &&
			timeoutSeconds > 0
				? timeoutSeconds
				: DEFAULT_TIMEOUT
	}

	private String safeText(Object value) {
		return value == null
			? ''
			: value.toString().trim()
	}
}


















































import static com.kms.katalon.core.testcase.TestCaseFactory.findTestCase

import java.io.File

import com.kms.katalon.core.model.FailureHandling
import com.kms.katalon.core.util.KeywordUtil
import com.kms.katalon.core.webui.keyword.WebUiBuiltInKeywords as WebUI


// ======================================================
// MODULE CONFIG
// ======================================================

String targetEnvironment = 'REC'

String menuName = 'investigations'
String menuResultName = 'investigations'

String sectionFilterValue = '2g3'
String bilanSourceValue = 'bilan'
String resultatSourceValue = 'resultat'

String mainExportLabel = 'Investigations_Main'
String bilanExportLabel = 'Investigations_Bilan_Detail'
String resultatExportLabel = 'Investigations_Resultat_Detail'

String loginTestCase =
	'Component/Authentication/Login ' +
	targetEnvironment

String closeApplicationTestCase =
	'Component/Authentication/Close Application'


// ======================================================
// REUSABLE FILTER CONFIG
// ======================================================

String filterImageGroup =
	'JustificationDesComptesDeLatent'

String filterObjectGroup =
	'justificationDesComptesDeLatent'

String sourceHoverImageKey =
	'sourceParent'

String sourceHoverObjectKey =
	'sourceHeader'

String grayFilterButtonImageKey =
	'filterButton'

String grayFilterButtonObjectKey =
	'filterButton'

String sourceTextInputImageKey =
	'textFilterFR'

String sourceTextInputObjectKey =
	'filterTextInput'

String sourceCloseButtonImageKey =
	'closeFiltersButtonFR'

String sourceCloseButtonObjectKey =
	'closeFilterButtonFR'

String bilanCheckboxImageKey =
	'bilan'

String bilanCheckboxObjectKey =
	'sourceBilanCheckbox'

String resultatCheckboxImageKey =
	'resultat'

String resultatCheckboxObjectKey =
	'sourceResultatCheckbox'


// ======================================================
// TIMEOUT CONFIG
// ======================================================

Integer menuTimeout = 20
Integer popupTimeout = 60
Integer filterTimeout = 60
Integer alertTimeout = 15
Integer screenLoadTimeout = 90
Integer detailWindowTimeout = 90


// ======================================================
// AUTHENTICATION
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Authenticate and open application\n' +
	'Environment: ' + targetEnvironment
)

WebUI.callTestCase(
	findTestCase(loginTestCase),
	[:],
	FailureHandling.STOP_ON_FAILURE
)


// ======================================================
// OPEN INVESTIGATIONS
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Open Investigations screen\n' +
	'Search: ' + menuName + '\n' +
	'Result: ' + menuResultName
)

CustomKeywords.'component.MenuNavigator.searchAndOpenMenuByText'(
	menuName,
	menuResultName,
	menuTimeout
)


// ======================================================
// SELECT FIRST DATE AND APPLY
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Configure Investigations dates popup\n' +
	'- Clean the selected dates list\n' +
	'- Select the first available date\n' +
	'- Double click the selected date\n' +
	'- Apply the selection\n' +
	'- Wait for the Investigations tab'
)

Map datesSelection =
	CustomKeywords.
	'pageObject.JustificationDesComptesDeLatentKeywords.validateDefaultDatesAndApply'(
		popupTimeout,
		screenLoadTimeout
	)

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Investigations date applied successfully\n' +
	'Selected date: ' + datesSelection.selectedDate + '\n' +
	'Opened tab: ' + datesSelection.openedTab
)


// ======================================================
// VALIDATE REF COMPTA REQUIRED ALERT
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Validate error when REF COMPTA is not selected\n' +
	'Action: Double click first Total (ccy) result'
)

CustomKeywords.
'pageObject.JustificationDesComptesDeLatentKeywords.validateMissingRefComptaAlert'(
	alertTimeout
)


// ======================================================
// APPLY REF COMPTA FILTER
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Apply REF COMPTA filter\n' +
	'- Clear current selection\n' +
	'- Drag REF COMPTA to the target area\n' +
	'- Validate filter was applied\n' +
	'- Validate available filter list was updated'
)

CustomKeywords.
'pageObject.JustificationDesComptesDeLatentKeywords.applyRefComptaFilter'(
	filterTimeout
)


// ======================================================
// APPLY SECTION FILTER
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Apply Section filter\n' +
	'Value: ' + sectionFilterValue
)

CustomKeywords.
'pageObject.JustificationDesComptesDeLatentKeywords.applySectionFilter'(
	sectionFilterValue,
	filterTimeout
)


// ======================================================
// EXPORT MAIN INVESTIGATIONS FILE
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Export main Investigations file\n' +
	'Environment: ' + targetEnvironment + '\n' +
	'Export label: ' + mainExportLabel
)

CustomKeywords.'commands.ExportFile.exportFromTwoExportImagesToNo'(
	mainExportLabel,
	targetEnvironment
)

Map mainExportValidation =
	CustomKeywords.
	'commands.ExportFileValidationKeywords.validateExportedFileExistsAndHasRows'(
		targetEnvironment,
		mainExportLabel
	)

String mainExportPath = new File(
	mainExportValidation.folderPath.toString(),
	mainExportValidation.fileName.toString()
).absolutePath

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Main Investigations export validation passed\n' +
	'File: ' + mainExportValidation.fileName + '\n' +
	'Data rows: ' + mainExportValidation.dataRows + '\n' +
	'File size: ' + mainExportValidation.fileSize + ' bytes'
)


// ======================================================
// VALIDATE SOURCE VALUES IN MAIN EXPORT
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Validate Source values in main Investigations export\n' +
	'Expected non-empty values:\n' +
	'- BILAN\n' +
	'- RESULTAT\n' +
	'Blank Source cells and total rows are ignored'
)

Map mainSourceValidation =
	CustomKeywords.
	'pageObject.JustificationDesComptesDeLatentKeywords.validateMainExportSourceValues'(
		mainExportPath
	)

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Main export Source validation passed\n' +
	'File: ' + mainSourceValidation.fileName + '\n' +
	'Source values: ' + mainSourceValidation.sourceValues + '\n' +
	'Non-empty Source rows: ' +
		mainSourceValidation.nonEmptySourceRows + '\n' +
	'Ignored blank or total rows: ' +
		mainSourceValidation.ignoredBlankRows
)


// ======================================================
// FILTER BY BILAN SOURCE
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Apply Source filter\n' +
	'Hover: ' + sourceHoverObjectKey + '\n' +
	'Search and select only: ' +
		bilanSourceValue.toUpperCase()
)

CustomKeywords.'component.FilterKeywords.applyTextCheckboxFilter'(
	filterImageGroup,
	sourceHoverImageKey,
	grayFilterButtonImageKey,
	sourceTextInputImageKey,
	bilanCheckboxImageKey,
	sourceCloseButtonImageKey,
	'',
	bilanSourceValue,
	filterTimeout
)


// ======================================================
// EXPORT BILAN DETAIL FILE
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Open and export Bilan detail\n' +
	'Action: Double click first Total (ccy) result\n' +
	'Export label: ' + bilanExportLabel
)

String bilanWindowTitle =
	CustomKeywords.
	'pageObject.JustificationDesComptesDeLatentKeywords.openInvestigationDetail'(
		bilanSourceValue,
		detailWindowTimeout
	)

CustomKeywords.'commands.ExportFile.exportFromExportsAndExportButtonToNo'(
	bilanExportLabel,
	targetEnvironment
)

Map bilanExportValidation =
	CustomKeywords.
	'commands.ExportFileValidationKeywords.validateExportedFileExistsAndHasRows'(
		targetEnvironment,
		bilanExportLabel
	)

String bilanExportPath = new File(
	bilanExportValidation.folderPath.toString(),
	bilanExportValidation.fileName.toString()
).absolutePath

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Bilan detail export validation passed\n' +
	'Window: ' + bilanWindowTitle + '\n' +
	'File: ' + bilanExportValidation.fileName + '\n' +
	'Data rows: ' + bilanExportValidation.dataRows
)

Integer bilanDataRows =
	bilanExportValidation.dataRows as Integer

if (bilanDataRows != 1) {
	KeywordUtil.markFailedAndStop(
		'Bilan detail export must contain exactly one data row.\n' +
		'Actual data rows: ' + bilanDataRows + '\n' +
		'File: ' + bilanExportValidation.fileName
	)
}

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Bilan detail contains exactly one data row.'
)

CustomKeywords.
'pageObject.JustificationDesComptesDeLatentKeywords.closeDetailAndReturnToMain'(
	targetEnvironment,
	detailWindowTimeout
)


// ======================================================
// FILTER BY RESULTAT SOURCE
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Apply Source filter\n' +
	'Hover: ' + sourceHoverObjectKey + '\n' +
	'Search and select only: ' +
		resultatSourceValue.toUpperCase()
)

CustomKeywords.'component.FilterKeywords.applyTextCheckboxFilter'(
	filterImageGroup,
	sourceHoverImageKey,
	grayFilterButtonImageKey,
	sourceTextInputImageKey,
	resultatCheckboxImageKey,
	sourceCloseButtonImageKey,
	'',
	resultatSourceValue,
	filterTimeout
)


// ======================================================
// EXPORT RESULTAT DETAIL FILE
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Open and export Resultat detail\n' +
	'Action: Double click first Total (ccy) result\n' +
	'Export label: ' + resultatExportLabel
)

String resultatWindowTitle =
	CustomKeywords.
	'pageObject.JustificationDesComptesDeLatentKeywords.openInvestigationDetail'(
		resultatSourceValue,
		detailWindowTimeout
	)

CustomKeywords.'commands.ExportFile.exportFromExportsAndExportButtonToNo'(
	resultatExportLabel,
	targetEnvironment
)

Map resultatExportValidation =
	CustomKeywords.
	'commands.ExportFileValidationKeywords.validateExportedFileExistsAndHasRows'(
		targetEnvironment,
		resultatExportLabel
	)

String resultatExportPath = new File(
	resultatExportValidation.folderPath.toString(),
	resultatExportValidation.fileName.toString()
).absolutePath

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Resultat detail export validation passed\n' +
	'Window: ' + resultatWindowTitle + '\n' +
	'File: ' + resultatExportValidation.fileName + '\n' +
	'Data rows: ' + resultatExportValidation.dataRows
)

Integer resultatDataRows =
	resultatExportValidation.dataRows as Integer

if (resultatDataRows != 1) {
	KeywordUtil.markFailedAndStop(
		'Resultat detail export must contain exactly one data row.\n' +
		'Actual data rows: ' + resultatDataRows + '\n' +
		'File: ' + resultatExportValidation.fileName
	)
}

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Resultat detail contains exactly one data row.'
)

CustomKeywords.
'pageObject.JustificationDesComptesDeLatentKeywords.closeDetailAndReturnToMain'(
	targetEnvironment,
	detailWindowTimeout
)


// ======================================================
// VALIDATE MONTANT CCY BETWEEN FILES
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Validate exported Investigations files\n' +
	'Validations:\n' +
	'- Main file contains Total (ccy)\n' +
	'- Bilan detail contains matching Montant ccy\n' +
	'- Resultat detail contains matching Montant ccy\n' +
	'- Comparison uses absolute values rounded to zero decimals'
)

Map amountValidation =
	CustomKeywords.
	'pageObject.JustificationDesComptesDeLatentKeywords.validateMontantCcyBetweenExports'(
		mainExportPath,
		bilanExportPath,
		resultatExportPath
	)

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Investigations amount validation passed\n' +
	'Main file: ' + amountValidation.mainFileName + '\n' +
	'Bilan file: ' + amountValidation.bilanFileName + '\n' +
	'Resultat file: ' + amountValidation.resultatFileName + '\n' +
	'Main Total (ccy): ' + amountValidation.mainTotalCcy
)


// ======================================================
// CLOSE APPLICATION
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Close application\n' +
	'Environment: ' + targetEnvironment
)

WebUI.callTestCase(
	findTestCase(closeApplicationTestCase),
	[:],
	FailureHandling.STOP_ON_FAILURE
)

KeywordUtil.markPassed(
	targetEnvironment +
	' Investigations filters, exports and amount validations completed successfully.'
)


