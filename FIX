1️⃣ WindowsObjectLoader.groovy

Atualiza somente o dicionário menu com estes valores.
Incluí o botão Annuler, sem mexer no resto que já existe.

'menu': [
	'searchBox'                                  : "//*[@Name='Type keywords here']",

	'moduleDesResponsabiliteGroup'              : "//*[@Name='Balance des responsabilités']",
	'moduleDesResponsabiliteResult'             : "//*[@Name='Cube Balance des Responsabilités']",
	'moduleDesResponsabiliteOpenedTitle'        : "//*[@Name='Cube Balance des Responsabilités']",
	'moduleDesResponsabiliteOpenedCRS'          : "//*[@Name='CRS 1']",
	'moduleDesResponsabiliteOpenedData'         : "//*[@Name='Données']",
	'moduleDesResponsabiliteOpenedTotal'        : "//*[@Name='Total général']",

	'plEconomiqueBrutContentHeader'             : "//*[@Name='Montant hf Total']",

	'datesPopup'                                : "//*[@Name='Dates à afficher']",
	'datesPopupAddButton'                       : "//*[@Name='Dates à afficher']//*[@LocalizedControlType='button' and @Name='>']",
	'datesPopupApplyButton'                     : "//*[@Name='Dates à afficher']//*[@LocalizedControlType='button' and @Name='Appliquer']",
	'datesPopupCancelButton'                    : "//*[@LocalizedControlType='button' and @Name='Annuler']",
	'datesPopupSelectedListItem'                : "//*[@Name='Dates à afficher']//*[@Name='PanelControl3']//*[@LocalizedControlType='list item']",

	'columnsPopup'                              : "//*[@Name='Choix des colonnes à afficher']",
	'columnsPopupApplyButton'                   : "//*[@Name='Choix des colonnes à afficher']//*[@LocalizedControlType='button' and @Name='Appliquer']"
]
2️⃣ TC completo corrigido
REC - PL économique Brut - Validate Content
import static com.kms.katalon.core.testcase.TestCaseFactory.findTestCase

import com.kms.katalon.core.model.FailureHandling
import com.kms.katalon.core.testobject.WindowsTestObject
import com.kms.katalon.core.util.KeywordUtil
import com.kms.katalon.core.webui.keyword.WebUiBuiltInKeywords as WebUI
import com.kms.katalon.core.windows.keyword.WindowsBuiltinKeywords as Windows

import commands.WindowsObjectLoader

String targetEnvironment = 'REC'

String menuName = 'PL économique Brut'
String menuResultName = 'PL économique Brut'
String openedTabName = menuResultName

String loginTestCase = 'Component/Authentication/Login ' + targetEnvironment

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'🚀 Archive Mode validation\n' +
	'Menu search: ' + menuName + '\n' +
	'Validation: opened tab only'
)

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Authenticate and open application'
)

WebUI.callTestCase(
	findTestCase(loginTestCase),
	[:],
	FailureHandling.STOP_ON_FAILURE
)

CustomKeywords.'component.SettingsArchiveModeKeywords.enableArchiveModeFromSettings'(
	30
)

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Open Archive Mode menu\n' +
	'Result: ' + menuResultName
)

CustomKeywords.'component.MenuNavigator.searchAndOpenMenuByText'(
	menuName,
	menuResultName,
	60
)

WindowsTestObject openedTab = WindowsObjectLoader.getTabByName(openedTabName)
WindowsTestObject datesPopup = WindowsObjectLoader.getObject('menu', 'datesPopup')

Boolean tabOpened = false
Boolean datesPopupOpened = false

Integer elapsedSeconds = 0
Integer timeoutSeconds = 60

while (elapsedSeconds < timeoutSeconds) {

	if (Windows.waitForElementPresent(openedTab, 1, FailureHandling.OPTIONAL)) {
		tabOpened = true
		break
	}

	if (Windows.waitForElementPresent(datesPopup, 1, FailureHandling.OPTIONAL)) {
		datesPopupOpened = true
		break
	}

	Windows.delay(1)
	elapsedSeconds++
}

if (!tabOpened && !datesPopupOpened) {
	CustomKeywords.'commands.ImageLoader.takeEvidence'(
		'PLEconomiqueBrutTabNotOpened.png'
	)

	KeywordUtil.markFailedAndStop(
		'Archive Mode tab was not opened.\n' +
		'Expected tab or popup: ' + openedTabName
	)
}

if (datesPopupOpened) {
	CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
		'✅ Archive Mode menu opened successfully\n' +
		'Popup displayed: Dates à afficher\n' +
		'Action: cancel popup before validating tab'
	)

	cancelDatesPopupIfPresent()

	Windows.delay(2)

	tabOpened = Windows.waitForElementPresent(
		openedTab,
		30,
		FailureHandling.OPTIONAL
	)
}

if (!tabOpened) {
	CustomKeywords.'commands.ImageLoader.takeEvidence'(
		'PLEconomiqueBrutTabNotVisibleAfterPopupCancel.png'
	)

	KeywordUtil.markFailedAndStop(
		'Archive Mode tab was not visible after closing Dates à afficher popup.\n' +
		'Expected tab: ' + openedTabName
	)
}

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'✅ Archive Mode tab opened successfully\n' +
	'Tab: ' + openedTabName
)

CustomKeywords.'component.CloseTabKeywords.closeTabByName'(
	openedTabName,
	30
)

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Close application'
)

WebUI.callTestCase(
	findTestCase('Component/Authentication/Close Application'),
	[:],
	FailureHandling.STOP_ON_FAILURE
)

KeywordUtil.markPassed(
	targetEnvironment + ' Archive Mode validation completed successfully. Menu: ' + menuResultName
)

void cancelDatesPopupIfPresent() {

	try {
		WindowsTestObject datesPopup = WindowsObjectLoader.getObject('menu', 'datesPopup')

		if (!Windows.waitForElementPresent(datesPopup, 3, FailureHandling.OPTIONAL)) {
			return
		}

		WindowsTestObject cancelButton = WindowsObjectLoader.getObject('menu', 'datesPopupCancelButton')

		if (Windows.waitForElementPresent(cancelButton, 10, FailureHandling.OPTIONAL)) {
			Windows.click(cancelButton, FailureHandling.OPTIONAL)
			Windows.delay(2)
			return
		}

		CustomKeywords.'commands.ImageLoader.takeEvidence'(
			'PLEconomiqueBrutDatesPopupCancelButtonNotFound.png'
		)

		KeywordUtil.markFailedAndStop(
			'Dates à afficher popup was opened, but Annuler button was not found.'
		)
	} catch (Throwable e) {
		CustomKeywords.'commands.ImageLoader.takeEvidence'(
			'PLEconomiqueBrutDatesPopupCancelError.png'
		)

		KeywordUtil.markFailedAndStop(
			'Error while closing Dates à afficher popup.\n' +
			'Reason: ' + e.getMessage()
		)
	}
}
