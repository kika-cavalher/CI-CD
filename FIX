import java.awt.Robot
import java.awt.Toolkit
import java.awt.datatransfer.StringSelection
import java.awt.event.KeyEvent

import com.katalon.keywords.windows.WindowsImageKeywords
import com.kms.katalon.core.configuration.RunConfiguration
import com.kms.katalon.core.util.KeywordUtil
import com.kms.katalon.core.windows.keyword.WindowsBuiltinKeywords as Windows

import commands.ImageLoader

String sectionValue = '228'
String imageGroup = 'General'
String rechercheImageKey = 'rechercheTab'
String filtresImageKey = 'filtres'

Robot robot = new Robot()
robot.setAutoDelay(150)

ImageLoader imageLoader = new ImageLoader()

Closure<String> getImagePathFromLoader = { String groupName, String imageKey ->
	Map<String, String> images = imageLoader.getImages(groupName)

	if (!images.containsKey(imageKey)) {
		KeywordUtil.markFailedAndStop(
			'Image key not found in ImageLoader.\n' +
			'Image group: ' + groupName + '\n' +
			'Image key: ' + imageKey + '\n' +
			'Available keys: ' + images.keySet().join(', ')
		)
	}

	String relativePath = images[imageKey]
	String absolutePath = new File(RunConfiguration.getProjectDir(), relativePath).getAbsolutePath()
	File imageFile = new File(absolutePath)

	if (!imageFile.exists()) {
		KeywordUtil.markFailedAndStop(
			'Image file not found.\n' +
			'Image group: ' + groupName + '\n' +
			'Image key: ' + imageKey + '\n' +
			'Relative path: ' + relativePath + '\n' +
			'Absolute path: ' + absolutePath
		)
	}

	return absolutePath
}

Closure clickImageFromLoaderOrStop = { String groupName, String imageKey ->
	String imagePath = getImagePathFromLoader(groupName, imageKey)

	try {
		KeywordUtil.logInfo(
			'Click image from ImageLoader.\n' +
			'Image group: ' + groupName + '\n' +
			'Image key: ' + imageKey + '\n' +
			'Path: ' + imagePath
		)

		WindowsImageKeywords.clickImage(imagePath)
		Windows.delay(1)
	} catch (Exception e) {
		KeywordUtil.markFailedAndStop(
			'Could not click image.\n' +
			'Image group: ' + groupName + '\n' +
			'Image key: ' + imageKey + '\n' +
			'Path: ' + imagePath + '\n' +
			'Reason: ' + e.getMessage()
		)
	}
}

Closure pasteText = { String text ->
	StringSelection selection = new StringSelection(text)
	Toolkit.getDefaultToolkit().getSystemClipboard().setContents(selection, null)

	Windows.delay(1)

	robot.keyPress(KeyEvent.VK_CONTROL)
	robot.keyPress(KeyEvent.VK_V)
	robot.keyRelease(KeyEvent.VK_V)
	robot.keyRelease(KeyEvent.VK_CONTROL)
}

Closure clearFocusedField = {
	robot.keyPress(KeyEvent.VK_CONTROL)
	robot.keyPress(KeyEvent.VK_A)
	robot.keyRelease(KeyEvent.VK_A)
	robot.keyRelease(KeyEvent.VK_CONTROL)

	Windows.delay(1)

	robot.keyPress(KeyEvent.VK_BACK_SPACE)
	robot.keyRelease(KeyEvent.VK_BACK_SPACE)
}

KeywordUtil.logInfo('Step 1. Open Recherche tab.')
clickImageFromLoaderOrStop(imageGroup, rechercheImageKey)
Windows.delay(2)

KeywordUtil.logInfo('Step 2. Fill section filter with value: ' + sectionValue)
clearFocusedField()
pasteText(sectionValue)
Windows.delay(1)

KeywordUtil.logInfo('Step 3. Click Filtres button.')
clickImageFromLoaderOrStop(imageGroup, filtresImageKey)
Windows.delay(10)

KeywordUtil.markPassed('Comparatif Inter filter applied successfully. Section: ' + sectionValue)
