import groovy.json.JsonOutput
import groovy.json.JsonSlurperClassic

def saveSuiteResults(script, String reportsRoot, List suiteResults) {
    String targetFile = "${reportsRoot}\\suite-results.json"
    script.writeFile(
        file: targetFile,
        text: JsonOutput.prettyPrint(JsonOutput.toJson(suiteResults ?: [])),
        encoding: 'UTF-8'
    )
}

def loadSuiteResults(script, String reportsRoot) {
    String targetFile = "${reportsRoot}\\suite-results.json"

    if (script.fileExists(targetFile)) {
        try {
            String raw = script.readFile(file: targetFile, encoding: 'UTF-8')
            if (raw?.trim()) {
                def parsed = new JsonSlurperClassic().parseText(raw)
                if (parsed instanceof List) {
                    return parsed
                }
            }
        }
        catch (ignored) {
        }
    }

    String desktopResultFile = "${reportsRoot}\\_desktop_logs\\desktop-run-result.json"

    if (script.fileExists(desktopResultFile)) {
        try {
            String raw = script.readFile(file: desktopResultFile, encoding: 'UTF-8')
            if (raw?.trim()) {
                def parsed = new JsonSlurperClassic().parseText(raw)
                if (parsed instanceof Map) {
                    Map result = parsed as Map

                    if (!result.durationSeconds) {
                        result.durationSeconds = '0'
                    }

                    if (!result.logRel) {
                        result.logRel = '_desktop_logs/katalon_current.log'
                    }

                    if (!(result.caseSummary instanceof List)) {
                        result.caseSummary = []
                    }

                    return [result]
                }
            }
        }
        catch (ignored) {
        }
    }

    return []
}

def readSuiteMeta(script, String reportsRoot, String assetName) {
    String safeName = sanitizeFileName(assetName)
    String metaFile = "${reportsRoot}\\logs\\${safeName}.meta.properties"

    if (!script.fileExists(metaFile)) {
        return [:]
    }

    String raw = script.readFile(file: metaFile, encoding: 'UTF-8')
    Map result = [:]

    raw.readLines().each { line ->
        int idx = line.indexOf('=')
        if (idx > 0) {
            String key = line.substring(0, idx).trim()
            String value = line.substring(idx + 1).trim()
            result[key] = value
        }
    }

    return result
}

def splitSummary(String value) {
    if (!value?.trim()) {
        return []
    }

    return value
        .split(/\s*\|\|\|\s*/)
        .collect { it?.trim() }
        .findAll { it }
}

def sanitizeFileName(String value) {
    return (value ?: 'suite')
        .replaceAll(/[\\\\\\/:"*?<>|]+/, '_')
        .replaceAll(/\s+/, '_')
}

return this
