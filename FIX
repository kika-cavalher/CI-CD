// ============================================================================
// FIXED LOGIC
// ============================================================================

def executionFailed = false
def manualAborted = false

def asErrorText = { Throwable err ->
    def parts = []
    def current = err

    while (current != null) {
        parts << "${current.getClass().getName()}: ${current.getMessage() ?: ''}"
        current = current.getCause()
    }

    return parts.join(' | ').toLowerCase()
}

def isTimeoutError = { Throwable err ->
    def text = asErrorText(err)
    return text.contains('timeout') ||
           text.contains('exceededtimeout') ||
           text.contains('org.jenkinsci.plugins.workflow.steps.timeout')
}

def isManualAbortError = { Throwable err ->
    def text = asErrorText(err)
    return text.contains('flowinterruptedexception') ||
           text.contains('aborted by') ||
           text.contains('aborted')
}

def stopCurrentExecution = {
    bat '''
@echo off
schtasks /end /tn "\\eMatch Automation\\RunEMatchCommand" >nul 2>nul
schtasks /end /tn "startWinDriver" >nul 2>nul
taskkill /f /im WinAppDriver.exe /t >nul 2>nul
taskkill /f /im katalonc.exe /t >nul 2>nul
taskkill /f /im KatalonStudio.exe /t >nul 2>nul
ping -n 3 127.0.0.1 >nul
exit /b 0
'''
}

def runCollection = { String stageName, String cmdFile ->
    if (manualAborted) {
        echo "${stageName} skipped because build was manually aborted."
        return
    }

    stage(stageName) {
        try {
            timeout(time: COLLECTION_TIMEOUT_HOURS, unit: 'HOURS') {
                bat "call \"${env.WORKSPACE}\\jenkins\\${cmdFile}\""
            }

            echo "${stageName} completed successfully."
        } catch (Throwable err) {
            if (isManualAbortError(err)) {
                manualAborted = true
                currentBuild.result = 'ABORTED'

                echo "${stageName} was manually aborted by user."

                stage("Stop ${stageName}") {
                    stopCurrentExecution()
                }

                return
            }

            executionFailed = true

            if (isTimeoutError(err)) {
                echo "${stageName} exceeded the ${COLLECTION_TIMEOUT_HOURS} hour limit and was stopped."
            } else {
                echo "${stageName} failed: ${err}"
            }

            stage("Stop ${stageName}") {
                stopCurrentExecution()
            }
        }
    }
}

node(NODE_LABEL) {
    ws(SHORT_WORKSPACE) {
        stage('Checkout') {
            deleteDir()
            checkout scm
        }

        COLLECTIONS.each { collection ->
            if (!manualAborted) {
                runCollection(collection.stage as String, collection.cmd as String)
            }
        }
    }
}

if (manualAborted) {
    currentBuild.result = 'ABORTED'
    echo 'Build manually aborted. Next pipeline will not be scheduled.'
    return
}

stage('Trigger next pipeline') {
    if (RUN_NEXT_JOB && NEXT_JOB?.trim()) {
        echo "Scheduling next pipeline regardless of current result: ${NEXT_JOB}"
        build job: NEXT_JOB, wait: false, propagate: false
    } else {
        echo 'Next pipeline disabled.'
    }
}

currentBuild.result = executionFailed ? 'FAILURE' : 'SUCCESS'
return
