🛠️ Files to update in a new project
🧭 Core classes
📄 File	🔧 What must be changed	✅ Mandatory?	📝 Notes
JiraFlowCoordinator.groovy	JIRA_TOKEN, PROJECT_KEY, EPIC_KEY, JIRA_BASE_URL, ENVIRONMENT, LABELS, TEST_PLAN_SUFFIX, XML file names, report/evidence file names, suite naming logic, report folder resolution	✅ Yes	This is the main file to adapt first
JiraGateway.groovy	Jira request behavior, JQL logic, response parsing, accepted status codes, request object names if changed	✅ Yes	Review if the new Jira/Xray setup behaves differently
JiraTestPlanManager.groovy	Test Plan reuse logic, exact match strategy, manual key behavior, create-new rules	⚠️ Depends	Change if the new project needs a different Test Plan strategy
JiraExecutionPublisher.groovy	ZIP content rules, excluded folders/files, attachment upload order, artifact paths	⚠️ Depends	Change if reports/evidence structure is different
JiraGaiaReportBuilder.groovy	XML parsing rules, log parsing, screenshot matching, timing fallback, evidence logic	⚠️ Depends	Change if the new project logs/results differ
JiraGaiaHtmlTemplateBuilder.groovy	App name, title, logo path, colors, labels, layout, charts, evidence display	⚠️ Depends	Change if branding or report layout should differ



🔧 How to Reuse the Jira Integration in a New Katalon Project
📘 Overview

This page explains how to copy and configure the current Jira integration into a new Katalon project. The objective is to allow another project to reuse the same publishing flow, including Test Plan resolution, Test Execution import, execution update, attachment upload, and custom HTML report generation. To make the integration work in a different project, it is necessary to copy the integration classes and request objects, keep the expected structure, and update all project-specific values such as Jira base URL, project key, Epic key, labels, environment, report file names, logo, and any naming rules used by the new automation suite.

📂 Files that must be copied to the new project

Copy the following Groovy classes into the new Katalon project under the same package structure, keeping the package name jiraIntegration unless you intentionally want to rename it and update imports accordingly:

• JiraFlowCoordinator
• JiraGateway
• JiraTestPlanManager
• JiraExecutionPublisher
• JiraGaiaReportBuilder
• JiraGaiaHtmlTemplateBuilder

Also copy the Jira request objects into the new project under the same Object Repository folder, or recreate them manually with the same names and payload structure:

• Jira Integration/Import Results
• Jira Integration/Create Test Plan
• Jira Integration/Update Test Plan
• Jira Integration/Update Test Execution Name
• Jira Integration/Evidence attachment

Optional and currently unused objects can also be copied if you want to keep the full repository aligned with the original project:

• Jira Integration/Create Test Set
• Jira Integration/Get Test Plan
• Jira Integration/Transition Issue Status

If the new project will also use the GAIA-style HTML report with logo, copy the image asset used by the report to the same expected relative location:

• Image/gaia-logo.png

🗂️ Recommended folder structure in the new project

To avoid changing more code than necessary, the easiest approach is to keep the same expected structure inside the new Katalon project:

• Groovy classes under the package jiraIntegration
• Jira request objects under Object Repository/Jira Integration/
• report output under the project Reports folder
• logo asset under Image/gaia-logo.png
• local Test Plan key cache under TestPlanKeys/
• optional LastRun.xlsx in the project root if your flow uses it

Keeping this structure reduces the number of code changes required and makes the migration faster and safer.

🚀 Minimum setup steps in the new project

To make the integration work in another project, the recommended setup order is:

• copy the integration classes
• copy or recreate the Jira request objects
• copy the report logo if the HTML report will still use it
• confirm the new project generates a supported XML result file
• update the configuration values inside JiraFlowCoordinator
• review Jira request object URLs and payloads
• test the flow with one suite only
• confirm that the Test Plan is created or reused correctly
• confirm that the Test Execution is created and updated
• confirm that gaiaReport.html, Results.zip, and Evidences.zip are uploaded correctly

⚙️ Main file to configure first

The first file that must be updated in the new project is:

• JiraFlowCoordinator

This class contains the main integration configuration and controls the overall publishing flow. It is the central place where the new project identity, Jira mapping, naming, and file conventions should be adjusted first.

🔑 Variables that must be updated for the new project

Inside JiraFlowCoordinator, review and update at least the following values:

Jira connection and issue mapping

• JIRA_TOKEN
• PROJECT_KEY
• EPIC_KEY
• JIRA_BASE_URL

These values are project-specific and must match the Jira instance and Jira project used by the new automation repository.

Execution metadata

• ENVIRONMENT
• LABELS

These should reflect the target environment and the labeling standard used by the new project.

Optional behavior flags

• MANUAL_TEST_PLAN_KEY
• FORCE_CREATE_NEW_TEST_PLAN
• RESET_SAVED_TEST_PLAN_KEY

These values control how Test Plans are reused or recreated and may stay blank or false initially unless the new project needs a fixed behavior.

Naming and file conventions

• TEST_PLAN_SUFFIX
• PRIMARY_XML_FILE_NAME
• SECONDARY_XML_FILE_NAME
• GAIA_REPORT_FILE_NAME
• RESULT_ZIP_NAME
• EVIDENCE_FOLDER_NAME
• EVIDENCE_ZIP_NAME

These must match the file outputs generated by the new project. If the new project produces a different XML file name or uses another evidence folder name, these values must be updated here.

🧪 What the new project must generate for this integration to work

The integration expects that the project execution produces at least one valid XML result file. By default, the current code looks for:

• cucumber.xml
• JUnit_Report.xml

If the new project generates a different XML file name, update:

• PRIMARY_XML_FILE_NAME
• SECONDARY_XML_FILE_NAME

The integration also expects the report folder to contain the execution artifacts and optionally an evidence folder with screenshots or images. If the new project uses another folder name for evidence, change:

• EVIDENCE_FOLDER_NAME

🧾 What must be updated in the Jira request objects

Even if the classes are copied correctly, the integration will not work in a new project unless the Jira request objects are reviewed and aligned with the new Jira setup.

Review these points in every used request object

• Jira base URL
• bearer token variable mapping
• project key variable mapping
• Epic field mapping
• request body field names
• endpoint version
• object name and folder path consistency

Most important request object checks
Import Results

Confirm the import endpoint matches the Jira/Xray version used by the new project and still accepts the XML format generated by the automation.

Create Test Plan

Confirm the issue type is still Test Plan and the Epic relation field is still correct.

Update Test Plan

Confirm the Xray endpoint version still matches the Jira/Xray instance.

Update Test Execution Name

Confirm the fields being updated exist in the target Jira project, especially environment and labels.

Evidence attachment

Confirm attachment uploads are allowed and that the same authentication and X-Atlassian-Token: no-check pattern still applies.

⚠️ Jira field that may need special attention

One of the most important things to validate in the new project is the Epic link field used in Create Test Plan. The current object uses:

• customfield_10006

This field ID is Jira-instance-specific and may be different in another project or environment. If the new Jira project uses another custom field ID for Epic linking, update the request body in the Create Test Plan request object accordingly. If this is not updated correctly, Test Plans may still be created, but they may not be linked to the expected Epic.

🖼️ If the new project is not GAIA

If the new project is not called GAIA, you should also review the visual naming in the custom HTML report builder.

Files to review

• JiraGaiaReportBuilder
• JiraGaiaHtmlTemplateBuilder

Most common updates

• report title prefix
• application name shown in the banner
• report subtitle text
• logo file path
• logo image file
• evidence labels if needed

For example, in JiraGaiaHtmlTemplateBuilder, review constants such as:

• APP_NAME
• REPORT_TITLE_PREFIX
• LOGO_SRC

If you want the report to show another application name, branding, or logo, change them there.

🏷️ Naming rules that should be reviewed in the new project

The current integration builds names automatically from the suite name. This is useful, but it also means the naming logic may need adjustment depending on how the new project organizes its suites.

Review these methods in JiraFlowCoordinator

• getCurrentSuiteName()
• getExecutionSummary()
• getTestPlanName()

What to validate

• how the suite name is extracted from the report folder
• whether timestamp folders are used the same way
• whether the Test Plan naming rule fits the new project
• whether the execution summary format is acceptable in Jira

If the new project has different suite naming conventions, these methods are likely the first place to adjust.

💾 Saved Test Plan key reuse in the new project

The integration stores resolved Test Plan keys locally by suite name under the TestPlanKeys folder. This helps reuse Test Plans between executions and avoids searching Jira every time.

For a first migration test

It is recommended to start with:

• RESET_SAVED_TEST_PLAN_KEY = true

This forces the new project to ignore any old cached mapping and resolve the correct Test Plan fresh during the first test run.

After the first successful run, this can be changed back if you want the local reuse behavior.

📎 Attachments and ZIP packaging in the new project

The integration packages and uploads:

• gaiaReport.html
• Evidences.zip
• Results.zip

If the new project has a different artifact structure, review:

• JiraExecutionPublisher.shouldSkipReportEntry()
• JiraExecutionPublisher.shouldSkipEvidenceEntry()
• JiraExecutionPublisher.zipReports()
• JiraExecutionPublisher.zipEvidences()

These methods define what is included or excluded from the uploaded ZIP files. If the new project stores useful artifacts in other folders, or if some folders should no longer be skipped, update the exclusion rules there.

🖼️ Evidence and screenshot behavior in the new project

If the new project stores screenshots in a different way, review the evidence discovery logic.

Main files to review

• JiraFlowCoordinator
• JiraGaiaReportBuilder

Main points to validate

• evidence folder name
• evidence search roots
• screenshot file extensions
• skip rules for unwanted images
• how failed screenshots are matched to scenarios

If the new project uses another evidence naming pattern or folder structure, you will likely need to adapt:

• getEvidenceSearchRoots()
• collectFailureImages()
• attachEvidenceToFailedScenarios()

🧩 What to change if the package name changes

If you do not want to keep the package jiraIntegration, you can rename it, but then all affected classes must be updated consistently. The safest option is to keep the original package name during the first migration. Renaming the package is optional and should only be done after the integration is already working in the new project.

✅ Migration checklist for another project

Use this checklist when onboarding the integration into a new project:

• copy all required Groovy classes
• copy or recreate all used Jira request objects
• copy the logo image if the HTML report should keep branding
• update Jira variables in JiraFlowCoordinator
• validate PROJECT_KEY, EPIC_KEY, and JIRA_BASE_URL
• validate customfield_10006 or replace it with the correct Epic field
• validate XML file names generated by the new project
• validate evidence folder naming
• review naming logic for suite, execution, and Test Plan
• review labels and environment values
• review ZIP skip rules if the artifact structure is different
• set RESET_SAVED_TEST_PLAN_KEY = true for the first migration run
• execute one test suite only
• confirm Test Plan resolution
• confirm Test Execution creation
• confirm execution metadata update
• confirm attachment upload
• confirm gaiaReport.html renders correctly

🚨 Common reasons why the migration fails

The most common causes of failure when reusing this integration in another project are:

• wrong Jira token
• wrong Jira base URL
• wrong project key
• wrong Epic field ID
• XML file not found because the new project uses another name
• evidence folder not matching the configured folder name
• request object URL still pointing to old Jira environment
• execution created but not linked to Test Plan due to Xray endpoint mismatch
• execution metadata update failing because the Jira field does not exist in the new project
• attachment upload failing because the file path is wrong or the file is empty
• report branding still pointing to old GAIA logo or old application name

🏁 Recommended first test strategy

For the first migration into a new project, the safest approach is to test with only one small suite and validate the entire flow end to end. Start by confirming report folder resolution and XML detection locally, then confirm the Test Plan search or creation, then confirm the execution import, metadata update, and attachments. Only after that should the integration be considered ready for broader reuse in the new automation repository.





📂 File by file guide
📄 JiraFlowCoordinator.groovy
🧩 Role

Main coordinator of the integration flow.

✏️ Update these values
Variable	What to set in the new project
JIRA_TOKEN	New Jira token
PROJECT_KEY	New Jira project key
EPIC_KEY	New Epic key
JIRA_BASE_URL	New Jira base URL
ENVIRONMENT	Target environment name
LABELS	Execution label
TEST_PLAN_SUFFIX	Test Plan naming suffix
PRIMARY_XML_FILE_NAME	Main XML file name
SECONDARY_XML_FILE_NAME	Fallback XML file name
GAIA_REPORT_FILE_NAME	HTML report file name
RESULT_ZIP_NAME	Results ZIP name
EVIDENCE_FOLDER_NAME	Evidence folder name
EVIDENCE_ZIP_NAME	Evidence ZIP name
🔍 Review these methods if the structure changes

getCurrentSuiteName()

getExecutionSummary()

getTestPlanName()

getResolvedReportFolder()

findXmlFile()

⚠️ Change this file when

suite naming is different

report folder structure is different

XML file names are different

Jira project/config values are different

📄 JiraGateway.groovy
🧩 Role

Handles all Jira communication.

✏️ Review in the new project

request object names

response parsing

accepted status codes

search logic for Test Plans

Jira field update behavior

🔍 Main methods to validate

importExecutionResults()

searchTestPlans()

getIssueByKey()

createTestPlan()

addExecutionToTestPlan()

updateTestExecution()

uploadAttachment()

⚠️ Change this file when

Jira API behavior changes

Xray endpoint version changes

response format changes

JQL search needs to be different

📄 JiraTestPlanManager.groovy
🧩 Role

Controls Test Plan reuse vs creation.

✏️ Review in the new project

fixed Test Plan usage

force create behavior

saved key validation

exact summary matching strategy

🔍 Main methods to validate

findExistingTestPlanKey()

getOrCreateTestPlanKey()

⚠️ Change this file when

the project wants a different Test Plan strategy

exact name matching is not enough

local saved key behavior should change

📄 JiraExecutionPublisher.groovy
🧩 Role

Publishes execution results and uploads attachments.

✏️ Review in the new project

ZIP content rules

excluded files/folders

upload order

report artifact paths

🔍 Main methods to validate

publishExecution()

uploadExecutionAttachments()

zipReports()

zipEvidences()

shouldSkipReportEntry()

shouldSkipEvidenceEntry()

⚠️ Change this file when

the project stores files in different folders

more artifacts must be zipped

some folders should no longer be skipped

📄 JiraGaiaReportBuilder.groovy
🧩 Role

Builds the custom HTML report data.

✏️ Review in the new project

XML parsing

execution log parsing

timing fallback logic

screenshot matching

evidence collection

🔍 Main methods to validate

createGaiaHtmlReport()

parseXmlCases()

parseExecutionScenarios()

normalizeScenarioSteps()

applySequentialTimingFallback()

attachEvidenceToFailedScenarios()

⚠️ Change this file when

logs look different

XML structure is different

screenshots are saved differently

scenario timing is not matching correctly

📄 JiraGaiaHtmlTemplateBuilder.groovy
🧩 Role

Renders the final HTML report.

✏️ Update these items
Item	What to review
APP_NAME	New application name
REPORT_TITLE_PREFIX	New report title
LOGO_SRC	New logo path
Status colors	New project palette
Labels and texts	New wording if needed
🔍 Main methods to validate

buildHtml()

normalizeModel()

buildScenarioListItem()

buildScenarioPanel()

buildChartsPanel()

buildStyles()

buildScripts()

⚠️ Change this file when

branding changes

logo changes

report layout should change

charts or evidence display should change

🌐 Jira request objects to review
📄 Import Results
✅ Must validate

base URL

Xray import endpoint

token variable

project variable

XML file variable

⚠️ Change when

XML import route changes

XML type changes

Xray version changes

📄 Create Test Plan
✅ Must validate

base URL

project key

summary

issue type = Test Plan

Epic field ID

⚠️ Very important

Check if customfield_10006 is still the correct Epic field in the new Jira project.

📄 Update Test Plan
✅ Must validate

base URL

Xray endpoint version

testPlanKey

JSON body structure

⚠️ Change when

linking execution to Test Plan works differently in the new Jira/Xray setup

📄 Update Test Execution Name
✅ Must validate

base URL

summary field

labels field

environment field

⚠️ Change when

the new Jira project uses different execution fields

more metadata must be updated

📄 Evidence attachment
✅ Must validate

base URL

attachment endpoint

X-Atlassian-Token: no-check

file variable mapping

⚠️ Change when

attachment rules differ

file upload permissions differ

new artifact types must be uploaded

🖼️ Branding files
📄 Image/gaia-logo.png
Change this when

the new project is not GAIA

another logo should appear in the report

Options

replace the file with a new logo

or change LOGO_SRC in JiraGaiaHtmlTemplateBuilder

💾 Local cache files
📄 TestPlanKeys/<suite>.txt
What to do in a new project

ignore old mappings on the first run

enable fresh resolution first

Recommended first run

Set:

RESET_SAVED_TEST_PLAN_KEY = true

Then switch back after the first successful execution if needed.

✅ Quick checklist
Before first test

 Copy all classes

 Copy all used Jira request objects

 Copy the logo file

 Update JiraFlowCoordinator.groovy

 Validate Jira URLs

 Validate PROJECT_KEY

 Validate EPIC_KEY

 Validate Epic field ID

 Validate XML file names

 Validate evidence folder name

First validation run

 Run one small suite

 Confirm Test Plan was found or created

 Confirm Test Execution was created

 Confirm summary/labels/environment were updated

 Confirm gaiaReport.html was generated

 Confirm Results.zip and Evidences.zip were uploaded

🚨 Most common migration problems
Jira config issues

wrong token

wrong Jira base URL

wrong project key

wrong Epic key

File structure issues

XML file name does not match expected name

evidence folder has another name

report folder structure is different

Jira object issues

Epic custom field ID is wrong

request object still points to old environment

execution metadata fields do not exist in new Jira project

Report issues

old logo still appears

app name still says GAIA

evidence is not being found

🏁 Best first test approach
Recommended

Use one simple suite only and validate the whole chain:

report folder found

XML found

Test Plan found or created

execution imported

execution updated

attachments uploaded

HTML report rendered correctly
