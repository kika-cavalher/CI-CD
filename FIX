pipeline {
    // Do not allocate a node globally.
    // We will choose the node inside the main execution stage.
    agent none

    options {
        // Add timestamps to every console line.
        timestamps()

        // Enable ANSI colors in console output.
        ansiColor('xterm')

        // Do not allow two builds of this pipeline at the same time.
        // This helps prevent workspace duplication and @2/@3 folders.
        disableConcurrentBuilds()

        // Global pipeline timeout.
        timeout(time: 50, unit: 'MINUTES')

        // Keep build history under control.
        buildDiscarder(logRotator(numToKeepStr: '10', daysToKeepStr: '14'))

        // We will checkout manually.
        skipDefaultCheckout(true)
    }

    parameters {
        choice(
            name: 'RUN_MODE',
            choices: ['VISIBLE', 'HEADLESS'],
            description: 'Choose how Chrome should run'
        )

        string(
            name: 'STARTUP_WAIT_SECONDS',
            defaultValue: '60',
            description: 'Seconds to wait after Gaia startup'
        )
    }

    environment {
        // Katalon Engine installation path.
        KATALON_HOME   = 'C:\\Users\\adgi5appjenkins\\Documents\\Katalon_Studio_Engine_Windows_64-8.1.0'

        // Repository URL.
        GIT_URL        = 'https://bitbucket-mut.mycloud.intrabpce.fr/scm/gi5/gaia-portal.git'

        // Jenkins credential ID for Git access.
        GIT_CRED       = '965eceec-b3a3-4ec5-8820-a857c87dd80f'

        // Real project file name found in the Gaia workspace.
        PROJECT_FILE   = 'gaia-portal.prj'

        // Reports folder inside the fixed workspace.
        REPORTS_DIR    = 'Reports'

        // Browser display settings.
        DISPLAY_WIDTH  = '1920'
        DISPLAY_HEIGHT = '1080'

        // Fixed workspace to avoid @2, @3 and @tmp folders.
        FIXED_WS       = 'D:\\jenkins\\work\\workspace\\GI5\\Gaia\\Gaia'

        // Scheduled task used by Gaia backend and frontend startup.
        GAIA_START_TASK = 'GaiaBackFrontSetup'
    }

    stages {
        stage('Execute on proper node') {
            agent {
                node {
                    // Use the visible node for visible runs.
                    // Use the standard node for headless runs.
                    label "${params.RUN_MODE == 'VISIBLE' ? 'GI5_SWDCFRNXGI55817_VISIBLE' : 'GI5_SWDCFRNXGI55817'}"

                    // Force Jenkins to always use the exact same workspace.
                    customWorkspace "${env.FIXED_WS}"
                }
            }

            stages {

                stage('Environment Setup') {
                    steps {
                        script {
                            // Print a clear execution header.
                            echo "========================================"
                            echo "Starting Gaia pipeline"
                            echo "Build Number : ${env.BUILD_NUMBER}"
                            echo "Node         : ${env.NODE_NAME}"
                            echo "Workspace    : ${env.WORKSPACE}"
                            echo "Run Mode     : ${params.RUN_MODE}"
                            echo "Project File : ${env.PROJECT_FILE}"
                            echo "========================================"
                        }

                        bat """
                            @echo off
                            REM -------------------------------------------------
                            REM Prepare reports structure in the fixed workspace.
                            REM -------------------------------------------------

                            echo Preparing reports directory...

                            if exist "${env.REPORTS_DIR}" (
                                echo Cleaning existing reports...
                                rmdir /s /q "${env.REPORTS_DIR}"
                            )

                            mkdir "${env.REPORTS_DIR}"
                            mkdir "${env.REPORTS_DIR}\\_jenkins_summary"

                            echo Reports directory ready.
                            exit /b 0
                        """

                        script {
                            // Create the summary file used later by Jenkins.
                            writeFile file: "${env.REPORTS_DIR}\\_jenkins_summary\\build_description.txt", text: ""

                            echo "Environment setup completed"
                        }
                    }
                }

                stage('Start Gaia Backend + Frontend') {
                    steps {
                        script {
                            // Print a clear startup section.
                            echo "========================================"
                            echo "Starting Gaia backend and frontend"
                            echo "Task Name: ${env.GAIA_START_TASK}"
                            echo "========================================"
                        }

                        bat """
                            @echo off
                            REM -------------------------------------------------
                            REM Start Gaia backend and frontend via Task Scheduler.
                            REM -------------------------------------------------

                            schtasks /run /tn "${env.GAIA_START_TASK}"
                            exit /b 0
                        """

                        script {
                            // Wait for Gaia startup to stabilize.
                            echo "Waiting ${params.STARTUP_WAIT_SECONDS}s for Gaia to start..."
                        }

                        sleep(time: params.STARTUP_WAIT_SECONDS.toInteger(), unit: 'SECONDS')

                        script {
                            echo "Gaia startup wait finished"
                        }
                    }
                }

                stage('Checkout gaia-portal') {
                    steps {
                        script {
                            // Print a clear workspace cleanup section.
                            echo "========================================"
                            echo "Cleaning fixed workspace before checkout"
                            echo "Fixed Workspace: ${env.FIXED_WS}"
                            echo "========================================"
                        }

                        // Clean the fixed workspace only.
                        deleteDir()

                        script {
                            // Print checkout details.
                            echo "========================================"
                            echo "Checking out repository"
                            echo "Branch: master"
                            echo "Repo  : ${env.GIT_URL}"
                            echo "========================================"
                        }

                        checkout([
                            $class: 'GitSCM',
                            branches: [[name: '*/master']],
                            userRemoteConfigs: [[url: env.GIT_URL, credentialsId: env.GIT_CRED]],
                            extensions: [
                                // Wipe the fixed workspace before checkout.
                                [$class: 'WipeWorkspace'],

                                // Full clone with timeout.
                                [$class: 'CloneOption', shallow: false, noTags: false, timeout: 30]
                            ]
                        ])

                        bat """
                            @echo off
                            REM -------------------------------------------------
                            REM Recreate report folders after checkout.
                            REM -------------------------------------------------

                            if not exist "${env.REPORTS_DIR}" mkdir "${env.REPORTS_DIR}"
                            if not exist "${env.REPORTS_DIR}\\_jenkins_summary" mkdir "${env.REPORTS_DIR}\\_jenkins_summary"

                            exit /b 0
                        """

                        script {
                            // Reset the summary file after checkout.
                            writeFile file: "${env.REPORTS_DIR}\\_jenkins_summary\\build_description.txt", text: ""

                            echo "Checkout completed successfully"
                        }
                    }
                }

                stage('Run GAIA TSC') {
                    steps {
                        script {
                            // Define browser type.
                            def browserType = 'Chrome'

                            // Build Chrome arguments based on run mode.
                            def chromeArgs = buildChromeArgs(params.RUN_MODE)

                            // Print Katalon execution header.
                            echo "========================================"
                            echo "Starting Katalon"
                            echo "Suite Collection : Test Suites/GAIA TSC"
                            echo "Browser          : ${browserType}"
                            echo "Run Mode         : ${params.RUN_MODE}"
                            echo "Node             : ${env.NODE_NAME}"
                            echo "Workspace        : ${env.WORKSPACE}"
                            echo "Project Path     : ${env.WORKSPACE}\\${env.PROJECT_FILE}"
                            echo "Report Folder    : ${env.WORKSPACE}\\${env.REPORTS_DIR}"
                            echo "========================================"

                            // Do not swallow manual aborts.
                            // If the user aborts, Jenkins should stop immediately.
                            catchError(
                                buildResult: 'SUCCESS',
                                stageResult: 'FAILURE',
                                catchInterruptions: false
                            ) {
                                executeKatalon(
                                    // Katalon installation path.
                                    location: env.KATALON_HOME,

                                    // Full execution arguments.
                                    executeArgs:
                                        "-projectPath=\"${env.WORKSPACE}\\${env.PROJECT_FILE}\" " +
                                        "-testSuiteCollectionPath=\"Test Suites/GAIA TSC\" " +
                                        "-browserType=\"${browserType}\" " +
                                        "-consoleLog -logLevel=DEBUG " +
                                        "-reportFolder=\"${env.WORKSPACE}\\${env.REPORTS_DIR}\" " +
                                        chromeArgs,

                                    // Plugin required placeholders.
                                    version: '',
                                    x11Display: '',
                                    xvfbConfiguration: ''
                                )
                            }

                            echo "Katalon execution finished"
                        }
                    }
                }
            }

            post {
                always {
                    script {
                        echo "Archiving reports..."
                    }

                    archiveArtifacts artifacts: 'Reports/**/*', allowEmptyArchive: true, excludes: '**/*.lck'

                    catchError(buildResult: 'UNSTABLE', stageResult: 'FAILURE', catchInterruptions: false) {
                        junit allowEmptyResults: true, testResults: 'Reports/**/JUnit_Report.xml, Reports/**/junit*.xml, Reports/**/TEST-*.xml'
                    }

                    script {
                        // Build a clear final status message.
                        def result = currentBuild.currentResult ?: 'UNKNOWN'

                        if (result == 'SUCCESS') {
                            echo "SUCCESS: All good"
                        } else if (result == 'UNSTABLE') {
                            echo "UNSTABLE: Finished with test failures"
                        } else if (result == 'ABORTED') {
                            echo "ABORTED: Build was canceled by user"
                        } else {
                            echo "FAILURE: Pipeline failed. Check console and reports"
                        }
                    }
                }

                aborted {
                    echo 'ABORTED: cancel requested by user. Stopping external processes now...'

                    bat """
                        @echo off
                        REM -------------------------------------------------
                        REM Stop external processes immediately when aborted.
                        REM This helps Jenkins abort without waiting forever.
                        REM -------------------------------------------------

                        schtasks /end /tn "${env.GAIA_START_TASK}" >nul 2>&1

                        taskkill /IM katalonc.exe /T /F >nul 2>&1
                        taskkill /IM chrome.exe /T /F >nul 2>&1
                        taskkill /IM chromedriver.exe /T /F >nul 2>&1
                        taskkill /IM java.exe /T /F >nul 2>&1
                        taskkill /IM cmd.exe /T /F >nul 2>&1

                        exit /b 0
                    """
                }
            }
        }
    }
}

/**
 * Build Chrome arguments for each execution mode.
 */
def buildChromeArgs(runMode) {
    // Headless mode arguments.
    if (runMode == 'HEADLESS') {
        def args = [
            '--headless=new',
            "--window-size=${env.DISPLAY_WIDTH},${env.DISPLAY_HEIGHT}",
            '--no-sandbox',
            '--disable-gpu',
            '--disable-dev-shm-usage'
        ]

        return "--config -webui.chrome.args='${args.join(' ')}'"
    }

    // Visible mode arguments.
    def args = [
        "--window-size=${env.DISPLAY_WIDTH},${env.DISPLAY_HEIGHT}",
        '--start-maximized'
    ]

    return "--config -webui.chrome.args='${args.join(' ')}'"
}
