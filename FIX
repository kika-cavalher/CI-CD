1) Keywords/jiraIntegration/JiraGateway.groovy

Replace the top of the file with this structure:

package jiraIntegration

class JiraGateway {

    JiraFlowCoordinator owner

    JiraGateway(JiraFlowCoordinator owner) {
        this.owner = owner
    }

    // keep the rest of your methods below
}
2) Keywords/jiraIntegration/JiraTestPlanManager.groovy

Replace the top of the file with this structure:

package jiraIntegration

class JiraTestPlanManager {

    JiraFlowCoordinator owner
    JiraGateway gateway

    JiraTestPlanManager(JiraFlowCoordinator owner, JiraGateway gateway) {
        this.owner = owner
        this.gateway = gateway
    }

    // keep the rest of your methods below
}
3) Keywords/jiraIntegration/JiraGaiaReportBuilder.groovy

Use this at the top:

package jiraIntegration

import java.io.File
import java.util.Date

import groovy.xml.XmlSlurper

class JiraGaiaReportBuilder {

    JiraFlowCoordinator owner

    JiraGaiaReportBuilder(JiraFlowCoordinator owner) {
        this.owner = owner
    }

    // keep the rest of your methods below
}
4) Keywords/jiraIntegration/JiraExecutionPublisher.groovy

Replace the top of the file with this structure:

package jiraIntegration

class JiraExecutionPublisher {

    JiraFlowCoordinator owner
    JiraGateway gateway

    JiraExecutionPublisher(JiraFlowCoordinator owner, JiraGateway gateway) {
        this.owner = owner
        this.gateway = gateway
    }

    // keep the rest of your methods below
}
5) Remove wrong imports from these helper files

If any of these files has this import:

import keywords.JiraIntegration

delete it.

Also delete any field like:

JiraIntegration owner

and replace with:

JiraFlowCoordinator owner
6) Your JiraFlowCoordinator.groovy instantiation is correct

This part should stay like this:

JiraGateway gateway = new JiraGateway(this)
JiraTestPlanManager testPlanManager = new JiraTestPlanManager(this, gateway)
JiraGaiaReportBuilder reportBuilder = new JiraGaiaReportBuilder(this)
JiraExecutionPublisher executionPublisher = new JiraExecutionPublisher(this, gateway)
