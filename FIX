Final model
	•	One task name
\eMatch Automation\RunEMatchDesktop
	•	One env file
D:\Projects\eMatchStarters\current-env.txt
	•	Two launchers
D:\RecEmatchAsUser.bat
D:\ProdEmatchAsUser.bat
	•	One generic starter
D:\Projects\eMatchStarters\run-ematch-command.bat



@echo off
setlocal

runas /user:"cib\cavalherer" /savecred "C:\Windows\Microsoft.NET\Framework\v4.0.30319\dfsvc.exe"
timeout /t 2 /nobreak >nul
runas /user:"cib\cavalherer" /savecred "rundll32 C:\Windows\System32\dfshim.dll,ShOpenVerbApplication \\cib.net\shareparis\Soft\ematch\IHM\Livraisons\REC\IXIS.eMatch.IHM.application"

exit /b %errorlevel%



@echo off
setlocal

runas /user:"cib\cavalherer" /savecred "C:\Windows\Microsoft.NET\Framework\v4.0.30319\dfsvc.exe"
timeout /t 2 /nobreak >nul
runas /user:"cib\cavalherer" /savecred "rundll32 C:\Windows\System32\dfshim.dll,ShOpenVerbApplication \\cib.net\shareparis\Soft\ematch\IHM\PROD\IXIS.eMatch.IHM.application"

exit /b %errorlevel%



@echo off
setlocal EnableExtensions

set "TARGET_ENV=%~1"

if not defined TARGET_ENV (
    echo [ERROR] Use REC or PRD
    exit /b 1
)

if /I "%TARGET_ENV%"=="REC" (
    set "TARGET_ENV=REC"
) else if /I "%TARGET_ENV%"=="PRD" (
    set "TARGET_ENV=PRD"
) else (
    echo [ERROR] Invalid environment: %TARGET_ENV%
    echo [ERROR] Use REC or PRD
    exit /b 1
)

> "D:\Projects\eMatchStarters\current-env.txt" echo %TARGET_ENV%
echo [SUCCESS] Environment saved: %TARGET_ENV%

exit /b 0



@echo off
setlocal EnableExtensions

set "ENV_FILE=D:\Projects\eMatchStarters\current-env.txt"
set "TARGET_ENV=%~1"

if not defined TARGET_ENV (
    if exist "%ENV_FILE%" (
        set /p TARGET_ENV=<"%ENV_FILE%"
    )
)

if not defined TARGET_ENV set "TARGET_ENV=REC"

if /I "%TARGET_ENV%"=="REC" (
    set "TARGET_ENV=REC"
    set "LAUNCHER=D:\RecEmatchAsUser.bat"
) else if /I "%TARGET_ENV%"=="PRD" (
    set "TARGET_ENV=PRD"
    set "LAUNCHER=D:\ProdEmatchAsUser.bat"
) else (
    echo [ERROR] Invalid environment: %TARGET_ENV%
    exit /b 1
)

set "RESOLUTION_SCRIPT=D:\setResolution.ps1"

echo [INFO] ==========================================
echo [INFO] Environment : %TARGET_ENV%
echo [INFO] Launcher    : %LAUNCHER%
echo [INFO] ==========================================

if exist "%RESOLUTION_SCRIPT%" (
    echo [INFO] Running resolution script...
    powershell -ExecutionPolicy Bypass -File "%RESOLUTION_SCRIPT%"
)

timeout /t 2 /nobreak >nul

if not exist "%LAUNCHER%" (
    echo [ERROR] Launcher not found: %LAUNCHER%
    exit /b 1
)

call "%LAUNCHER%"
if errorlevel 1 (
    echo [ERROR] Launcher failed
    exit /b 1
)

echo [INFO] Waiting for eMatch window...

powershell -NoProfile -Command "$deadline=(Get-Date).AddSeconds(120); do { $p=Get-Process | Where-Object { $_.MainWindowTitle -like '*eMatch*' }; if ($p) { exit 0 }; Start-Sleep -Seconds 2 } while ((Get-Date) -lt $deadline); exit 1"

if errorlevel 1 (
    echo [ERROR] eMatch window did not appear
    exit /b 1
)

echo [SUCCESS] eMatch window detected
exit /b 0

