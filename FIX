
import static com.kms.katalon.core.testdata.TestDataFactory.findTestData
import static com.kms.katalon.core.testobject.ObjectRepository.findWindowsObject

import java.awt.Robot
import java.awt.event.KeyEvent

import com.katalon.keywords.windows.WindowsImageKeywords
import com.kms.katalon.core.configuration.RunConfiguration
import com.kms.katalon.core.windows.keyword.WindowsBuiltinKeywords as Windows
import com.kms.katalon.core.windows.keyword.helper.WindowsActionHelper
import internal.GlobalVariable as GlobalVariable
import com.kms.katalon.core.windows.driver.WindowsDriverFactory

Robot robot = new Robot();
String menuName = 'PL Économique';
String rows, file;
def compareFiles = GlobalVariable.compareFiles as ArrayList;
def compareRows = GlobalVariable.compareFiles as ArrayList;

//Initializing images
menuImage = new File(RunConfiguration.getProjectDir(), 'Images/Contre-valorisation IHM/PL Economique.PNG').getAbsolutePath();
search = new File(RunConfiguration.getProjectDir(), 'Images/General/Recherce.PNG').getAbsolutePath();
export = new File(RunConfiguration.getProjectDir(), 'Images/General/Exports.PNG').getAbsolutePath();
export2 = new File(RunConfiguration.getProjectDir(), 'Images/General/Exports2.PNG').getAbsolutePath();
settings = new File(RunConfiguration.getProjectDir(), 'Images/General/HOME.PNG').getAbsolutePath();

WindowsImageKeywords.clickImage(settings);
Windows.delay(5);

//Search and open menu
Windows.waitForElementPresent(findWindowsObject('General/txtSearch'), 10);
Windows.setText(findWindowsObject('General/txtSearch'), menuName);
WindowsImageKeywords.clickImage(menuImage);
Windows.delay(50);

//Adding search criteria
WindowsImageKeywords.clickImage(search);
Windows.delay(1);
//Windows.clearText(findWindowsObject('JV Emission/txtAsOfDate'));
//Windows.sendKeys(findWindowsObject('JV Emission/txtAsOfDate'), findTestData('Emissions Input/refElementValorise').getObjectValue(2, 1));
Windows.delay(1);
Windows.clearText(findWindowsObject('Object Repository/Contre-valorisation IHM/txtSection'));
Windows.sendKeys(findWindowsObject('Object Repository/Contre-valorisation IHM/txtSection'), '228');

Windows.click(findWindowsObject('General/btnSearch'));
Windows.delay(20);

GlobalVariable.currencyType = Windows.getText(findWindowsObject('Object Repository/Contre-valorisation IHM/currType'));

String env = Windows.getText(findWindowsObject('Object Repository/General/curEnv'));
WindowsActionHelper.create(WindowsDriverFactory.getWindowsSession()).takeScreenshot(RunConfiguration.getReportFolder() + '\\Evidences\\Evidence_' + GlobalVariable.currencyType + '\\' + env + '_Rows.PNG');

//Checks if number of rows is displayed 
//if (CustomKeywords.'com.katalon.ematch.helper.EmatchHelper.checkForRows'() == 0) {
	//Exporting Excel File
	WindowsImageKeywords.clickImage(export);
	Windows.delay(2);
	WindowsImageKeywords.clickImage(export2);
	Windows.delay(1);
	
	//Get name of the file and change path to save
	file = Windows.getText(findWindowsObject('General/txtFileName'));
	compareFiles.add(file);
	
	//Save file to Report path
	CustomKeywords.'com.katalon.ematch.helper.EmatchHelper.saveToPathCurrency'();
	
	//Click no to not open the file
	non = new File(RunConfiguration.getProjectDir(), 'Images/General/No.PNG').getAbsolutePath();
	Windows.delay(15);
	WindowsImageKeywords.clickImage(non);

/*} else {
	rows = CustomKeywords.'com.katalon.ematch.helper.EmatchHelper.checkForRows'();
	//Add rows with name of the Menu
	compareRows.add(rows);
	println compareRows;
}*/

//Gets if its on EUR, USD our Mixte

	
GlobalVariable.currencyType = Windows.getText(findWindowsObject('Object Repository/Contre-valorisation IHM/currType'));

Windows.click(findWindowsObject('Object Repository/General/closeAllTabs'));
Windows.delay(10);
robot.keyPress(KeyEvent.VK_ENTER);
