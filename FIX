package commands

import static com.kms.katalon.core.testobject.ObjectRepository.findWindowsObject

import java.awt.Robot
import java.awt.Toolkit
import java.awt.datatransfer.StringSelection
import java.awt.event.InputEvent
import java.awt.event.KeyEvent

import com.katalon.keywords.windows.WindowsImageKeywords
import com.kms.katalon.core.annotation.Keyword
import com.kms.katalon.core.configuration.RunConfiguration
import com.kms.katalon.core.model.FailureHandling
import com.kms.katalon.core.testobject.WindowsTestObject
import com.kms.katalon.core.testobject.WindowsTestObject.LocatorStrategy
import com.kms.katalon.core.util.KeywordUtil
import com.kms.katalon.core.windows.driver.WindowsDriverFactory
import com.kms.katalon.core.windows.keyword.WindowsBuiltinKeywords as Windows
import com.kms.katalon.core.windows.keyword.helper.WindowsActionHelper

class MenuNavigator {

	private ImageLoader imageLoader = new ImageLoader()

	@Keyword
	def searchAndOpenMenu(String menuName, String imageGroup, String expectedObjectPath, int timeoutSeconds) {
		int effectiveTimeout = getSafeTimeout(timeoutSeconds)

		KeywordUtil.logInfo('Searching and opening menu: ' + menuName)

		imageLoader.validateGroupImagesExist(imageGroup)

		WindowsTestObject searchBox = resolveSearchBox()

		clickElementCenterByCoordinates(searchBox)
		Windows.delay(1)

		clearFocusedField()
		pasteText(menuName)
		Windows.delay(2)

		KeywordUtil.logInfo(
				'Menu search text pasted. Waiting for menu image to appear.\n' +
				'Menu name: ' + menuName + '\n' +
				'Image group: ' + imageGroup + '\n' +
				'Expected menu tab: ' + expectedObjectPath + '\n' +
				'Timeout: ' + effectiveTimeout + ' seconds'
				)

		boolean menuOpened = waitForImageClickAndValidateOpenedMenuTab(
				imageGroup,
				expectedObjectPath,
				effectiveTimeout
				)

		if (!menuOpened) {
			takeEvidence('MenuNotOpened.png')

			KeywordUtil.markFailedAndStop(
					'Menu was not opened correctly.\n' +
					'Menu name: ' + menuName + '\n' +
					'Image group: ' + imageGroup + '\n' +
					'Expected opened menu tab: ' + expectedObjectPath + '\n' +
					'The image was waited until timeout and validated only by the opened tab.'
					)
		}

		Windows.delay(3)

		takeEvidence('OpenedScreen.png')

		KeywordUtil.logInfo('Menu opened successfully and tab was validated: ' + menuName)
	}

	private boolean waitForImageClickAndValidateOpenedMenuTab(String imageGroup, String expectedObjectPath, int timeoutSeconds) {
		Map<String, String> images = imageLoader.getImages(imageGroup)

		int elapsedSeconds = 0
		int logInterval = 5

		while (elapsedSeconds < timeoutSeconds) {
			for (String imageKey : images.keySet()) {
				String imagePath = images[imageKey]

				try {
					def imageFound = WindowsImageKeywords.findImage(imagePath)

					if (isImageFound(imageFound)) {
						KeywordUtil.logInfo(
								'Menu image found. Trying click now.\n' +
								'Image group: ' + imageGroup + '\n' +
								'Image key: ' + imageKey + '\n' +
								'Image path: ' + imagePath + '\n' +
								'Elapsed seconds: ' + elapsedSeconds
								)

						boolean openedAfterSingleClick = clickAndValidateOpenedMenuTab(
								imagePath,
								expectedObjectPath,
								5,
								false
								)

						if (openedAfterSingleClick) {
							KeywordUtil.logInfo(
									'Opened menu tab validated after single click.\n' +
									'Menu tab: ' + expectedObjectPath
									)

							return true
						}

						KeywordUtil.logInfo(
								'Single click did not open the expected menu tab. Trying double click.\n' +
								'Image key: ' + imageKey
								)

						boolean openedAfterDoubleClick = clickAndValidateOpenedMenuTab(
								imagePath,
								expectedObjectPath,
								8,
								true
								)

						if (openedAfterDoubleClick) {
							KeywordUtil.logInfo(
									'Opened menu tab validated after double click.\n' +
									'Menu tab: ' + expectedObjectPath
									)

							return true
						}

						KeywordUtil.logInfo(
								'Image was found and clicked, but the expected menu tab is still not open.\n' +
								'Will continue waiting until timeout.'
								)
					}
				} catch (Exception e) {
					KeywordUtil.logInfo(
							'Image search attempt failed.\n' +
							'Image group: ' + imageGroup + '\n' +
							'Image key: ' + imageKey + '\n' +
							'Elapsed seconds: ' + elapsedSeconds + '\n' +
							'Reason: ' + e.getMessage()
							)
				}
			}

			if (elapsedSeconds == 0 || elapsedSeconds % logInterval == 0) {
				KeywordUtil.logInfo(
						'Still waiting for menu image...\n' +
						'Image group: ' + imageGroup + '\n' +
						'Elapsed seconds: ' + elapsedSeconds + '/' + timeoutSeconds
						)
			}

			Windows.delay(1)
			elapsedSeconds = elapsedSeconds + 1
		}

		KeywordUtil.logInfo(
				'Menu image was not visible before timeout.\n' +
				'Image group: ' + imageGroup + '\n' +
				'Timeout: ' + timeoutSeconds + ' seconds'
				)

		return false
	}

	private boolean clickAndValidateOpenedMenuTab(
			String imagePath,
			String expectedObjectPath,
			int waitAfterClickSeconds,
			boolean useDoubleClick
			) {
		try {
			if (useDoubleClick) {
				WindowsImageKeywords.doubleClickImage(imagePath)
				KeywordUtil.logInfo('Double click executed on image: ' + imagePath)
			} else {
				WindowsImageKeywords.clickImage(imagePath)
				KeywordUtil.logInfo('Single click executed on image: ' + imagePath)
			}
		} catch (Exception clickError) {
			KeywordUtil.logInfo(
					'Image click action failed.\n' +
					'Image path: ' + imagePath + '\n' +
					'Double click: ' + useDoubleClick + '\n' +
					'Reason: ' + clickError.getMessage()
					)
			return false
		}

		Windows.delay(waitAfterClickSeconds)

		if (isOpenedMenuTabVisible(expectedObjectPath, 20)) {
			return true
		}

		return false
	}

	private boolean isOpenedMenuTabVisible(String expectedObjectPath, int timeoutSeconds) {
		String cleanPath = expectedObjectPath
				.replace('Object Repository/', '')
				.replace('Object Repository\\', '')

		try {
			WindowsTestObject openedMenuTab = findWindowsObject(cleanPath)

			boolean isPresent = Windows.verifyElementPresent(
					openedMenuTab,
					timeoutSeconds,
					FailureHandling.OPTIONAL
					)

			if (isPresent) {
				KeywordUtil.logInfo('Opened menu tab is visible: ' + cleanPath)
				return true
			}

			KeywordUtil.logInfo('Opened menu tab is not visible yet: ' + cleanPath)
			return false
		} catch (Exception e) {
			KeywordUtil.logInfo(
					'Opened menu tab validation failed.\n' +
					'Tab object: ' + cleanPath + '\n' +
					'Reason: ' + e.getMessage()
					)

			return false
		}
	}

	private WindowsTestObject resolveSearchBox() {
		List<WindowsTestObject> candidates = []

		try {
			candidates.add(findWindowsObject('General/txtSearch'))
		} catch (Exception ignored) {
		}

		candidates.add(buildXpathObject("//*[contains(@Name,'Type keywords here')]"))
		candidates.add(buildXpathObject("//*[contains(@Name,'Type keywords')]"))
		candidates.add(buildXpathObject("//*[@LocalizedControlType='combo box' and contains(@Name,'Type')]"))
		candidates.add(buildXpathObject("//*[@LocalizedControlType='edit' and contains(@Name,'Type')]"))
		candidates.add(buildXpathObject("//*[contains(@ClassName,'ComboBox') and contains(@Name,'Type')]"))

		for (WindowsTestObject candidate : candidates) {
			try {
				boolean isPresent = Windows.verifyElementPresent(
						candidate,
						2,
						FailureHandling.OPTIONAL
						)

				if (isPresent) {
					KeywordUtil.logInfo('Search field found.')
					return candidate
				}
			} catch (Exception ignored) {
			}
		}

		takeEvidence('SearchFieldNotFound.png')

		KeywordUtil.markFailedAndStop(
				'Search field was not found.\n' +
				'Tried Object Repository: General/txtSearch\n' +
				'Tried dynamic XPath using Name: Type keywords here'
				)

		return null
	}

	private WindowsTestObject buildXpathObject(String xpath) {
		WindowsTestObject object = new WindowsTestObject()
		object.setLocatorStrategy(LocatorStrategy.XPATH)
		object.setLocator(xpath)

		return object
	}

	private void clickElementCenterByCoordinates(WindowsTestObject object) {
		def rect = Windows.getElementRect(object)

		int x = rect.getX() + Math.floor(rect.getWidth() / 2).intValue()
		int y = rect.getY() + Math.floor(getSafeHeight(rect) / 2).intValue()

		mouseClick(x, y)
	}

	private void clearFocusedField() {
		Robot robot = new Robot()
		robot.setAutoDelay(150)

		robot.keyPress(KeyEvent.VK_CONTROL)
		robot.keyPress(KeyEvent.VK_A)
		robot.keyRelease(KeyEvent.VK_A)
		robot.keyRelease(KeyEvent.VK_CONTROL)

		Windows.delay(1)

		robot.keyPress(KeyEvent.VK_BACK_SPACE)
		robot.keyRelease(KeyEvent.VK_BACK_SPACE)
	}

	private void pasteText(String text) {
		StringSelection selection = new StringSelection(text)
		Toolkit.getDefaultToolkit().getSystemClipboard().setContents(selection, null)

		Robot robot = new Robot()
		robot.setAutoDelay(150)

		robot.keyPress(KeyEvent.VK_CONTROL)
		robot.keyPress(KeyEvent.VK_V)
		robot.keyRelease(KeyEvent.VK_V)
		robot.keyRelease(KeyEvent.VK_CONTROL)

		Windows.delay(1)
	}

	private void mouseClick(int x, int y) {
		Robot robot = new Robot()
		robot.setAutoDelay(150)

		robot.mouseMove(x, y)
		Windows.delay(1)

		robot.mousePress(InputEvent.BUTTON1_DOWN_MASK)
		Windows.delay(1)
		robot.mouseRelease(InputEvent.BUTTON1_DOWN_MASK)

		Windows.delay(1)
	}

	private int getSafeHeight(def rect) {
		int height = rect.getHeight()

		if (height <= 0) {
			return 20
		}

		return height
	}

	private int getSafeTimeout(int timeoutSeconds) {
		if (timeoutSeconds <= 0) {
			return 20
		}

		return timeoutSeconds
	}

	private boolean isImageFound(def imageResult) {
		return imageResult != null && imageResult != false
	}

	private void takeEvidence(String fileName) {
		try {
			String evidenceDir = RunConfiguration.getReportFolder() + '\\Evidences\\'
			new File(evidenceDir).mkdirs()

			WindowsActionHelper
					.create(WindowsDriverFactory.getWindowsSession())
					.takeScreenshot(evidenceDir + fileName)

			KeywordUtil.logInfo('Evidence captured: ' + evidenceDir + fileName)
		} catch (Exception e) {
			KeywordUtil.logInfo('Could not capture evidence: ' + e.getMessage())
		}
	}
}
