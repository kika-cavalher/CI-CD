private Object firstPresent(Object... values) {
	if (values == null) {
		return null
	}

	for (Object value : values) {
		if (value == null) {
			continue
		}

		if (value instanceof CharSequence && safeValue(value).isEmpty()) {
			continue
		}

		return value
	}

	return null
}

private String firstNonBlank(Object... values) {
	if (values == null) {
		return ''
	}

	return firstNonBlankFromCollection(values.toList())
}

private String firstNonBlank(Collection values) {
	return firstNonBlankFromCollection(values)
}

private String firstNonBlankFromCollection(Collection values) {
	if (values == null) {
		return ''
	}

	for (def value : values) {
		String text = safeValue(value)
		if (!text.isEmpty()) {
			return text
		}
	}

	return ''
}

private List<Map> normalizeEvidence(Map item, Map model) {
	List rawEvidence = asList(firstPresent(
		item.evidence,
		item.evidences,
		item.images,
		item.attachments
	))

	String baseFolder = firstNonBlank(
		item.reportFolder,
		model.reportFolder,
		model.baseFolder,
		model.executionFolder
	)

	List<Map> result = []

	rawEvidence.each { def raw ->
		if (raw == null) {
			return
		}

		String name = ''
		String path = ''

		if (raw instanceof Map) {
			name = firstNonBlank(
				raw.name,
				raw.fileName,
				raw.title,
				raw.label
			)

			path = firstNonBlank(
				raw.href,
				raw.path,
				raw.file,
				raw.absolutePath,
				raw.relativePath,
				raw.imageSrc,
				raw.imgSrc
			)
		} else {
			path = safeValue(raw)
			name = new File(path).name
		}

		File resolved = resolveFile(path, baseFolder)
		if (resolved == null || !resolved.exists()) {
			return
		}

		String href = resolved.toURI().toString()
		boolean image = isImageFile(resolved.name)

		result.add([
			name   : name.isEmpty() ? resolved.name : name,
			href   : href,
			image  : image,
			imgSrc : image ? href : ''
		])
	}

	return result
}
