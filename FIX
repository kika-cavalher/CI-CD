import static com.kms.katalon.core.testobject.ObjectRepository.findWindowsObject

import java.awt.Robot
import java.awt.event.KeyEvent

import com.katalon.keywords.windows.WindowsImageKeywords
import com.kms.katalon.core.configuration.RunConfiguration
import com.kms.katalon.core.util.KeywordUtil
import com.kms.katalon.core.windows.driver.WindowsDriverFactory
import com.kms.katalon.core.windows.keyword.WindowsBuiltinKeywords as Windows
import com.kms.katalon.core.windows.keyword.helper.WindowsActionHelper

String parent = 'Ecarts expliqués';
String menu = 'Synthèse';
Robot robot = new Robot();

CustomKeywords.'jiraIntegration.JiraReportSteps.note'("Start Archive Mode validation for parent menu: ${parent} and child menu: ${menu}")

Windows.waitForElementPresent(findWindowsObject('General/txtSearch'), 10);
CustomKeywords.'jiraIntegration.JiraReportSteps.note'("Search field found. Typing menu: ${menu}")
Windows.setText(findWindowsObject('General/txtSearch'), menu);
		
//Check file image based on input name
temp = new File(RunConfiguration.getProjectDir(), ('Images/Archive Mode/' + menu + '.PNG')).getAbsolutePath();
tempParent = new File(RunConfiguration.getProjectDir(), ('Images/Archive Mode/' + parent + 'Menu.PNG')).getAbsolutePath();

CustomKeywords.'jiraIntegration.JiraReportSteps.note'("Resolved image paths for parent and child menu")

//Clicking images
Windows.delay(1);
CustomKeywords.'jiraIntegration.JiraReportSteps.note'("Clicking parent menu image: ${parent}")
WindowsImageKeywords.clickImage(tempParent);
Windows.delay(1);
CustomKeywords.'jiraIntegration.JiraReportSteps.note'("Clicking child menu image: ${menu}")
WindowsImageKeywords.clickImage(temp);

CustomKeywords.'com.katalon.keywords.windows.WindowsImprovedKeywords.errorWindow'(menu);

try {
	CustomKeywords.'jiraIntegration.JiraReportSteps.note'("Waiting for tab to open: ${menu}")
	Windows.delay(60)
	Windows.waitForElementPresent(findWindowsObject('Object Repository/Archive Mode/tabName'), 120);
	
	//if tab is found, take a screen shot then close it
	CustomKeywords.'jiraIntegration.JiraReportSteps.note'("Tab opened successfully. Capturing evidence for: ${menu}")
	WindowsActionHelper.create(WindowsDriverFactory.getWindowsSession()).takeScreenshot(
		RunConfiguration.getReportFolder() + '\\Evidences\\' + menu + '_' + parent + '.PNG'
	)
	
	CustomKeywords.'jiraIntegration.JiraReportSteps.note'("Closing opened tab: ${menu}")
	Windows.click(findWindowsObject('Archive Mode/btnFermer'))
	KeywordUtil.markPassed('[SUCCESS] Tab of menu ' + menu + ' was opened sucessfully.');
} catch (Exception e) {
	//otherwise, tabe was not found and there is an error
	Windows.delay(2)
	
	CustomKeywords.'jiraIntegration.JiraReportSteps.note'("Tab was not opened. Capturing error evidence for: ${menu}")
	WindowsActionHelper.create(WindowsDriverFactory.getWindowsSession()).takeScreenshot(
		RunConfiguration.getReportFolder() + '\\Evidences\\' + '[ERROR] ' + menu + '.PNG'
	)
	
	CustomKeywords.'jiraIntegration.JiraReportSteps.note'("Handling error popup for menu: ${menu}")
	Windows.click(findWindowsObject('Object Repository/Justification des Comptes de Latent/confirmBtn'))
	KeywordUtil.markFailed('\n[ERROR] Tab of menu ' + menu + ' was not opened.');
}









import static com.kms.katalon.core.testobject.ObjectRepository.findWindowsObject

import java.awt.Robot
import java.awt.event.KeyEvent

import com.katalon.keywords.windows.WindowsImageKeywords
import com.kms.katalon.core.configuration.RunConfiguration
import com.kms.katalon.core.util.KeywordUtil
import com.kms.katalon.core.windows.driver.WindowsDriverFactory
import com.kms.katalon.core.windows.keyword.WindowsBuiltinKeywords as Windows
import com.kms.katalon.core.windows.keyword.helper.WindowsActionHelper

String menu = 'Détail';

Robot robot = new Robot();

CustomKeywords.'jiraIntegration.JiraReportSteps.note'("Start Archive Mode validation for menu: ${menu} inside Ecarts de Normes")

Windows.waitForElementPresent(findWindowsObject('General/txtSearch'), 10);
CustomKeywords.'jiraIntegration.JiraReportSteps.note'("Search field found. Typing parent menu: Ecarts de Normes")
Windows.setText(findWindowsObject('General/txtSearch'), "Ecarts de Normes");
		
//Check file image based on input name
temp = new File(RunConfiguration.getProjectDir(), ('Images/Archive Mode/' + menu + '.PNG')).getAbsolutePath();

CustomKeywords.'jiraIntegration.JiraReportSteps.note'("Resolved image path for child menu: ${menu}")

//Clicking images
Windows.delay(1);
CustomKeywords.'jiraIntegration.JiraReportSteps.note'("Clicking menu image: ${menu}")
WindowsImageKeywords.clickImage(temp);
CustomKeywords.'com.katalon.keywords.windows.WindowsImprovedKeywords.errorWindow'(menu);

CustomKeywords.'jiraIntegration.JiraReportSteps.note'("Waiting for tab to open: ${menu}")
Windows.delay(50);

try {
	Windows.waitForElementPresent(findWindowsObject('Object Repository/Archive Mode/tabName'), 120);
	
	//if tab is found, take a screen shot then close it
	CustomKeywords.'jiraIntegration.JiraReportSteps.note'("Tab opened successfully. Capturing evidence for: ${menu}")
	WindowsActionHelper.create(WindowsDriverFactory.getWindowsSession()).takeScreenshot(
		RunConfiguration.getReportFolder() + '\\Evidences\\' + menu + '.PNG'
	)
	
	CustomKeywords.'jiraIntegration.JiraReportSteps.note'("Closing opened tab: ${menu}")
	Windows.click(findWindowsObject('Archive Mode/btnFermer'))
	KeywordUtil.markPassed('[SUCCESS] Tab of menu ' + menu + ' was opened sucessfully.');
} catch (Exception e) {
	//otherwise, tabe was not found and there is an error
	Windows.delay(2)
	
	CustomKeywords.'jiraIntegration.JiraReportSteps.note'("Tab was not opened. Capturing error evidence for: ${menu}")
	WindowsActionHelper.create(WindowsDriverFactory.getWindowsSession()).takeScreenshot(
		RunConfiguration.getReportFolder() + '\\Evidences\\' + '[ERROR] ' + menu + '.PNG'
	)
	
	CustomKeywords.'jiraIntegration.JiraReportSteps.note'("Handling error popup for menu: ${menu}")
	Windows.click(findWindowsObject('Object Repository/Justification des Comptes de Latent/confirmBtn'))
	KeywordUtil.markFailed('\n[ERROR] Tab of menu ' + menu + ' was not opened.');
}
