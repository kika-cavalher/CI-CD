@echo off

echo =====================================
echo STARTING EMATCH APPLICATION
echo =====================================

REM Mata instância anterior
taskkill /F /IM IXIS.Ematch.IHM.exe >nul 2>&1

REM Aguarda limpar
timeout /t 3 >nul

REM Abre app via shortcut (igual funcionava antes)
start "" "D:\RunAseMatchReq\eMatch.lnk"

REM Aguarda app subir
timeout /t 10 >nul

echo =====================================
echo EMATCH STARTED
echo =====================================

exit /b 0
