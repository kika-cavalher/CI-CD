import java.io.File

import com.kms.katalon.core.util.KeywordUtil


// ======================================================
// COMPARISON CONFIG
// ======================================================

String recEnvironment = 'REC'
String prdEnvironment = 'PRD'

String exportLabel = 'CapsFloorsGrecques'

Integer expectedColumnIndex = 14

String scenarioName =
	'REC x PRD - Caps Floors Grecques - Validate Row Sum Min Max'


// ======================================================
// RESOLVE REC EXPORT
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Resolve REC Caps Floors Grecques export\n' +
	'Environment: ' +
	recEnvironment +
	'\nExport label: ' +
	exportLabel
)

Map recExportValidation =
	CustomKeywords.
	'commands.ExportFileValidationKeywords.validateExportedFileExistsAndHasRows'(
		recEnvironment,
		exportLabel
	)

String recExportPath =
	new File(
		recExportValidation.folderPath.toString(),
		recExportValidation.fileName.toString()
	).absolutePath

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'REC Caps Floors Grecques export resolved successfully\n' +
	'File: ' +
	recExportValidation.fileName +
	'\nPath: ' +
	recExportPath +
	'\nData rows: ' +
	recExportValidation.dataRows +
	'\nFile size: ' +
	recExportValidation.fileSize +
	' bytes'
)


// ======================================================
// RESOLVE PRD EXPORT
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Resolve PRD Caps Floors Grecques export\n' +
	'Environment: ' +
	prdEnvironment +
	'\nExport label: ' +
	exportLabel
)

Map prdExportValidation =
	CustomKeywords.
	'commands.ExportFileValidationKeywords.validateExportedFileExistsAndHasRows'(
		prdEnvironment,
		exportLabel
	)

String prdExportPath =
	new File(
		prdExportValidation.folderPath.toString(),
		prdExportValidation.fileName.toString()
	).absolutePath

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'PRD Caps Floors Grecques export resolved successfully\n' +
	'File: ' +
	prdExportValidation.fileName +
	'\nPath: ' +
	prdExportPath +
	'\nData rows: ' +
	prdExportValidation.dataRows +
	'\nFile size: ' +
	prdExportValidation.fileSize +
	' bytes'
)


// ======================================================
// COMPARE REC AND PRD EXPORTS
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Compare Caps Floors Grecques REC and PRD exports\n' +
	'REC file: ' +
	recExportValidation.fileName +
	'\nPRD file: ' +
	prdExportValidation.fileName +
	'\nZero-based column index: ' +
	expectedColumnIndex +
	'\nValidations:\n' +
	'- Number of data rows\n' +
	'- Column sum\n' +
	'- Minimum value\n' +
	'- Maximum value'
)

Map comparison =
	CustomKeywords.
	'commands.RecPrdExportValidationKeywords.validateRecPrdExportRowSumMinMax'(
		recExportPath,
		prdExportPath,
		expectedColumnIndex
	)

boolean success =
	comparison != null &&
	comparison.success == true

String errors =
	comparison == null
		? '[ERROR] Validation keyword returned no result.'
		: safeText(
			comparison.errors
			)

String passedDetails =
	comparison == null
		? ''
		: safeText(
			comparison.passedDetails
			)


// ======================================================
// REPORT COMPARISON RESULT
// ======================================================

if (comparison != null) {

	CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
		(success ? '[PASS] ' : '[FAIL] ') +
		'Caps Floors Grecques REC x PRD comparison result\n' +
		'REC file: ' +
		comparison.recFileName +
		'\nPRD file: ' +
		comparison.prdFileName +
		'\nColumn index: ' +
		comparison.columnIndex +
		'\nREC column: ' +
		comparison.recColumnName +
		'\nPRD column: ' +
		comparison.prdColumnName +
		'\nREC rows: ' +
		comparison.recRows +
		'\nPRD rows: ' +
		comparison.prdRows +
		'\nREC numeric values: ' +
		comparison.recNumericValues +
		'\nPRD numeric values: ' +
		comparison.prdNumericValues +
		'\nREC sum: ' +
		comparison.recSum +
		'\nPRD sum: ' +
		comparison.prdSum +
		'\nREC minimum: ' +
		comparison.recMinimum +
		'\nPRD minimum: ' +
		comparison.prdMinimum +
		'\nREC maximum: ' +
		comparison.recMaximum +
		'\nPRD maximum: ' +
		comparison.prdMaximum +
		'\nREC read mode: ' +
		comparison.recReadMode +
		'\nPRD read mode: ' +
		comparison.prdReadMode
	)
}


// ======================================================
// FINAL RESULT
// ======================================================

if (!success) {

	String finalErrorMessage =
		errors.isEmpty()
			? 'REC x PRD validation returned an unsuccessful result.'
			: errors

	CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
		'[FAIL] Final Caps Floors Grecques validation result\n' +
		finalErrorMessage +
		(
			passedDetails.isEmpty()
				? ''
				: '\n\nSuccessful validations\n' +
					passedDetails
		)
	)

	KeywordUtil.markFailed(
		scenarioName +
		' failed.\n' +
		finalErrorMessage
	)

} else {

	CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
		'[PASS] Final Caps Floors Grecques validation result\n' +
		'REC x PRD validation completed successfully.\n' +
		passedDetails
	)

	KeywordUtil.markPassed(
		scenarioName +
		' completed successfully.'
	)
}


// ======================================================
// HELPERS
// ======================================================

String safeText(Object value) {

	if (value == null) {
		return ''
	}

	return value
		.toString()
		.trim()
}



no modelo desse CT acima pega os testes antigos e mude somente a coluna e index de comparação e me mandepor aqui resumindo so o
// COMPARISON CONFIG



Détail - Caps Floors VI
import org.apache.poi.ss.usermodel.WorkbookFactory

import com.kms.katalon.core.configuration.RunConfiguration
import com.kms.katalon.core.util.KeywordUtil

import internal.GlobalVariable

String errors = '';
String msg = '';
def rows = GlobalVariable.compareRows as ArrayList;
def files = GlobalVariable.compareFiles as ArrayList;
String evPath = RunConfiguration.getReportFolder() + '\\Evidences\\';

if (rows.size() > 0) {
	if (rows.get(0) != rows.get(1)) errors+= '\n[ERROR] Number of rows (' + rows.get(0) + ') and (' + rows.get(1) + ') are different between the envs in Détail - Caps Floors VI.';
	else msg += '\nNumber of rows (' + rows.get(0) + ') and (' + rows.get(1) + ') are equal between the envs in Détail - Caps Floors VI.';
} else {
	//Getting files path
	String file1 = evPath + files.get(0) + '.xlsx';
	String file2 = evPath + files.get(1) + '.xlsx';
	
	//Reading excel files for comparison
	input1 = new FileInputStream(file1);
	input2 = new FileInputStream(file2);
	wbEnv1 = WorkbookFactory.create(input1);
	wbEnv2 = WorkbookFactory.create(input2);
	sheetEnv1 = wbEnv1.getSheetAt(0);
	sheetEnv2 = wbEnv2.getSheetAt(0);
	int linesEnv1 = sheetEnv1.getLastRowNum();
	int linesEnv2 = sheetEnv2.getLastRowNum();
	
	//Number of Lines
	if (linesEnv1 != linesEnv2) errors += '\n[ERROR] Number of rows (' + linesEnv1 + ') of file ' + files.get(0) + ' is different than (' + linesEnv2 + ') of file ' + files.get(1) + '.';
	else msg += '\nNumber of rows (' + linesEnv1 + ') of file ' + files.get(0) + ' is equal to (' + linesEnv2 + ') of file ' + files.get(1) + '.';
}

//Fails if any validation didn't pass
if (errors.length() > 1)
	KeywordUtil.markFailed(errors + msg);
else
	KeywordUtil.markPassed('\n[SUCCESS] The between ' + GlobalVariable.env + ' and PROD of Détail - Caps Floors VI finished successfully!\n' + msg);





Détail - CMS Tests
import org.apache.poi.ss.usermodel.WorkbookFactory

import com.kms.katalon.core.configuration.RunConfiguration
import com.kms.katalon.core.util.KeywordUtil

import internal.GlobalVariable

int index = 15; //15 = Valo Finale CCY
BigDecimal sum1, min1, max1, sum2, min2, max2;
String errors = '';
String msg = '';
def rows = GlobalVariable.compareRows as ArrayList;
def files = GlobalVariable.compareFiles as ArrayList;
String evPath = RunConfiguration.getReportFolder() + '\\Evidences\\';

if (rows.size() > 0) {
	if (rows.get(0) != rows.get(1)) errors+= '\n[ERROR] Number of rows (' + rows.get(0) + ') and (' + rows.get(1) + ') are different between the envs in Détail - CMS.';
	else msg += '\nNumber of rows (' + rows.get(0) + ') and (' + rows.get(1) + ') are equal between the envs in Détail - CMS.';
} else {
	//Getting files path
	String file1 = evPath + files.get(0) + '.xlsx';
	String file2 = evPath + files.get(1) + '.xlsx';
	
	//Reading excel files for comparison
	input1 = new FileInputStream(file1);
	input2 = new FileInputStream(file2);
	wbEnv1 = WorkbookFactory.create(input1);
	wbEnv2 = WorkbookFactory.create(input2);
	sheetEnv1 = wbEnv1.getSheetAt(0);
	sheetEnv2 = wbEnv2.getSheetAt(0);
	int linesEnv1 = sheetEnv1.getLastRowNum();
	int linesEnv2 = sheetEnv2.getLastRowNum();
	String col = sheetEnv1.getRow(0).getCell(index);
	
	//Number of Lines
	if (linesEnv1 != linesEnv2) errors += '\n[ERROR] Number of rows (' + linesEnv1 + ') of file ' + files.get(0) + ' is different than (' + linesEnv2 + ') of file ' + files.get(1) + '.';
	else msg += '\nNumber of rows (' + linesEnv1 + ') of file ' + files.get(0) + ' is equal to (' + linesEnv2 + ') of file ' + files.get(1) + '.';
	
	//Sum Validation
	sum1 = CustomKeywords.'com.katalon.keywords.excelHandling.ExcelValidations.totalSum'(sheetEnv1, index);
	sum2 = CustomKeywords.'com.katalon.keywords.excelHandling.ExcelValidations.totalSum'(sheetEnv2, index);
	sum1 = sum1.setScale(2, BigDecimal.ROUND_HALF_EVEN);
	sum2 = sum2.setScale(2, BigDecimal.ROUND_HALF_EVEN);
	
	if (sum1 != sum2) errors += '\n[ERROR] The sum of the amounts of REC (' + sum1 + ') and PROD (' + sum2 + ') are different.';
	else msg += '\nThe sum of the amounts of column ' + col + ' in ' + GlobalVariable.env + ' (' + sum1 + ') and PROD (' + sum2 + ') are equal.';
	
	//Min Validation
	min1 = CustomKeywords.'com.katalon.keywords.excelHandling.ExcelValidations.minAmount'(sheetEnv1, index);
	min2 = CustomKeywords.'com.katalon.keywords.excelHandling.ExcelValidations.minAmount'(sheetEnv2, index);
	min1 = min1.setScale(2,BigDecimal.ROUND_HALF_EVEN);
	min2= min2.setScale(2,BigDecimal.ROUND_HALF_EVEN);
	
	if (min1 != min2) errors += '\n[ERROR] The minimun amount of column ' + col + ' in ' + GlobalVariable.env + ' (' + min1 + ') and PROD (' + min2 + ') are different.';
	else msg += '\nThe minimun amount of column ' + col + ' in ' + GlobalVariable.env + ' (' + min1 + ') and PROD (' + min2 + ') are equal.';
	
	//Max Validation
	max1 = CustomKeywords.'com.katalon.keywords.excelHandling.ExcelValidations.maxAmount'(sheetEnv1, index);
	max2 = CustomKeywords.'com.katalon.keywords.excelHandling.ExcelValidations.maxAmount'(sheetEnv2, index);
	max1 = max1.setScale(2,BigDecimal.ROUND_HALF_EVEN);
	max2 = max2.setScale(2,BigDecimal.ROUND_HALF_EVEN);
	
	if (max1 != max2) errors += '\n[ERROR] The maximum amount of column ' + col + ' in ' + GlobalVariable.env + ' (' + max1 + ') and PROD (' + max2 + ') are different.';
	else msg += '\nThe maximum amount of the amount of column ' + col + ' in ' + GlobalVariable.env + ' (' + max1 + ') and PROD (' + max2 + ') are equal.';
}

//Fails if any validation didn't pass
if (errors.length() > 1)
	KeywordUtil.markFailed(errors + msg);
else
	KeywordUtil.markPassed('\n[SUCCESS] The between ' + GlobalVariable.env + ' and PROD of Détail - CMS finished successfully!\n' + msg);






Détail - Swaps Vanilles Tests
import org.apache.poi.ss.usermodel.WorkbookFactory

import com.kms.katalon.core.configuration.RunConfiguration
import com.kms.katalon.core.util.KeywordUtil

import internal.GlobalVariable


int index = 21; //21 = Notionel devise Ref
BigDecimal sum1, min1, max1, sum2, min2, max2;
String errors = '';
String msg = '';
def rows = GlobalVariable.compareRows as ArrayList;
def files = GlobalVariable.compareFiles as ArrayList;
String evPath = RunConfiguration.getReportFolder() + '\\Evidences\\';

if (rows.size() > 0) {
	if (rows.get(0) != rows.get(1)) errors+= '\n[ERROR] Number of rows (' + rows.get(0) + ') and (' + rows.get(1) + ') are different between the envs in Détail - Swaps Vanilles.';
	else msg += '\nNumber of rows (' + rows.get(0) + ') and (' + rows.get(1) + ') are equal between the envs in Détail - Swaps Vanilles.';
} else {
	//Getting files path
	String file1 = evPath + files.get(0) + '.xlsx';
	String file2 = evPath + files.get(1) + '.xlsx';
	
	//file1 = 'C:\\Users\\netoos\\Katalon Studio\\ematch-nr-tests\\Reports\\20220504_151500\\JV Emissions Suite\\Emission Filtre - Collection\\Emission Filtre\\20220504_151500\\Evidences\\DEV2_20220504_Emission_Filtree.xlsx';
	//file2 = 'C:\\Users\\netoos\\Katalon Studio\\ematch-nr-tests\\Reports\\20220504_151500\\JV Emissions Suite\\Emission Filtre - Collection\\Emission Filtre\\20220504_151500\\Evidences\\PROD_20220504_Emission_Filtree.xlsx';
	
	//Reading excel files for comparison
	input1 = new FileInputStream(file1);
	input2 = new FileInputStream(file2);
	wbEnv1 = WorkbookFactory.create(input1);
	wbEnv2 = WorkbookFactory.create(input2);
	sheetEnv1 = wbEnv1.getSheetAt(0);
	sheetEnv2 = wbEnv2.getSheetAt(0);
	int linesEnv1 = sheetEnv1.getLastRowNum();
	int linesEnv2 = sheetEnv2.getLastRowNum();
	String col = sheetEnv1.getRow(0).getCell(index);
	
	//Number of Lines
	if (linesEnv1 != linesEnv2) errors += '\n[ERROR] Number of rows (' + linesEnv1 + ') of file ' + files.get(0) + ' is different than (' + linesEnv2 + ') of file ' + files.get(1) + '.';
	else msg += '\nNumber of rows (' + linesEnv1 + ') of file ' + files.get(0) + ' is equal to (' + linesEnv2 + ') of file ' + files.get(1) + '.';
	
	//Sum Validation
	sum1 = CustomKeywords.'com.katalon.keywords.excelHandling.ExcelValidations.totalSum'(sheetEnv1, index);
	sum2 = CustomKeywords.'com.katalon.keywords.excelHandling.ExcelValidations.totalSum'(sheetEnv2, index);
	sum1 = sum1.setScale(2, BigDecimal.ROUND_HALF_EVEN);
	sum2 = sum2.setScale(2, BigDecimal.ROUND_HALF_EVEN);
	
	if (sum1 != sum2) errors += '\n[ERROR] The sum of the amounts of REC (' + sum1 + ') and PROD (' + sum2 + ') are different.';
	else msg += '\nThe sum of the amounts of column ' + col + ' in ' + GlobalVariable.env + ' (' + sum1 + ') and PROD (' + sum2 + ') are equal.';
	
	//Min Validation
	min1 = CustomKeywords.'com.katalon.keywords.excelHandling.ExcelValidations.minAmount'(sheetEnv1, index);
	min2 = CustomKeywords.'com.katalon.keywords.excelHandling.ExcelValidations.minAmount'(sheetEnv2, index);
	min1 = min1.setScale(2,BigDecimal.ROUND_HALF_EVEN);
	min2= min2.setScale(2,BigDecimal.ROUND_HALF_EVEN);
	
	if (min1 != min2) errors += '\n[ERROR] The minimun amount of column ' + col + ' in ' + GlobalVariable.env + ' (' + min1 + ') and PROD (' + min2 + ') are different.';
	else msg += '\nThe minimun amount of column ' + col + ' in ' + GlobalVariable.env + ' (' + min1 + ') and PROD (' + min2 + ') are equal.';
	
	//Max Validation
	max1 = CustomKeywords.'com.katalon.keywords.excelHandling.ExcelValidations.maxAmount'(sheetEnv1, index);
	max2 = CustomKeywords.'com.katalon.keywords.excelHandling.ExcelValidations.maxAmount'(sheetEnv2, index);
	max1 = max1.setScale(2,BigDecimal.ROUND_HALF_EVEN);
	max2 = max2.setScale(2,BigDecimal.ROUND_HALF_EVEN);
	
	if (max1 != max2) errors += '\n[ERROR] The maximum amount of column ' + col + ' in ' + GlobalVariable.env + ' (' + max1 + ') and PROD (' + max2 + ') are different.';
	else msg += '\nThe maximum amount of the amount of column ' + col + ' in ' + GlobalVariable.env + ' (' + max1 + ') and PROD (' + max2 + ') are equal.';
}

//Fails if any validation didn't pass
if (errors.length() > 1)
	KeywordUtil.markFailed(errors + msg);
else
	KeywordUtil.markPassed('\n[SUCCESS] The between ' + GlobalVariable.env + ' and PROD of Détail - Swaps Vanilles finished successfully!\n' + msg);





Tests - Caps Grecques Tests
import org.apache.poi.ss.usermodel.WorkbookFactory

import com.kms.katalon.core.configuration.RunConfiguration
import com.kms.katalon.core.util.KeywordUtil

import internal.GlobalVariable

String errors = '';
String msg = '';
def rows = GlobalVariable.compareRows as ArrayList;
def files = GlobalVariable.compareFiles as ArrayList;
String evPath = RunConfiguration.getReportFolder() + '\\Evidences\\';

if (rows.size() > 0) {
	if (rows.get(0) != rows.get(1)) errors+= '\n[ERROR] Number of rows (' + rows.get(0) + ') and (' + rows.get(1) + ') are different between the envs in Tests - Caps Floors Grecques.';
	else msg += '\nNumber of rows (' + rows.get(0) + ') and (' + rows.get(1) + ') are equal between the envs in Tests - Caps Floors Grecques.';
} else {
	//Getting files path
	String file1 = evPath + files.get(0)
	String file2 = evPath + files.get(1)
	
	//Reading excel files for comparison
	input1 = new FileInputStream(file1);
	input2 = new FileInputStream(file2);
	wbEnv1 = WorkbookFactory.create(input1);
	wbEnv2 = WorkbookFactory.create(input2);
	sheetEnv1 = wbEnv1.getSheetAt(0);
	sheetEnv2 = wbEnv2.getSheetAt(0);
	int linesEnv1 = sheetEnv1.getLastRowNum();
	int linesEnv2 = sheetEnv2.getLastRowNum();
	
	//Number of Lines
	if (linesEnv1 != linesEnv2) errors += '\n[ERROR] Number of rows (' + linesEnv1 + ') of file ' + files.get(0) + ' is different than (' + linesEnv2 + ') of file ' + files.get(1) + '.';
	else msg += '\nNumber of rows (' + linesEnv1 + ') of file ' + files.get(0) + ' is equal to (' + linesEnv2 + ') of file ' + files.get(1) + '.';
}

//Fails if any validation didn't pass
if (errors.length() > 1)
	KeywordUtil.markFailed(errors + msg);
else
	KeywordUtil.markPassed('\n[SUCCESS] The between ' + GlobalVariable.env + ' and PROD of Tests - Caps Floors Grecques finished successfully!\n' + msg);






Tests - Caps VI Tests
import org.apache.poi.ss.usermodel.WorkbookFactory

import com.kms.katalon.core.configuration.RunConfiguration
import com.kms.katalon.core.util.KeywordUtil

import internal.GlobalVariable

String errors = '';
String msg = '';
def rows = GlobalVariable.compareRows as ArrayList;
def files = GlobalVariable.compareFiles as ArrayList;
String evPath = RunConfiguration.getReportFolder() + '\\Evidences\\';

if (rows.size() > 0) {
	if (rows.get(0) != rows.get(1)) errors+= '\n[ERROR] Number of rows (' + rows.get(0) + ') and (' + rows.get(1) + ') are different between the envs in Tests - Caps Floors VI.';
	else msg += '\nNumber of rows (' + rows.get(0) + ') and (' + rows.get(1) + ') are equal between the envs in Tests - Caps Floors VI.';
} else {
	//Getting files path
	String file1 = evPath + files.get(0) 
	String file2 = evPath + files.get(1) 
	
	//Reading excel files for comparison
	input1 = new FileInputStream(file1);
	input2 = new FileInputStream(file2);
	wbEnv1 = WorkbookFactory.create(input1);
	wbEnv2 = WorkbookFactory.create(input2);
	sheetEnv1 = wbEnv1.getSheetAt(0);
	sheetEnv2 = wbEnv2.getSheetAt(0);
	int linesEnv1 = sheetEnv1.getLastRowNum();
	int linesEnv2 = sheetEnv2.getLastRowNum();
	
	//Number of Lines
	if (linesEnv1 != linesEnv2) errors += '\n[ERROR] Number of rows (' + linesEnv1 + ') of file ' + files.get(0) + ' is different than (' + linesEnv2 + ') of file ' + files.get(1) + '.';
	else msg += '\nNumber of rows (' + linesEnv1 + ') of file ' + files.get(0) + ' is equal to (' + linesEnv2 + ') of file ' + files.get(1) + '.';
}

//Fails if any validation didn't pass
if (errors.length() > 1)
	KeywordUtil.markFailed(errors + msg);
else
	KeywordUtil.markPassed('\n[SUCCESS] The between ' + GlobalVariable.env + ' and PROD of Tests - Caps Floors VI finished successfully!\n' + msg);





Tests - CMS Tests
import org.apache.poi.ss.usermodel.WorkbookFactory

import com.kms.katalon.core.configuration.RunConfiguration
import com.kms.katalon.core.util.KeywordUtil

import internal.GlobalVariable

String errors = '';
String msg = '';
def rows = GlobalVariable.compareRows as ArrayList;
def files = GlobalVariable.compareFiles as ArrayList;
String evPath = RunConfiguration.getReportFolder() + '\\Evidences\\';

if (rows.size() > 0) {
	if (rows.get(0) != rows.get(1)) errors+= '\n[ERROR] Number of rows (' + rows.get(0) + ') and (' + rows.get(1) + ') are different between the envs in Tests - CMS.';
	else msg += '\nNumber of rows (' + rows.get(0) + ') and (' + rows.get(1) + ') are equal between the envs in Tests - CMS.';
} else {
	//Getting files path
	String file1 = evPath + files.get(0)
	String file2 = evPath + files.get(1)
	
	//Reading excel files for comparison
	input1 = new FileInputStream(file1);
	input2 = new FileInputStream(file2);
	wbEnv1 = WorkbookFactory.create(input1);
	wbEnv2 = WorkbookFactory.create(input2);
	sheetEnv1 = wbEnv1.getSheetAt(0);
	sheetEnv2 = wbEnv2.getSheetAt(0);
	int linesEnv1 = sheetEnv1.getLastRowNum();
	int linesEnv2 = sheetEnv2.getLastRowNum();
	
	//Number of Lines
	if (linesEnv1 != linesEnv2) errors += '\n[ERROR] Number of rows (' + linesEnv1 + ') of file ' + files.get(0) + ' is different than (' + linesEnv2 + ') of file ' + files.get(1) + '.';
	else msg += '\nNumber of rows (' + linesEnv1 + ') of file ' + files.get(0) + ' is equal to (' + linesEnv2 + ') of file ' + files.get(1) + '.';
}

//Fails if any validation didn't pass
if (errors.length() > 1)
	KeywordUtil.markFailed(errors + msg);
else
	KeywordUtil.markPassed('\n[SUCCESS] The between ' + GlobalVariable.env + ' and PROD of Tests - CMS finished successfully!\n' + msg);









Tests - Swaps Vanilles Tests
import org.apache.poi.ss.usermodel.WorkbookFactory

import com.kms.katalon.core.configuration.RunConfiguration
import com.kms.katalon.core.util.KeywordUtil

import internal.GlobalVariable


int index = 28; //28 = Valo Finale CCY
BigDecimal sum1, min1, max1, sum2, min2, max2;
String errors = '';
String msg = '';
def rows = GlobalVariable.compareRows as ArrayList;
def files = GlobalVariable.compareFiles as ArrayList;
String evPath = RunConfiguration.getReportFolder() + '\\Evidences\\';

if (rows.size() > 0) {
	if (rows.get(0) != rows.get(1)) errors+= '\n[ERROR] Number of rows (' + rows.get(0) + ') and (' + rows.get(1) + ') are different between the envs in Tests - Swaps Vanilles.';
	else msg += '\nNumber of rows (' + rows.get(0) + ') and (' + rows.get(1) + ') are equal between the envs in Tests - Swaps Vanilles.';
} else {
	//Getting files path
	String file1 = evPath + files.get(0) 
	String file2 = evPath + files.get(1) 
	
	//Reading excel files for comparison
	input1 = new FileInputStream(file1);
	input2 = new FileInputStream(file2);
	wbEnv1 = WorkbookFactory.create(input1);
	wbEnv2 = WorkbookFactory.create(input2);
	sheetEnv1 = wbEnv1.getSheetAt(0);
	sheetEnv2 = wbEnv2.getSheetAt(0);
	int linesEnv1 = sheetEnv1.getLastRowNum();
	int linesEnv2 = sheetEnv2.getLastRowNum();
	String col = sheetEnv1.getRow(0).getCell(index);
	
	//Number of Lines
	if (linesEnv1 != linesEnv2) errors += '\n[ERROR] Number of rows (' + linesEnv1 + ') of file ' + files.get(0) + ' is different than (' + linesEnv2 + ') of file ' + files.get(1) + '.';
	else msg += '\nNumber of rows (' + linesEnv1 + ') of file ' + files.get(0) + ' is equal to (' + linesEnv2 + ') of file ' + files.get(1) + '.';
	
	//Sum Validation
	sum1 = CustomKeywords.'com.katalon.keywords.excelHandling.ExcelValidations.totalSum'(sheetEnv1, index);
	sum2 = CustomKeywords.'com.katalon.keywords.excelHandling.ExcelValidations.totalSum'(sheetEnv2, index);
	sum1 = sum1.setScale(2, BigDecimal.ROUND_HALF_EVEN);
	sum2 = sum2.setScale(2, BigDecimal.ROUND_HALF_EVEN);
	
	if (sum1 != sum2) errors += '\n[ERROR] The sum of the amounts of REC (' + sum1 + ') and PROD (' + sum2 + ') are different.';
	else msg += '\nThe sum of the amounts of column ' + col + ' in ' + GlobalVariable.env + ' (' + sum1 + ') and PROD (' + sum2 + ') are equal.';
	
	//Min Validation
	min1 = CustomKeywords.'com.katalon.keywords.excelHandling.ExcelValidations.minAmount'(sheetEnv1, index);
	min2 = CustomKeywords.'com.katalon.keywords.excelHandling.ExcelValidations.minAmount'(sheetEnv2, index);
	min1 = min1.setScale(2,BigDecimal.ROUND_HALF_EVEN);
	min2= min2.setScale(2,BigDecimal.ROUND_HALF_EVEN);
	
	if (min1 != min2) errors += '\n[ERROR] The minimun amount of column ' + col + ' in ' + GlobalVariable.env + ' (' + min1 + ') and PROD (' + min2 + ') are different.';
	else msg += '\nThe minimun amount of column ' + col + ' in ' + GlobalVariable.env + ' (' + min1 + ') and PROD (' + min2 + ') are equal.';
	
	//Max Validation
	max1 = CustomKeywords.'com.katalon.keywords.excelHandling.ExcelValidations.maxAmount'(sheetEnv1, index);
	max2 = CustomKeywords.'com.katalon.keywords.excelHandling.ExcelValidations.maxAmount'(sheetEnv2, index);
	max1 = max1.setScale(2,BigDecimal.ROUND_HALF_EVEN);
	max2 = max2.setScale(2,BigDecimal.ROUND_HALF_EVEN);
	
	if (max1 != max2) errors += '\n[ERROR] The maximum amount of column ' + col + ' in ' + GlobalVariable.env + ' (' + max1 + ') and PROD (' + max2 + ') are different.';
	else msg += '\nThe maximum amount of the amount of column ' + col + ' in ' + GlobalVariable.env + ' (' + max1 + ') and PROD (' + max2 + ') are equal.';
}

//Fails if any validation didn't pass
if (errors.length() > 1)
	KeywordUtil.markFailed(errors + msg);
else
	KeywordUtil.markPassed('\n[SUCCESS] The between ' + GlobalVariable.env + ' and PROD of Tests - Swaps Vanilles finished successfully!\n' + msg);







Exclusion Books Tests
import org.apache.poi.ss.usermodel.WorkbookFactory

import com.kms.katalon.core.configuration.RunConfiguration
import com.kms.katalon.core.util.KeywordUtil

import internal.GlobalVariable

String errors = '';
String msg = '';
def rows = GlobalVariable.compareRows as ArrayList;
def files = GlobalVariable.compareFiles as ArrayList;
String evPath = RunConfiguration.getReportFolder() + '\\Evidences\\';

if (rows.size() > 0) {
	if (rows.get(0) != rows.get(1)) errors+= '\n[ERROR] Number of rows (' + rows.get(0) + ') and (' + rows.get(1) + ') are different between the envs in Exclusion books.';
	else msg += '\nNumber of rows (' + rows.get(0) + ') and (' + rows.get(1) + ') are equal between the envs in Exclusion books.';
} else {
	//Getting files path
	String file1 = evPath + files.get(0) + '.xlsx';
	String file2 = evPath + files.get(1) + '.xlsx';
	
	//Reading excel files for comparison
	input1 = new FileInputStream(file1);
	input2 = new FileInputStream(file2);
	wbEnv1 = WorkbookFactory.create(input1);
	wbEnv2 = WorkbookFactory.create(input2);
	sheetEnv1 = wbEnv1.getSheetAt(0);
	sheetEnv2 = wbEnv2.getSheetAt(0);
	int linesEnv1 = sheetEnv1.getLastRowNum();
	int linesEnv2 = sheetEnv2.getLastRowNum();
	
	//Number of Lines
	if (linesEnv1 != linesEnv2) errors += '\n[ERROR] Number of rows (' + linesEnv1 + ') of file ' + files.get(0) + ' is different than (' + linesEnv2 + ') of file ' + files.get(1) + '.';
	else msg += '\nNumber of rows (' + linesEnv1 + ') of file ' + files.get(0) + ' is equal to (' + linesEnv2 + ') of file ' + files.get(1) + '.';
}

//Fails if any validation didn't pass
if (errors.length() > 1)
	KeywordUtil.markFailed(errors + msg);
else
	KeywordUtil.markPassed('\n[SUCCESS] The between ' + GlobalVariable.env + ' and PROD of Exclusion books finished successfully!\n' + msg);





Paramétrage Tests
import org.apache.poi.ss.usermodel.WorkbookFactory

import com.kms.katalon.core.configuration.RunConfiguration
import com.kms.katalon.core.util.KeywordUtil

import internal.GlobalVariable

String errors = '';
String msg = '';
def rows = GlobalVariable.compareRows as ArrayList;
def files = GlobalVariable.compareFiles as ArrayList;
String evPath = RunConfiguration.getReportFolder() + '\\Evidences\\';

if (rows.size() > 0) {
	if (rows.get(0) != rows.get(1)) errors+= '\n[ERROR] Number of rows (' + rows.get(0) + ') and (' + rows.get(1) + ') are different between the envs in Paramétrage.';
	else msg += '\nNumber of rows (' + rows.get(0) + ') and (' + rows.get(1) + ') are equal between the envs in Paramétrage.';
} else {
	//Getting files path
	String file1 = evPath + files.get(0) + '.xlsx';
	String file2 = evPath + files.get(1) + '.xlsx';
	
	//Reading excel files for comparison
	input1 = new FileInputStream(file1);
	input2 = new FileInputStream(file2);
	wbEnv1 = WorkbookFactory.create(input1);
	wbEnv2 = WorkbookFactory.create(input2);
	sheetEnv1 = wbEnv1.getSheetAt(0);
	sheetEnv2 = wbEnv2.getSheetAt(0);
	int linesEnv1 = sheetEnv1.getLastRowNum();
	int linesEnv2 = sheetEnv2.getLastRowNum();
	
	//Number of Lines
	if (linesEnv1 != linesEnv2) errors += '\n[ERROR] Number of rows (' + linesEnv1 + ') of file ' + files.get(0) + ' is different than (' + linesEnv2 + ') of file ' + files.get(1) + '.';
	else msg += '\nNumber of rows (' + linesEnv1 + ') of file ' + files.get(0) + ' is equal to (' + linesEnv2 + ') of file ' + files.get(1) + '.';
}

//Fails if any validation didn't pass
if (errors.length() > 1)
	KeywordUtil.markFailed(errors + msg);
else
	KeywordUtil.markPassed('\n[SUCCESS] The between ' + GlobalVariable.env + ' and PROD of Paramétrage finished successfully!\n' + msg);






Rapprochement OTR Tests
import org.apache.poi.ss.usermodel.WorkbookFactory

import com.kms.katalon.core.configuration.RunConfiguration
import com.kms.katalon.core.util.KeywordUtil

import internal.GlobalVariable

String errors = '';
String msg = '';
def rows = GlobalVariable.compareRows as ArrayList;
def files = GlobalVariable.compareFiles as ArrayList;
String evPath = RunConfiguration.getReportFolder() + '\\Evidences\\';

if (rows.size() > 0) {
	if (rows.get(0) != rows.get(1)) errors+= '\n[ERROR] Number of rows (' + rows.get(0) + ') and (' + rows.get(1) + ') are different between the envs in Rapprochement OTR.';
	else msg += '\nNumber of rows (' + rows.get(0) + ') and (' + rows.get(1) + ') are equal between the envs in Rapprochement OTR.';
} else {
	//Getting files path
	String file1 = evPath + files.get(0) + '.xlsx';
	String file2 = evPath + files.get(1) + '.xlsx';
	
	//Reading excel files for comparison
	input1 = new FileInputStream(file1);
	input2 = new FileInputStream(file2);
	wbEnv1 = WorkbookFactory.create(input1);
	wbEnv2 = WorkbookFactory.create(input2);
	sheetEnv1 = wbEnv1.getSheetAt(0);
	sheetEnv2 = wbEnv2.getSheetAt(0);
	int linesEnv1 = sheetEnv1.getLastRowNum();
	int linesEnv2 = sheetEnv2.getLastRowNum();
	
	//Number of Lines
	if (linesEnv1 != linesEnv2) errors += '\n[ERROR] Number of rows (' + linesEnv1 + ') of file ' + files.get(0) + ' is different than (' + linesEnv2 + ') of file ' + files.get(1) + '.';
	else msg += '\nNumber of rows (' + linesEnv1 + ') of file ' + files.get(0) + ' is equal to (' + linesEnv2 + ') of file ' + files.get(1) + '.';
}

//Fails if any validation didn't pass
if (errors.length() > 1)
	KeywordUtil.markFailed(errors + msg);
else
	KeywordUtil.markPassed('\n[SUCCESS] The between ' + GlobalVariable.env + ' and PROD of Rapprochement OTR finished successfully!\n' + msg);
