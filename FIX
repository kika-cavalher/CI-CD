@echo off

set ENV=%1

echo =====================================
echo STARTING EMATCH - ENV: %ENV%
echo =====================================

taskkill /F /IM IXIS.Ematch.IHM.exe >nul 2>&1
timeout /t 3 >nul

IF "%ENV%"=="REC" (
    start "" "D:\RunAseMatchReq\eMatch_REC.lnk"
)

IF "%ENV%"=="PRD" (
    start "" "D:\RunAseMatchReq\eMatch_PRD.lnk"
)

timeout /t 10 >nul

exit /b 0
