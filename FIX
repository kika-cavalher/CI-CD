REC - Favorites - Validate Create Open Delete.groovy

import static com.kms.katalon.core.testcase.TestCaseFactory.findTestCase

import com.kms.katalon.core.model.FailureHandling
import com.kms.katalon.core.util.KeywordUtil
import com.kms.katalon.core.webui.keyword.WebUiBuiltInKeywords as WebUI

import internal.GlobalVariable


// ======================================================
// MODULE CONFIG
// ======================================================

String targetEnvironment = 'REC'
String language = 'fr'

String favoriteTitle = 'Katalon Fav Test'
String favoriteImage = 'Images/GUI behavior/KatalonCreatedFav.PNG'
String filterValue = '10E'

String configurationMenuName = language.equalsIgnoreCase('fr')
	? 'Liste des périmètres'
	: 'List of perimeters'

String configurationMenuImage = language.equalsIgnoreCase('fr')
	? 'Images/GUI behavior/listePerimetresMenu.PNG'
	: 'Images/GUI behavior/listePerimetresMenuEN.PNG'

String configurationFormObject =
	'GUI behavior/listeDesPerimetresFormulaire'

String loginTestCase =
	'Component/Authentication/Login ' + targetEnvironment

String closeApplicationTestCase =
	'Component/Authentication/Close Application'


// ======================================================
// TIMEOUT CONFIG
// ======================================================

Integer landingPageTimeout = 6
Integer configurationTimeout = 60
Integer filterLoadDelay = 25
Integer favoriteControlTimeout = 4
Integer favoritePopupTimeout = 30
Integer favoriteOpenTimeout = 40


// ======================================================
// AUTHENTICATION
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Authenticate and open application\n' +
	'Environment: ' + targetEnvironment
)

WebUI.callTestCase(
	findTestCase(loginTestCase),
	[:],
	FailureHandling.STOP_ON_FAILURE
)


// ======================================================
// LANDING PAGE VALIDATION
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Validate GUI landing page\n' +
	'- Bottom bar information\n' +
	'- User profiles image\n' +
	'- Calendar controls\n' +
	'- Top bar layout and enabled states'
)

CustomKeywords.'pageObject.guiValidation.validateLandingPage'(
	landingPageTimeout
)

CustomKeywords.'commands.ImageLoader.takeEvidence'(
	targetEnvironment + '_GUI_LandingPage.png'
)


// ======================================================
// OPEN CONFIGURATION SCREEN
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Open configuration screen\n' +
	'Menu: ' + configurationMenuName + '\n' +
	'Language: ' + language
)

CustomKeywords.'pageObject.guiValidation.openConfigurationMenu'(
	configurationMenuName,
	configurationMenuImage,
	configurationFormObject,
	configurationTimeout
)

CustomKeywords.'commands.ImageLoader.takeEvidence'(
	targetEnvironment + '_GUI_ListePerimetres_Open.png'
)


// ======================================================
// APPLY FILTER
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Apply section filter before creating favorite\n' +
	'Filter value: ' + filterValue
)

CustomKeywords.'pageObject.guiValidation.applySectionFilter'(
	filterValue,
	language,
	filterLoadDelay
)

CustomKeywords.'commands.ImageLoader.takeEvidence'(
	targetEnvironment + '_GUI_FilteredDataBeforeFavorite.png'
)


// ======================================================
// DOWNLOAD ORIGINAL VIEW
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Download original filtered view\n' +
	'Purpose: create the reference file for favorite comparison'
)

CustomKeywords.'pageObject.guiValidation.resetDownloadedFiles'()

CustomKeywords.'pageObject.guiValidation.downloadCurrentView'('')

CustomKeywords.'commands.ImageLoader.takeEvidence'(
	targetEnvironment + '_GUI_OriginalViewDownloaded.png'
)


// ======================================================
// CREATE FAVORITE
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Create favorite\n' +
	'Title: ' + favoriteTitle + '\n' +
	'Validate controls before and after filling the title'
)

CustomKeywords.'pageObject.guiValidation.startFavoriteCreation'(
	favoriteControlTimeout
)

CustomKeywords.'pageObject.guiValidation.enterFavoriteTitle'(
	favoriteTitle,
	favoriteControlTimeout
)

CustomKeywords.'pageObject.guiValidation.saveFavorite'(
	favoritePopupTimeout
)

CustomKeywords.'commands.ImageLoader.takeEvidence'(
	targetEnvironment + '_GUI_FavoriteCreatedPopup.png'
)

CustomKeywords.'pageObject.guiValidation.confirmFavoriteCreation'()


// ======================================================
// OPEN FAVORITE
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Close current tab and open created favorite\n' +
	'Favorite: ' + favoriteTitle
)

CustomKeywords.'pageObject.guiValidation.closeAllOpenedTabs'()

CustomKeywords.'pageObject.guiValidation.openCreatedFavorite'(
	favoriteImage,
	favoriteOpenTimeout
)

CustomKeywords.'commands.ImageLoader.takeEvidence'(
	targetEnvironment + '_GUI_FavoriteOpened.png'
)


// ======================================================
// DOWNLOAD FAVORITE VIEW
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Download favorite view\n' +
	'Purpose: compare favorite data with the original filtered view'
)

CustomKeywords.'pageObject.guiValidation.downloadCurrentView'(
	'favorite File'
)

CustomKeywords.'commands.ImageLoader.takeEvidence'(
	targetEnvironment + '_GUI_FavoriteViewDownloaded.png'
)


// ======================================================
// COMPARE FILES
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Compare original and favorite files\n' +
	'Expected result: both exported files contain the same data'
)

CustomKeywords.'pageObject.guiValidation.compareDownloadedFiles'()


// ======================================================
// DELETE FAVORITE
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Delete created favorite\n' +
	'Favorite: ' + favoriteTitle + '\n' +
	'Expected result: favorite is no longer displayed'
)

CustomKeywords.'commands.ImageLoader.takeEvidence'(
	targetEnvironment + '_GUI_FavoriteBeforeDelete.png'
)

CustomKeywords.'pageObject.guiValidation.deleteCreatedFavorite'(
	favoriteImage
)

CustomKeywords.'commands.ImageLoader.takeEvidence'(
	targetEnvironment + '_GUI_FavoriteAfterDelete.png'
)


// ======================================================
// CLOSE APPLICATION
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Close application\n' +
	'Environment: ' + targetEnvironment
)

WebUI.callTestCase(
	findTestCase(closeApplicationTestCase),
	[:],
	FailureHandling.STOP_ON_FAILURE
)

KeywordUtil.markPassed(
	targetEnvironment +
	' favorite create, open, compare and delete flow completed successfully.'
)























REC - Group By - Validate Hierarchy.groovy

import static com.kms.katalon.core.testcase.TestCaseFactory.findTestCase

import com.kms.katalon.core.model.FailureHandling
import com.kms.katalon.core.util.KeywordUtil
import com.kms.katalon.core.webui.keyword.WebUiBuiltInKeywords as WebUI


// ======================================================
// MODULE CONFIG
// ======================================================

String targetEnvironment = 'REC'

String menuName = 'synthèse Justification Latent'
String menuResultName = 'Synthèse Justification Latent'
String openedTabName = menuResultName

String firstGroupSourceImage =
	'Images/GUI behavior/grappeHeader.PNG'

String emptyGroupTargetImage =
	'Images/GUI behavior/emptyGroupSection.PNG'

String secondGroupSourceImage =
	'Images/GUI behavior/ecartBilanVSCompteResultatCCY.PNG'

String filledGroupTargetImage =
	'Images/GUI behavior/filledGroupSection.PNG'

String loginTestCase =
	'Component/Authentication/Login ' + targetEnvironment

String closeApplicationTestCase =
	'Component/Authentication/Close Application'


// ======================================================
// TIMEOUT CONFIG
// ======================================================

Integer menuTimeout = 60
Integer openedTabTimeout = 60
Integer groupByTimeout = 10
Integer detailsTimeout = 10
Integer dragAndDropDelay = 3


// ======================================================
// AUTHENTICATION
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Authenticate and open application\n' +
	'Environment: ' + targetEnvironment
)

WebUI.callTestCase(
	findTestCase(loginTestCase),
	[:],
	FailureHandling.STOP_ON_FAILURE
)


// ======================================================
// MENU NAVIGATION
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Open target module\n' +
	'Search: ' + menuName + '\n' +
	'Result: ' + menuResultName
)

CustomKeywords.'component.MenuNavigator.searchAndOpenMenuByText'(
	menuName,
	menuResultName,
	menuTimeout
)

CustomKeywords.'pageObject.guiValidation.validateOpenedTab'(
	openedTabName,
	openedTabTimeout
)

CustomKeywords.'commands.ImageLoader.takeEvidence'(
	targetEnvironment + '_GUI_GroupBy_OpenModule.png'
)


// ======================================================
// FIRST GROUP BY LEVEL
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Create first Group By level\n' +
	'Field: Grappe\n' +
	'Expected format: Grappe:INTEGER greater than zero'
)

CustomKeywords.'pageObject.guiValidation.dragAndDropImage'(
	firstGroupSourceImage,
	emptyGroupTargetImage,
	dragAndDropDelay
)

CustomKeywords.'commands.ImageLoader.takeEvidence'(
	targetEnvironment + '_GUI_GroupBy_Grappe.png'
)

// ======================================================
// SECOND GROUP BY LEVEL
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Create second Group By level\n' +
	'Field: Ecart bilan vs. compte de résultat ccy\n' +
	'Expected format: label:NUMBER'
)

CustomKeywords.'pageObject.guiValidation.dragAndDropImage'(
	secondGroupSourceImage,
	filledGroupTargetImage,
	dragAndDropDelay
)

CustomKeywords.'commands.ImageLoader.takeEvidence'(
	targetEnvironment + '_GUI_GroupBy_GrappeAndEcart.png'
)


// ======================================================
// GROUP BY HIERARCHY VALIDATION
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Validate Group By hierarchy\n' +
	'- Validate first level format\n' +
	'- Open first level\n' +
	'- Validate second level format\n' +
	'- Open second level'
)

CustomKeywords.'pageObject.guiValidation.validateFirstLevelGroupBy'(
	groupByTimeout
)

CustomKeywords.'pageObject.guiValidation.openFirstLevelGroupBy'(
	groupByTimeout
)

CustomKeywords.'pageObject.guiValidation.validateSecondLevelGroupBy'(
	groupByTimeout
)

CustomKeywords.'pageObject.guiValidation.openSecondLevelGroupBy'(
	groupByTimeout
)

CustomKeywords.'commands.ImageLoader.takeEvidence'(
	targetEnvironment + '_GUI_GroupBy_SecondLevelOpen.png'
)


// ======================================================
// DETAILS VALIDATION
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Validate Group By details\n' +
	'Expected result: details rows are displayed after opening level 2'
)

CustomKeywords.'pageObject.guiValidation.validateGroupByDetails'(
	detailsTimeout
)

CustomKeywords.'commands.ImageLoader.takeEvidence'(
	targetEnvironment + '_GUI_GroupBy_DetailsOpen.png'
)


// ======================================================
// CLOSE APPLICATION
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Close application\n' +
	'Environment: ' + targetEnvironment
)

WebUI.callTestCase(
	findTestCase(closeApplicationTestCase),
	[:],
	FailureHandling.STOP_ON_FAILURE
)

KeywordUtil.markPassed(
	targetEnvironment +
	' Group By hierarchy validation completed successfully.'
)























REC - Language - Validate French Selection.groovy

import static com.kms.katalon.core.testcase.TestCaseFactory.findTestCase

import com.kms.katalon.core.model.FailureHandling
import com.kms.katalon.core.util.KeywordUtil
import com.kms.katalon.core.webui.keyword.WebUiBuiltInKeywords as WebUI


// ======================================================
// MODULE CONFIG
// ======================================================

String targetEnvironment = 'REC'
String expectedLanguage = 'French'

String loginTestCase =
	'Component/Authentication/Login ' + targetEnvironment

String closeApplicationTestCase =
	'Component/Authentication/Close Application'


// ======================================================
// AUTHENTICATION
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Authenticate and open application\n' +
	'Environment: ' + targetEnvironment
)

WebUI.callTestCase(
	findTestCase(loginTestCase),
	[:],
	FailureHandling.STOP_ON_FAILURE
)


// ======================================================
// LANGUAGE VALIDATION
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Change application language\n' +
	'Expected language: ' + expectedLanguage + '\n' +
	'Validate selected and deselected language states'
)

CustomKeywords.'pageObject.guiValidation.changeLanguageToFrench'()

CustomKeywords.'commands.ImageLoader.takeEvidence'(
	targetEnvironment + '_GUI_LanguageSelectedFrench.png'
)


// ======================================================
// CLOSE APPLICATION
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Close application\n' +
	'Environment: ' + targetEnvironment
)

WebUI.callTestCase(
	findTestCase(closeApplicationTestCase),
	[:],
	FailureHandling.STOP_ON_FAILURE
)

KeywordUtil.markPassed(
	targetEnvironment +
	' language validation completed successfully for ' +
	expectedLanguage + '.'
)

