schtasks /run /tn "\eMatch Automation\RunEMatchDesktopREC"





schtasks /run /tn "\eMatch Automation\RunEMatchDesktopPRD"




@echo off
setlocal

runas /user:"cib\cavalheren" /savecred "C:\Windows\Microsoft.NET\Framework\v4.0.30319\dfsvc.exe"
timeout /t 2 /nobreak >nul
runas /user:"cib\cavalheren" /savecred "rundll32 C:\Windows\System32\dfshim.dll,ShOpenVerbApplication \\cib.net\shareparis\Soft\ematch\IHM\Livraisons\REC\IXIS.eMatch.IHM.application"

exit /b %errorlevel%








@echo off
setlocal

runas /user:"cib\cavalheren" /savecred "C:\Windows\Microsoft.NET\Framework\v4.0.30319\dfsvc.exe"
timeout /t 2 /nobreak >nul
runas /user:"cib\cavalheren" /savecred "rundll32 C:\Windows\System32\dfshim.dll,ShOpenVerbApplication \\cib.net\shareparis\Soft\ematch\IHM\PROD\IXIS.eMatch.IHM.application"

exit /b %errorlevel%






@echo off
setlocal EnableExtensions

set "TARGET_ENV=%~1"
if not defined TARGET_ENV set "TARGET_ENV=REC"

if /I "%TARGET_ENV%"=="REC" (
    set "LAUNCHER=D:\RecEmatchAsUser.bat"
    set "WINDOW_TITLE=eMatch - Login REC"
) else if /I "%TARGET_ENV%"=="PRD" (
    set "LAUNCHER=D:\ProdEmatchAsUser.bat"
    set "WINDOW_TITLE=eMatch - Login PRD"
) else (
    echo [ERROR] Invalid environment: %TARGET_ENV%
    exit /b 1
)

set "RESOLUTION_SCRIPT=D:\setResolution.ps1"

echo [INFO] Environment: %TARGET_ENV%
echo [INFO] Launcher   : %LAUNCHER%

if exist "%RESOLUTION_SCRIPT%" (
    powershell -ExecutionPolicy Bypass -File "%RESOLUTION_SCRIPT%"
)

timeout /t 2 /nobreak >nul

call "%LAUNCHER%"

echo [INFO] Waiting for window to appear...

powershell -NoProfile -Command "$deadline=(Get-Date).AddSeconds(120); do { $p=Get-Process | Where-Object { $_.MainWindowTitle -like '*eMatch*' }; if ($p) { exit 0 }; Start-Sleep -Seconds 2 } while ((Get-Date) -lt $deadline); exit 1"

if errorlevel 1 (
    echo [ERROR] eMatch window did not appear
    exit /b 1
)

echo [SUCCESS] eMatch window detected
exit /b 0

