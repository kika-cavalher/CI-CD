Comparatif Inter-Arretes
import static com.kms.katalon.core.testobject.ObjectRepository.findWindowsObject

import java.awt.Robot
import java.awt.event.KeyEvent

import com.kms.katalon.core.windows.keyword.WindowsBuiltinKeywords as Windows

Robot robot = new Robot()

String menuName = 'Comparatif Inter'
String menuResultName = 'Comparatif inter-arrêtés'
String exportLabel = 'ComparatifInterArretes'

// Search and open menu
CustomKeywords.'commands.MenuNavigator.searchAndOpenMenuByText'(menuName, menuResultName, 60)

Windows.delay(40)

// Exporting Excel File
CustomKeywords.'commands.ExportFile.exportFromOutilsToNo'(exportLabel)

// Close all opened tabs
Windows.click(findWindowsObject('Object Repository/General/closeAllTabs'))
Windows.delay(1)

robot.keyPress(KeyEvent.VK_ENTER)
robot.keyRelease(KeyEvent.VK_ENTER)
