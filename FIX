Vamos trocar nesse teste o filtro recherche por filtro por status
Ja temos uma keyword fde filtro quero que use a mesma e só ACRESCENTE logica que faltar no file sem mudar nada
Deixa o mais generico possivel para reuso 
Imagens e elementos nunca na keyword sempre no dicionario separado


Oque vamos mudar:
-Tira a tab recherche e mudar data por:
- Vai na coluna Statut (tenta primeiro elemento depois se nao achar por imagem ) faz o houver e quando aparecer o filtro clica nele 
- Dentro do filtro a logica aqui muda. Primeiro precisa clicar no: Filtres numeriques, depois da TAB, escreve 1 e aperta o botao Fermer
- Confirma se o statys ta 1


O resto export e validação igual

import static com.kms.katalon.core.testcase.TestCaseFactory.findTestCase

import com.kms.katalon.core.model.FailureHandling
import com.kms.katalon.core.util.KeywordUtil
import com.kms.katalon.core.webui.keyword.WebUiBuiltInKeywords as WebUI
import com.kms.katalon.core.windows.keyword.WindowsBuiltinKeywords as Windows

import internal.GlobalVariable


// ======================================================
// EXPORT CONFIG
// ======================================================

String targetEnvironment = 'REC'
String exportLabel = 'EcartsDeCours'

String menuName = 'ecarts de cours dérivés organisés'
String menuResultName = 'ecarts de cours dérivés organisés'
String openedTabName = 'Dérivés organisés'
String asOfDateValue = '31/03/2026'

String scenarioName = 
	targetEnvironment +
		' Generer Corrections Validate Filter And Export'

String loginTestCase =
	'Component/Authentication/Login ' + targetEnvironment

String closeApplicationTestCase =
	'Component/Authentication/Close Application'


// ======================================================
// TIMEOUT CONFIG
// ======================================================

Integer menuTimeout = 60
Integer openedTabTimeout = 60
Integer filterTimeout = 5
Integer exportLoadDelay = 3


// ======================================================
// AUTHENTICATION
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Authenticate and open application\n' +
	'Environment: ' + targetEnvironment
)

WebUI.callTestCase(
	findTestCase(loginTestCase),
	[:],
	FailureHandling.STOP_ON_FAILURE
)


// ======================================================
// MENU NAVIGATION
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Open Ajustements IPV menu\n' +
	'Search: ' + menuName + '\n' +
	'Result: ' + menuResultName
)

CustomKeywords.'component.MenuNavigator.searchAndOpenMenuByText'(
	menuName,
	menuResultName,
	menuTimeout
)


// ======================================================
// OPENED TAB VALIDATION
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Validate opened tab\n' +
	'Expected tab: ' + openedTabName
)

CustomKeywords.'component.MenuNavigator.validateTabOpenedByName'(
	openedTabName,
	openedTabTimeout,
	targetEnvironment + '_AjustementsIPV'
)


// ======================================================
// RECHERCHE FILTER
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Apply Recherche filter\n' +
	'Filter: As Of Date\n' +
	'Value: ' + asOfDateValue
)

CustomKeywords.'component.RechercheFilterKeywords.applyRechercheAsOfDateFilter'(
	asOfDateValue,
	filterTimeout
)

CustomKeywords.'commands.ImageLoader.takeEvidence'(
	targetEnvironment +
	'_AjustementsIPV_FilterApplied.png'
)



// ======================================================
// EXPORT
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Wait for filtered content before export\n' +
	'Delay: ' + exportLoadDelay + ' seconds'
)

Windows.delay(exportLoadDelay)

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Export filtered Ajustements IPV table\n' +
	'Flow: Exports tab > Export button > Save > Non\n' +
	'Environment: ' + targetEnvironment + '\n' +
	'Export label: ' + exportLabel
)

CustomKeywords.'commands.ExportFile.exportFromTwoExportImagesToNo'(
	exportLabel,
	targetEnvironment
)


// ======================================================
// CLOSE APPLICATION
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Close application\n' +
	'Environment: ' + targetEnvironment
)

WebUI.callTestCase(
	findTestCase(closeApplicationTestCase),
	[:],
	FailureHandling.STOP_ON_FAILURE
)


// ======================================================
// FILE VALIDATION
// ======================================================

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Validate downloaded export\n' +
	'Environment: ' + targetEnvironment + '\n' +
	'Export label: ' + exportLabel + '\n' +
	'Validations:\n' +
	'- File exists\n' +
	'- File is not empty\n' +
	'- Data row quantity is greater than zero'
)

Map exportValidation =
	CustomKeywords.'commands.ExportFileValidationKeywords.validateExportedFileExistsAndHasRows'(
		targetEnvironment,
		exportLabel
	)

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Downloaded export validation passed\n' +
	'File: ' + exportValidation.fileName + '\n' +
	'Data rows: ' + exportValidation.dataRows + '\n' +
	'File size: ' + exportValidation.fileSize + ' bytes\n' +
	'Folder: ' + exportValidation.folderPath
)

KeywordUtil.markPassed(
	scenarioName + ' completed successfully.'
)
