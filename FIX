2) Runner fixo
C:\Gaia\run_katalon_visible_from_jenkins.bat
@echo off
setlocal
title GAIA_VISIBLE_RUNNER

set COMMAND_FILE=C:\Gaia\visible_katalon_command.bat
set STATUS_FILE=C:\Gaia\gaia_visible_status.txt
set LOG_FILE=C:\Gaia\gaia_visible_console.log

if exist "%STATUS_FILE%" del /f /q "%STATUS_FILE%"
if exist "%LOG_FILE%" del /f /q "%LOG_FILE%"

if not exist "%COMMAND_FILE%" (
    > "%STATUS_FILE%" echo EXIT_CODE=999
    exit /b 999
)

call "%COMMAND_FILE%" > "%LOG_FILE%" 2>&1
set EXIT_CODE=%ERRORLEVEL%

> "%STATUS_FILE%" echo EXIT_CODE=%EXIT_CODE%
exit /b %EXIT_CODE%
3) Placeholder do arquivo gerado
C:\Gaia\visible_katalon_command.bat
@echo off
exit /b 0



pipeline {
    agent {
        node {
            // Always use the same Jenkins node
            label 'GI5_SWDCFRNXGI55817'

            // Always use the same workspace
            customWorkspace 'D:\\jenkins\\work\\workspace\\GI5\\Gaia\\Gaia_Corrections'
        }
    }

    options {
        // Add timestamps to every console line
        timestamps()

        // Enable ANSI colors in console output
        ansiColor('xterm')

        // Prevent concurrent executions of this pipeline
        disableConcurrentBuilds()

        // Global timeout for the whole pipeline
        timeout(time: 75, unit: 'MINUTES')

        // Keep Jenkins history under control
        buildDiscarder(logRotator(numToKeepStr: '10', daysToKeepStr: '14'))

        // Manual checkout only
        skipDefaultCheckout(true)
    }

    parameters {
        choice(
            name: 'RUN_MODE',
            choices: ['VISIBLE', 'HEADLESS'],
            description: 'Choose how Chrome should run'
        )

        string(
            name: 'STARTUP_WAIT_SECONDS',
            defaultValue: '60',
            description: 'Seconds to wait after Gaia startup'
        )

        string(
            name: 'SUITE_TIMEOUT_MINUTES',
            defaultValue: '8',
            description: 'Maximum time allowed for each suite'
        )
    }

    environment {
        // Fixed workspace path
        WORK_DIR = 'D:\\jenkins\\work\\workspace\\GI5\\Gaia\\Gaia_Corrections'

        // Fixed project file path
        PROJECT_PATH = 'D:\\jenkins\\work\\workspace\\GI5\\Gaia\\Gaia_Corrections\\gaia-portal.prj'

        // Katalon Engine installation path
        KATALON_HOME = 'C:\\Users\\adgi5appjenkins\\Documents\\Katalon_Studio_Engine_Windows_64-8.1.0'

        // Git repository information
        GIT_URL      = 'https://bitbucket-mut.mycloud.intrabpce.fr/scm/gi5/gaia-portal.git'
        GIT_CRED     = '965eceec-b3a3-4ec5-8820-a857c87dd80f'

        // Reports folder
        REPORTS_DIR  = 'Reports'

        // Gaia startup task
        GAIA_START_TASK = 'GaiaBackFrontSetup'

        // Visible execution task and status file
        VISIBLE_TASK_NAME = 'GaiaKatalon_Visible'
        VISIBLE_RUNNER_BAT = 'C:\\Gaia\\run_katalon_visible_from_jenkins.bat'
        VISIBLE_STATUS_FILE = 'C:\\Gaia\\gaia_visible_status.txt'
    }

    stages {
        stage('Environment Setup') {
            steps {
                script {
                    echo "========================================"
                    echo "Starting Gaia pipeline"
                    echo "Build Number : ${env.BUILD_NUMBER}"
                    echo "Node         : ${env.NODE_NAME}"
                    echo "Workspace    : ${env.WORKSPACE}"
                    echo "Run Mode     : ${params.RUN_MODE}"
                    echo "Work Dir     : ${env.WORK_DIR}"
                    echo "Project Path : ${env.PROJECT_PATH}"
                    echo "========================================"
                }

                bat """
                    @echo off
                    REM Prepare reports folder

                    if exist "${env.REPORTS_DIR}" rmdir /s /q "${env.REPORTS_DIR}"
                    mkdir "${env.REPORTS_DIR}"

                    exit /b 0
                """
            }
        }

        stage('Start Gaia Backend and Frontend') {
            steps {
                bat """
                    @echo off
                    REM Start Gaia backend and frontend

                    schtasks /run /tn "${env.GAIA_START_TASK}"
                    exit /b 0
                """

                sleep(time: params.STARTUP_WAIT_SECONDS.toInteger(), unit: 'SECONDS')
            }
        }

        stage('Checkout gaia-portal') {
            steps {
                deleteDir()

                checkout([
                    $class: 'GitSCM',
                    branches: [[name: '*/master']],
                    userRemoteConfigs: [[url: env.GIT_URL, credentialsId: env.GIT_CRED]],
                    extensions: [
                        [$class: 'WipeWorkspace'],
                        [$class: 'CloneOption', shallow: false, noTags: false, timeout: 30]
                    ]
                ])

                echo "Checkout completed successfully"
            }
        }

        stage('Execute Gaia Suites Separately') {
            steps {
                script {
                    def suites = [
                        [name: 'PreLoad',                path: 'Test Suites/PreLoad'],
                        [name: 'Help',                   path: 'Test Suites/Help'],
                        [name: 'AddCorrection',          path: 'Test Suites/AddCorrection'],
                        [name: 'ComputationSelection',   path: 'Test Suites/Computation Selection'],
                        [name: 'CorrectedDuplication',   path: 'Test Suites/CorrectedDuplication'],
                        [name: 'CorrectionDetails',      path: 'Test Suites/CorrectionDetails'],
                        [name: 'DownloadInvalidRecords', path: 'Test Suites/DownloadInvalidRecords']
                    ]

                    for (suite in suites) {
                        catchError(
                            buildResult: 'UNSTABLE',
                            stageResult: 'FAILURE',
                            catchInterruptions: false
                        ) {
                            timeout(time: params.SUITE_TIMEOUT_MINUTES.toInteger(), unit: 'MINUTES') {
                                runGaiaSuite(suite)
                            }
                        }
                    }
                }
            }
        }
    }

    post {
        always {
            archiveArtifacts artifacts: 'Reports/**/*', allowEmptyArchive: true, excludes: '**/*.lck'

            catchError(buildResult: 'UNSTABLE', stageResult: 'FAILURE', catchInterruptions: false) {
                junit allowEmptyResults: true, testResults: 'Reports/**/JUnit_Report.xml, Reports/**/junit*.xml, Reports/**/TEST-*.xml'
            }
        }

        aborted {
            bat """
                @echo off
                REM Stop current visible execution immediately

                schtasks /end /tn "${env.VISIBLE_TASK_NAME}" >nul 2>&1
                taskkill /IM katalonc.exe /T /F >nul 2>&1
                taskkill /IM chrome.exe /T /F >nul 2>&1
                taskkill /IM chromedriver.exe /T /F >nul 2>&1

                exit /b 0
            """
        }
    }
}

/**
 * Execute one suite.
 * Visible mode uses Task Scheduler.
 * Headless mode executes directly in Jenkins.
 */
def runGaiaSuite(Map suite) {
    def reportPath = "${env.WORK_DIR}\\${env.REPORTS_DIR}\\${suite.name}"

    echo "========================================"
    echo "Starting suite execution"
    echo "Suite Name   : ${suite.name}"
    echo "Suite Path   : ${suite.path}"
    echo "Run Mode     : ${params.RUN_MODE}"
    echo "Project Path : ${env.PROJECT_PATH}"
    echo "Report Path  : ${reportPath}"
    echo "========================================"

    bat """
        @echo off
        REM Prepare suite report folder

        if exist "${reportPath}" rmdir /s /q "${reportPath}"
        mkdir "${reportPath}"

        exit /b 0
    """

    if (params.RUN_MODE == 'HEADLESS') {
        def chromeArgs = buildChromeArgs()

        executeKatalon(
            location: env.KATALON_HOME,
            executeArgs:
                "-projectPath=\"${env.PROJECT_PATH}\" " +
                "-testSuitePath=\"${suite.path}\" " +
                "-browserType=\"Chrome\" " +
                "-consoleLog -logLevel=DEBUG " +
                "-reportFolder=\"${reportPath}\" " +
                chromeArgs,
            version: '',
            x11Display: '',
            xvfbConfiguration: ''
        )

        echo "Headless suite finished: ${suite.name}"
        return
    }

    bat """
        @echo off
        REM Clean only current visible leftovers

        schtasks /end /tn "${env.VISIBLE_TASK_NAME}" >nul 2>&1
        taskkill /IM katalonc.exe /T /F >nul 2>&1
        taskkill /IM chrome.exe /T /F >nul 2>&1
        taskkill /IM chromedriver.exe /T /F >nul 2>&1

        if exist "${env.VISIBLE_STATUS_FILE}" del /f /q "${env.VISIBLE_STATUS_FILE}" >nul 2>&1

        exit /b 0
    """

    createVisibleRunnerFile(suite, reportPath)

    bat """
        @echo off
        REM Trigger Task Scheduler in the visible session

        schtasks /run /tn "${env.VISIBLE_TASK_NAME}"
        exit /b 0
    """

    waitForVisibleSuite(suite.name)

    def exitCode = bat(
        returnStdout: true,
        script: """
            @echo off
            if not exist "${env.VISIBLE_STATUS_FILE}" (
                echo 999
                exit /b 0
            )

            for /f "tokens=2 delims==" %%A in ('type "${env.VISIBLE_STATUS_FILE}" ^| findstr /B "EXIT_CODE="') do echo %%A
            exit /b 0
        """
    ).trim()

    echo "Visible suite exit code for ${suite.name}: ${exitCode}"

    if (exitCode != '0') {
        error("Suite ${suite.name} failed with exit code ${exitCode}")
    }

    echo "Visible suite finished successfully: ${suite.name}"
}

/**
 * Rewrite the same Task Scheduler runner file for each suite.
 * Only one bat file is used.
 */
def createVisibleRunnerFile(Map suite, String reportPath) {
    bat """
        @echo off
        (
            echo @echo off
            echo setlocal
            echo title GAIA_VISIBLE_RUNNER
            echo cd /d "${env.WORK_DIR}"
            echo call "C:\\Gaia\\set_java_17_gaia.bat"
            echo "${env.KATALON_HOME}\\katalonc.exe" -projectPath="${env.PROJECT_PATH}" -testSuitePath="${suite.path}" -browserType="Chrome" -consoleLog -logLevel=DEBUG -reportFolder="${reportPath}"
            echo set EXIT_CODE=%%ERRORLEVEL%%
            echo ^> "${env.VISIBLE_STATUS_FILE}" echo EXIT_CODE=%%EXIT_CODE%%
            echo exit /b %%EXIT_CODE%%
        ) > "${env.VISIBLE_RUNNER_BAT}"

        echo ===== FILE CREATED IN C:\\Gaia =====
        type "${env.VISIBLE_RUNNER_BAT}"
        echo ====================================
    """
}

/**
 * Wait silently until the visible suite writes the status file.
 */
def waitForVisibleSuite(String suiteName) {
    powershell """
        \$statusFile = "${env.VISIBLE_STATUS_FILE}"
        \$timeoutSeconds = 480
        \$start = Get-Date

        while (-not (Test-Path \$statusFile)) {
            if (((Get-Date) - \$start).TotalSeconds -ge \$timeoutSeconds) {
                Write-Error "Timeout waiting for visible suite: ${suiteName}"
                exit 1
            }

            Start-Sleep -Seconds 2
        }

        Write-Host "Status file found for suite: ${suiteName}"
    """
}

/**
 * Build Chrome arguments for headless mode only.
 */
def buildChromeArgs() {
    def args = [
        '--headless=new',
        '--window-size=1920,1080',
        '--no-sandbox',
        '--disable-gpu',
        '--disable-dev-shm-usage'
    ]

    return "--config -webui.chrome.args='${args.join(' ')}'"
}
