pipeline {
    agent {
        node {
            label 'GI5_SWDCFRNXGI55817'
            customWorkspace 'D:\\jenkins\\work\\workspace\\GI5\\Gaia\\Gaia_Corrections'
        }
    }

    options {
        timestamps()
        ansiColor('xterm')
        disableConcurrentBuilds()
        timeout(time: 20, unit: 'MINUTES')
        buildDiscarder(logRotator(numToKeepStr: '10', daysToKeepStr: '14'))
        skipDefaultCheckout(true)
    }

    environment {
        GIT_URL = 'https://bitbucket-mut.mycloud.intrabpce.fr/scm/gi5/gaia-portal.git'
        GIT_CRED = '965eceec-b3a3-4ec5-8820-a857c87dd80f'
        GAIA_START_TASK = 'GaiaBackFrontSetup'
        VISIBLE_TASK_NAME = 'GaiaKatalon_Visible'
        STATUS_FILE = 'C:\\Gaia\\gaia_visible_status.txt'
    }

    stages {
        stage('Start Gaia') {
            steps {
                bat """
                    @echo off
                    schtasks /run /tn "${env.GAIA_START_TASK}"
                    exit /b 0
                """
                sleep(time: 60, unit: 'SECONDS')
            }
        }

        stage('Checkout') {
            steps {
                deleteDir()

                checkout([
                    $class: 'GitSCM',
                    branches: [[name: '*/master']],
                    userRemoteConfigs: [[url: env.GIT_URL, credentialsId: env.GIT_CRED]],
                    extensions: [
                        [$class: 'WipeWorkspace'],
                        [$class: 'CloneOption', shallow: false, noTags: false, timeout: 30]
                    ]
                ])
            }
        }

        stage('Run PreLoad Visible') {
            steps {
                bat """
                    @echo off
                    if exist "${env.STATUS_FILE}" del /f /q "${env.STATUS_FILE}" >nul 2>&1
                    schtasks /run /tn "${env.VISIBLE_TASK_NAME}"
                    exit /b 0
                """

                powershell """
                    \$statusFile = "${env.STATUS_FILE}"
                    \$timeoutSeconds = 600
                    \$start = Get-Date

                    while (-not (Test-Path \$statusFile)) {
                        if (((Get-Date) - \$start).TotalSeconds -ge \$timeoutSeconds) {
                            Write-Error "Timeout waiting for PreLoad status file"
                            exit 1
                        }
                        Start-Sleep -Seconds 2
                    }

                    Get-Content \$statusFile
                """
            }
        }
    }

    post {
        aborted {
            bat """
                @echo off
                schtasks /end /tn "${env.VISIBLE_TASK_NAME}" >nul 2>&1
                taskkill /IM katalonc.exe /T /F >nul 2>&1
                taskkill /IM chrome.exe /T /F >nul 2>&1
                taskkill /IM chromedriver.exe /T /F >nul 2>&1
                exit /b 0
            """
        }
    }
}






C:\Gaia\run_katalon_visible_from_jenkins.bat
@echo off
setlocal
title GAIA_VISIBLE_RUNNER

set STATUS_FILE=C:\Gaia\gaia_visible_status.txt

if exist "%STATUS_FILE%" del /f /q "%STATUS_FILE%"

call "C:\Gaia\set_java_17_gaia.bat"
cd /d "D:\jenkins\work\workspace\GI5\Gaia\Gaia_Corrections"

"C:\Users\adgi5appjenkins\Documents\Katalon_Studio_Engine_Windows_64-8.1.0\katalonc.exe" ^
-projectPath="D:\jenkins\work\workspace\GI5\Gaia\Gaia_Corrections\gaia-portal.prj" ^
-testSuitePath="Test Suites/PreLoad" ^
-browserType="Chrome" ^
-consoleLog ^
-logLevel=DEBUG ^
-reportFolder="D:\jenkins\work\workspace\GI5\Gaia\Gaia_Corrections\Reports\PreLoad"

set EXIT_CODE=%ERRORLEVEL%
echo EXIT_CODE=%EXIT_CODE%>"%STATUS_FILE%"

exit /b %EXIT_CODE%







C:\Gaia\run_katalon_visible_from_jenkins.bat

