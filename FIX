Pode me ajudar a transformar isso me uma keyword:


import com.kms.katalon.core.util.KeywordUtil

String environment = ''
String scenarioName = ''

try {
	environment = targetEnvironment
} catch (Exception ignored) {
	environment = ''
}

try {
	scenarioName = exportScenarioName
} catch (Exception ignored) {
	scenarioName = ''
}

environment = environment?.trim()?.toUpperCase()

if (environment == null || environment == '') {
	environment = 'REC'
}

if (environment == 'PROD') {
	environment = 'PRD'
}

if (scenarioName == null || scenarioName.trim() == '') {
	scenarioName = 'ModuleDesResponsabilite_ValidateDimensions'
}

String exportLabel = environment + '_' + scenarioName

KeywordUtil.logInfo(
	'Resolved export environment: ' + environment
)

KeywordUtil.logInfo(
	'Resolved export scenario name: ' + scenarioName
)

KeywordUtil.logInfo(
	'Resolved export file label: ' + exportLabel
)

CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Export expanded table\n' +
	'Environment: ' + environment + '\n' +
	'Export file: ' + exportLabel
)

CustomKeywords.'commands.ExportFile.exportFromOutilsToNo'(
	exportLabel
)

CustomKeywords.'commands.ImageLoader.takeEvidence'(
	exportLabel + '.png'
)



eu vou colocar no ExportFile então quando chamar comandos do ExportFile pode vir direto pois estara no mesmo file

ai precisa trocar isso
CustomKeywords.'jiraIntegration.JiraReportSteps.note'(
	'Download favorite view\n' +
	'Test Case: Component/Export Files/Export Current Table By Outils\n' +
	'Purpose: export the favorite data for comparison'
)

WebUI.callTestCase(
	findTestCase(
		'Component/Export Files/Export Current Table By Outils'
	),
	[:],
	FailureHandling.STOP_ON_FAILURE
)

CustomKeywords.'commands.ImageLoader.takeEvidence'(
	targetEnvironment +
	'_GUI_FavoriteViewDownloaded.png'
)

Por uma chamada da keyword (o note nao funciona como keyword pode tirar e deixar no tc)

